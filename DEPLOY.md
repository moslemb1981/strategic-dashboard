# یادداشت مهم درباره‌ی این تحویل

بررسی کامل بکاپ دیشب (strategic38-NewChange) انجام شد — **تمام بندهای
۱۴۵ تا ۱۶۹ که قبلاً تحویل داده بودم، از قبل درست توی کدتون اعمال شده
بودن** (فقط متن مستندات DEPLOY.md خودتون یه‌جا عقب مونده بود، نه خودِ
کد — با چک مستقیم چندتا ویژگی کلیدی تأیید شد).

**فقط یه مورد جا مونده بود: بند ۱۷۰ (خلاصه‌ی مدیریتی صفحه‌ی الزامات
قانونی)** — چون دفعه‌ی قبل بدون سرور زنده تحویلش داده بودم. این‌بار
با دیتابیس واقعی‌تون کامل تست شد (نتیجه: ۱۱۱ کل، ۶۴/۴۹ قانونی/سازمانی
و...، همه‌چیز درست). همچنین **هر ۲۲ صفحه‌ی سامانه** (شامل نقشه‌ی هر
۵ کسب‌وکار) با همین دیتابیس واقعی چک شد — همه سالم.

---

۱۷۰. افزودن «خلاصه‌ی مدیریتی» بالای صفحه‌ی «الزامات قانونی» — یه نوار
    ۶ کارتی: کل الزامات، تفکیک قانونی/سازمانی، تفکیک درون/برون‌سازمانی،
    تعداد دارای ریسک شناسایی‌شده، تعداد دارای فرصت شناسایی‌شده، و تعداد
    وصل‌شده به یه عامل PESTEL واقعی.

    ✅ این‌بار با سرور زنده و دیتابیس واقعی‌تون تست شد (نه فقط بررسی
    سینتکس) — نتیجه: ۱۱۱ کل، ۶۴/۴۹ قانونی/سازمانی، ۳۳/۱۶ درون/برون،
    ۱۰۱ دارای ریسک، ۱۰۷ دارای فرصت، ۴۹ وصل به PESTEL.

    فایل‌هایی که باید overwrite شوند:
    - strategic/views.py
    - templates/strategic/legal_requirements.html

    نیازی به هیچ دستوری نیست — فقط فایل‌ها را جایگزین و سرور را ری‌استارت
    کنید.

۱۷۱. بازطراحی کارت‌های خلاصه‌ی مدیریتی «الزامات قانونی» + ساخت همون
    طراحی برای «تحلیل ذینفعان» — طبق بازخورد کاربر که کارت‌های قبلی
    زیادی ساده بودن.

    **ظاهر جدید کارت‌ها**: آیکون اختصاصی + نوار رنگی بالای کارت +
    سایه‌ی نرم + انیمیشن هاور (بالا رفتن کارت) + بج درصد برای شاخص‌های
    تک‌عددی + نوار تناسب دورنگ برای شاخص‌های زوجی (مثل قانونی/سازمانی).

    **کارت‌های صفحه‌ی «تحلیل ذینفعان» (جدید، ۷ کارت)** — تست‌شده با
    داده‌ی واقعی:
    - کل ذینفعان: ۱۹۰
    - درون‌سازمانی/برون‌سازمانی: ۱۳۹ / ۵۱
    - دارای نیاز شناسایی‌شده: ۱۹۰ (۱۰۰٪)
    - دارای ریسک شناسایی‌شده: ۱۹۰ (۱۰۰٪)
    - دارای فرصت شناسایی‌شده: ۱۸۲ (۹۶٪)
    - ریسک بحرانی (۲۰٪ بالا): ۴۹
    - **ریسک بحرانی دارای اقدام/پروژه**: ۴۲ (۸۶٪) — نشون می‌ده چقدر از
      ریسک‌های جدی، واقعاً پیگیری عملی دارن

    هر دو صفحه با سرور زنده و دیتابیس واقعی‌تون کامل تست شدن.

    فایل‌هایی که باید overwrite شوند:
    - strategic/views.py
    - templates/strategic/legal_requirements.html
    - templates/strategic/stakeholders.html

    نیازی به هیچ دستوری نیست — فقط فایل‌ها را جایگزین و سرور را ری‌استارت
    کنید.

۱۷۲. کاهش ارتفاع کارت‌های خلاصه‌ی مدیریتی (بند ۱۷۱) — طبق بازخورد
    کاربر که ارتفاعشون زیاد بود. چیدمان از عمودی (آیکون بالا، عدد
    وسط، برچسب پایین) به **افقی** (آیکون کنار، عدد و برچسب فشرده کنار
    هم) تغییر کرد — همون اطلاعات، تقریباً نصف ارتفاع قبلی، همچنان با
    آیکون و نوار تناسب. روی هر دو صفحه (الزامات قانونی + تحلیل
    ذینفعان) با سرور زنده تست شد.

    فایل‌هایی که باید overwrite شوند:
    - templates/strategic/legal_requirements.html
    - templates/strategic/stakeholders.html

    نیازی به هیچ دستوری نیست — فقط فایل‌ها را جایگزین و سرور را ری‌استارت
    کنید.

۱۷۳. رفع دو مشکل ظاهری کارت‌های خلاصه (بند ۱۷۲) در هر دو صفحه (الزامات
    قانونی + تحلیل ذینفعان):

    الف) آیکون کارت «درون/برون‌سازمانی» (`fa-building-shield`) لود
    نمی‌شد — احتمالاً توی نسخه‌ی محلی فونت‌اویسام شما نبود. با
    `fa-shield-halved` جایگزین شد — آیکونی که از قبل ۴ بار توی همین
    سامانه (صفحه‌ی سناریوها) درست کار می‌کرد، پس مطمئنم لود می‌شه.

    ب) متن برچسب «درون/برون‌سازمانی» بریده می‌شد — سایز فونت برچسب‌ها
    کمی کوچیک‌تر شد (۹.۵ به ۸.۵ پیکسل) و فاصله‌ی اضافی اطراف «/» هم
    حذف شد تا کامل جا بشه.

    فایل‌هایی که باید overwrite شوند:
    - templates/strategic/legal_requirements.html
    - templates/strategic/stakeholders.html

    نیازی به هیچ دستوری نیست — فقط فایل‌ها را جایگزین و سرور را ری‌استارت
    کنید.

۱۷۳. رفع دو مشکل کارت‌های خلاصه‌ی مدیریتی (بند ۱۷۱-۱۷۲) — طبق گزارش
    کاربر:

    الف) آیکون کارت «درون/برون‌سازمانی» توی هر دو صفحه لود نمی‌شد —
    چون آیکونی که انتخاب کرده بودم توی بسته‌ی آفلاین فونت‌اوسام این
    سامانه موجود نبود. به `fa-building` عوض شد — دقیقاً همون آیکونی
    که توی خودِ منوی کناری («تحلیل محیط داخلی») همیشه استفاده و درست
    نمایش داده می‌شه.

    ب) برچسب «درون / برون‌سازمانی» توی صفحه‌ی ذینفعان با سه‌نقطه بریده
    می‌شد — چون فونت با یه قانون «نه بشکن، فقط ببر» ترکیب شده بود. الان
    اجازه‌ی شکستن به دو خط داده شد (به‌جای بریدن)، پس متن کامل دیده
    می‌شه.

    هر دو مورد با سرور زنده و دیتابیس واقعی‌تون تست شد.

    فایل‌هایی که باید overwrite شوند:
    - templates/strategic/legal_requirements.html
    - templates/strategic/stakeholders.html

    نیازی به هیچ دستوری نیست — فقط فایل‌ها را جایگزین و سرور را ری‌استارت
    کنید.

۱۷۴. جابه‌جایی دو باکس بالای صفحه‌ی «تحلیل ذینفعان» — طبق درخواست
    کاربر: «فرصت» الان سمت راست، «ریسک» سمت چپه (قبلاً برعکس بود).
    این تغییر فقط روی تمپلیت بود (نه دیتابیس)، با سرور زنده تست شد.

    فایلی که باید overwrite شود:
    - templates/strategic/stakeholders.html

    نیازی به هیچ دستوری نیست — فقط فایل را جایگزین و سرور را ری‌استارت
    کنید.

۱۷۵. رفع باگ واقعی «موقع کلیک روی مداد ذینفع، چک‌باکس پروژه‌های مرتبط
    تیک‌خورده نشون نمی‌ده» — طبق گزارش کاربر. دو تا باگ به‌هم قفل شده
    بودن:

    الف) موقع ست‌کردن چک‌باکس‌های «عوامل Porter مرتبط»، انتخابگر
    اشتباهی هر دو پیکر (Porter و پروژه‌ها) رو با هم لمس می‌کرد و
    چک‌باکس‌های پروژه رو زودتر پاک می‌کرد.

    ب) تابعی که کلاس بصری «انتخاب‌شده» رو روی برچسب‌ها اعمال می‌کنه
    (`syncPorterPicked`)، **قبل از** ست‌شدن چک‌باکس‌های پروژه صدا زده
    می‌شد — پس حتی وقتی چک‌باکس واقعاً تیک می‌خورد، نمایش بصریش
    هیچ‌وقت به‌روز نمی‌شد.

    هر دو اصلاح شد. این‌بار نه فقط با بررسی کد، بلکه با **شبیه‌سازی
    واقعی کلیک در مرورگر** (jsdom) روی داده‌ی واقعی خودتون تست شد —
    هم چک‌باکس تیک خورد، هم کلاس بصری «picked» درست اعمال شد.

    فایلی که باید overwrite شود:
    - templates/strategic/stakeholders.html

    نیازی به هیچ دستوری نیست — فقط فایل را جایگزین و سرور را ری‌استارت
    کنید.

۱۷۶. اصلاح آیکون «پروژه‌های تحول مرتبط» — طبق نظر درست کاربر که آیکون
    قبلی (نمودار سازمانی/فلوچارت) زیاد به مفهوم «پروژه» نمی‌خورد. الان
    به `fa-route` تغییر کرد — دقیقاً همون آیکونی که خودِ منوی کناری
    سایت برای «پروژه‌های تحول» همیشه استفاده می‌کنه (هماهنگی کامل).
    هم بج کنار ستون جدول، هم آیکون کارت خلاصه‌ی «بحرانی + اقدام» عوض
    شد. بج مربوط به SWOT (که آیکون قبلی براش واقعاً مناسب بود) دست‌
    نخورده موند.

    فایلی که باید overwrite شود:
    - templates/strategic/stakeholders.html

    نیازی به هیچ دستوری نیست — فقط فایل را جایگزین و سرور را ری‌استارت
    کنید.

۱۷۷. رنگ‌آمیزی مجدد باکس «استاندارد مرتبط» در صفحات PESTEL و Porter —
    طبق بازخورد مدیر که با رنگ خاکستری روشن قبلی تقریباً با پس‌زمینه
    یکی می‌شد و دیده نمی‌شد. الان با پس‌زمینه‌ی بنفش کم‌رنگ و متن بنفش
    پررنگ (هماهنگ با خانواده‌ی رنگی سامانه، متمایز از بج‌های قرمز/سبز
    کنارش) کاملاً قابل‌مشاهده‌ست. با یه نمونه‌ی واقعی (استاندارد
    «۱۰۰۰۴ و۹۰۰۱») تست شد.

    فایل‌هایی که باید overwrite شوند:
    - templates/strategic/pestel.html
    - templates/strategic/porter.html

    نیازی به هیچ دستوری نیست — فقط فایل‌ها را جایگزین و سرور را ری‌استارت
    کنید.

۱۷۸. افزودن ایموجی 🏅 قبل از متن باکس «استاندارد مرتبط» در PESTEL و
    Porter — طبق درخواست کاربر، بعد از رنگ‌آمیزی بند ۱۷۷. چون ایموجی
    یونیکده (نه آیکون فونت‌اوسام)، هیچ ریسک عدم‌نمایش نداره — مستقیم
    توسط خودِ مرورگر رندر می‌شه. با نمونه‌ی واقعی تست شد: «🏅 10004
    و9001».

    ⚠️ نکته: توی Porter، محل واقعی این باکس یه تمپلیت جزئی جداگانه
    (`_porter_item.html`) بود، نه خودِ porter.html.

    فایل‌هایی که باید overwrite شوند:
    - templates/strategic/pestel.html
    - templates/strategic/_porter_item.html

    نیازی به هیچ دستوری نیست — فقط فایل‌ها را جایگزین و سرور را ری‌استارت
    کنید.

۱۷۹. بازطراحی کامل صفحه‌ی «پروژه‌های تحول» — طبق درخواست کاربر، پس از
    بررسی دقیق‌تر کد. **این تغییر فقط روی تمپلیت (کد نمایش) بود، هیچ
    نیازی به دیتابیس اصلی نداشت** — چون فقط نحوه‌ی نمایش رکوردهای
    موجود عوض شد، نه خودِ داده.

    الف) **نمودار گانت و نوار اسکرول‌دار بالای صفحه کامل حذف شد** —
    بعد از بررسی مشخص شد این نمودار فقط از تاریخ شروع/پایان + یه عدد
    پیشرفت (نه داده‌ی واقعی ماهانه) استفاده می‌کرد؛ «ماه‌به‌ماه» فقط
    خط‌کش تقویمی نمودار بود، نه داده‌ی ذخیره‌شده. چون کاربر داده‌ی
    ماهانه‌ای نداره که نیاز به این نما داشته باشه، کل بخش (همراه با
    کتابخونه‌ی frappe-gantt) حذف شد.

    ب) **لیست افقی تمام‌عرض قبلی، به کارت‌های شبکه‌ای (۴ کارت در هر
    ردیف) تبدیل شد** — هر کارت: کد+عنوان، وضعیت (رنگی)، واحد مسئول،
    تاریخ شروع/پایان، نوار پیشرفت، بج‌های ارتباطی (اهداف/شاخص/TOWS/
    ریسک/ریشه‌ی راهبردی — همون‌هایی که قبلاً بودن)، و دکمه‌های ویرایش/
    حذف. همه‌ی اطلاعات قبلی حفظ شد، فقط چیدمانش عوض شد.

    ج) **جست‌وجوی زنده** اضافه شد — روی عنوان، کد، واحد مسئول، معاونت
    و کارگروه هم‌زمان جست‌وجو می‌کنه، بدون نیاز به رفرش صفحه.

    همه‌چیز با سرور زنده تست شد؛ جست‌وجو هم با **شبیه‌سازی واقعی تایپ
    در مرورگر** (نه فقط بررسی کد) روی چند پروژه‌ی آزمایشی تست و بعدش
    کامل پاک شد (بدون تغییر دائمی روی دیتابیس موقت شما).

    فایلی که باید overwrite شود:
    - templates/strategic/roadmap.html

    نیازی به هیچ دستوری نیست — فقط فایل را جایگزین و سرور را ری‌استارت
    کنید. (نیازی به فایل‌های frappe-gantt هم دیگه نیست، ولی نگه‌داشتنشون
    توی پوشه‌ی static ضرری نداره.)

۱۸۰. کاهش ارتفاع کارت‌های «پروژه‌های تحول» (بند ۱۷۹) + سایه‌ی محسوس‌تر —
    طبق درخواست کاربر. فاصله‌های داخلی و بین‌عنصری فشرده‌تر شدن (بدون
    حذف هیچ اطلاعاتی)، و سایه از یه لایه‌ی ملایم به **دو لایه** (یه
    سایه‌ی نرم و پخش‌شده‌ی زیر کارت + یه سایه‌ی نزدیک‌تر برای عمق) تغییر
    کرد تا کارت‌ها بیشتر «شناور» به‌نظر برسن؛ موقع هاور هم این سایه
    قوی‌تر می‌شه. فقط تمپلیت بود، بدون نیاز به دیتابیس.

    فایلی که باید overwrite شود:
    - templates/strategic/roadmap.html

    نیازی به هیچ دستوری نیست — فقط فایل را جایگزین و سرور را ری‌استارت
    کنید.

۱۸۱. سه اصلاح روی پنجره‌ی «آشنایی با SWOT» — طبق درخواست کاربر:

    الف) دکمه‌ی «آشنایی با SWOT» از استایل خنثی (که با دکمه‌ی «خروجی
    PDF» کنارش یکی به‌نظر می‌رسید) به یه دکمه‌ی **بنفش پررنگ و متمایز**
    (با سایه‌ی محسوس) تغییر کرد.

    ب) پنجره‌ی آشنایی حالا **خودکار، هر بار که وارد صفحه‌ی SWOT می‌شید**
    (از منو یا هر مسیر دیگه) باز می‌شه.

    ج) کلیک روی **پس‌زمینه‌ی بیرون از کادر** هم پنجره رو می‌بنده (نه
    فقط ضربدر بالای کادر) — کلیک روی خودِ محتوای کادر همچنان اون رو
    نمی‌بنده.

    هر سه مورد با **شبیه‌سازی واقعی رفتار مرورگر** (jsdom، نه فقط
    بررسی کد) تست شد. فقط تمپلیت بود، بدون نیاز به دیتابیس.

    فایلی که باید overwrite شود:
    - templates/strategic/swot.html

    نیازی به هیچ دستوری نیست — فقط فایل را جایگزین و سرور را ری‌استارت
    کنید.

۱۸۲. افزودن انیمیشن «تپش نور» ملایم به دکمه‌ی «آشنایی با SWOT» — طبق
    درخواست کاربر. با همون رنگ بنفش دکمه، سایه‌ی دورش هر ۲.۲ ثانیه
    نرم زیاد و کم می‌شه (نه چشمک‌زن یا آزاردهنده) تا توجه رو به خودش
    جلب کنه. موقع هاوردن، انیمیشن متوقف و سایه‌ی ثابت قوی‌تر می‌شه.
    فقط تمپلیت بود، بدون نیاز به دیتابیس.

    فایلی که باید overwrite شود:
    - templates/strategic/swot.html

    نیازی به هیچ دستوری نیست — فقط فایل را جایگزین و سرور را ری‌استارت
    کنید.

۱۸۳. ساخت ماژول جدید «هوش بازار» — یه زیرساخت کاملاً تازه، طبق طراحی
    توافق‌شده با کاربر:

    الف) مدل جدید `MarketIntelRecord` — هر بار به‌روزرسانی، یه رکورد
    جدید اضافه می‌کنه (نه جایگزینی)، تا تاریخچه/روند هم حفظ بشه.
    دسته‌بندی: نرخ ارز، مواد اولیه، رقبا، سایر.

    ب) صفحه‌ی جدید «هوش بازار» — لینک منو دقیقاً بعد از «پروژه‌های
    تحول» (طبق درخواست کاربر). دکمه‌ی «به‌روزرسانی از اینترنت» **فقط
    برای ادمین (superuser)** قابل‌مشاهده‌ست — دقیقاً طبق تصمیم کاربر
    («نمی‌خوام کس دیگه‌ای فعلاً انجام بده»).

    ج) **مکانیزم کلیدی**: با کلیک دکمه، فقط همون‌لحظه سرور جنگو (نه
    مرورگر، نه بقیه‌ی سامانه) یه درخواست کوتاه به اینترنت می‌زنه، داده
    رو می‌گیره و ذخیره می‌کنه — بقیه‌ی زمان سامانه دقیقاً به همون
    اندازه‌ی قبل آفلاینه.

    د) **نمونه‌ی اولیه‌ی کارکردی**: نرخ برابری USD به EUR/GBP (منبع:
    Frankfurter/ECB، رایگان و بدون نیاز به کلید). ⚠️ این فقط یه نمونه‌ی
    اثبات‌مفهومه، نه نرخ دلار/ریال ایران — چون منابع بین‌المللی استاندارد
    نرخ ریال رو (به‌خاطر تحریم‌ها) ندارن. برای نرخ ریال باید یه منبع
    اختصاصی ایرانی انتخاب بشه (قدم بعدی).

    ⚠️ **یادداشت مهم تست**: محیط کاری من محدودیت شبکه‌ی خاص خودش رو
    داره (فقط چندتا دامنه‌ی مشخص مجازن) — پس نتونستم خودِ اتصال
    اینترنتی رو زنده تست کنم (۴۰۳ از طرف محیط خودم گرفتم، نه از سایت
    مقصد). ولی با **شبیه‌سازی پاسخ موفق** (mock)، کل مسیر بعدی (ذخیره‌
    سازی، ثبت «چه‌کسی/چه‌زمانی»، پیام موفقیت، نمایش توی صفحه) کامل
    تست و تأیید شد. روی کامپیوتر خودتون (بدون این محدودیت) باید مستقیم
    کار کنه — لطفاً بعد از دپلوی یه‌بار امتحان کنید و نتیجه رو بگید.

    یه فایل جدید به `requirements.txt` اضافه شد: `requests==2.33.1`
    (برای درخواست‌های اینترنتی) — قبلش این کتابخونه نصب نبود.

    یه migration جدید لازمه:
    python manage.py makemigrations strategic
    python manage.py migrate
    pip install -r requirements.txt

    فایل‌هایی که باید overwrite/اضافه شوند:
    - strategic/models.py
    - strategic/views.py
    - strategic/urls.py
    - strategic/templatetags/jalali_tags.py
    - strategic/migrations/0076_marketintelrecord.py (فایل جدید)
    - templates/strategic/market_intel.html (فایل جدید)
    - templates/strategic/base.html
    - requirements.txt

۱۸۴. رفع مشکل «دقت اعشار خیلی طولانی» توی مقادیر شاخص‌های عملیاتی و
    کلان — طبق گزارش دقیق کاربر با نمونه‌های واقعی (مثل
    «58.139534883720934»، «91.30561209653969»).

    **علت پیدا شد**: فیلدهای هدف/عملکرد/درصد تحقق متنی (نه عددی)
    هستن، و مقادیری که احتمالاً از یه محاسبه‌ی خودکار (تقسیم) اومده
    بودن، بدون گرد کردن مستقیم ذخیره شده بودن.

    **راه‌حل سه‌لایه** (هر سه با تست واقعی تأیید شد):

    ۱) **دستور اصلاح داده‌های موجود** — یه‌بار روی کل بانک شاخص‌های
    عملیاتی و کلان اجرا می‌شه، هر عدد اعشاری طولانی رو به حداکثر ۲
    رقم گرد می‌کنه (مثلاً 58.139534883720934 → 58.14، و اگه بعد از
    گرد کردن عدد صحیح شد، بدون اعشار نشون می‌ده). مقادیر غیرعددی
    (مثل «—» یا یادداشت) دست‌نخورده می‌مونن:
    python manage.py clean_kpi_decimal_precision

    ۲) **ورودی اکسل هر دو صفحه** از این به بعد خودکار همین گردکردن
    رو انجام می‌ده — پس اگه دوباره از اکسل وارد کنید، مشکل تکرار
    نمی‌شه.

    ۳) **ثبت دستی از فرم (مداد)** هم همین گردکردن رو داره — حتی اگه
    یکی دستی یه عدد اعشاری طولانی تایپ/پیست کنه، خودکار گرد می‌شه.

    یه نکته‌ی فنی: تابع مشترک این منطق رو به `forms.py` منتقل کردم
    (به‌جای تعریف جداگانه در چند جا) تا هم فرم‌ها هم دستور مدیریتی از
    همون یه نسخه استفاده کنن.

    یه دستور لازمه (روی دیتابیس واقعی که تازه گرفتید):
    python manage.py clean_kpi_decimal_precision

    فایل‌هایی که باید overwrite/اضافه شوند:
    - strategic/views.py
    - strategic/forms.py
    - strategic/management/commands/clean_kpi_decimal_precision.py (فایل جدید)

۱۸۵. تعیین محور استراتژیک برای ۵۱ هدف بدون محور — طبق تصمیم مدیر
    («هیچ هدفی نباید بدون محور باشه»، بند مشابه قبلی). هر ۵۱ هدف
    دقیقاً از منظر «مالی» یا «مشتری» بودن (هیچ‌کدوم از منظر «فرآیند»/
    «یادگیری» بدون محور نبود، چون اون‌ها ذاتاً با محورها یکی‌ان).

    منطق تخصیص (نمونه‌ها):
    - اهداف عملیاتی/کارایی/رشد کلی (مثل «سود عملیاتی»، «استراتژی
      بهره‌وری») ← محور «مدیریت عملیاتی»
    - اهداف مستقیماً مرتبط با مشتری (مثل «حفظ مشتریان»، «خرید آسان
      و سریع») ← محور «مدیریت مشتری» (یا «مدیریت روابط با مشتریان»
      در بازرگانی قطعات/آپشن که محور جداگانه دارن)
    - اهداف مرتبط با تنوع/تازگی عرضه (مثل «تنوع خدمات مکمل») ← محور
      «نوآوری»
    - اهداف مرتبط با تأمین‌کنندگان/قطعه‌سازان (فقط در بازرگانی قطعات
      و آپشن که محور «تکمیل‌کنندگان» دارن) ← همون محور

    نتیجه با تست واقعی: هر ۵۱ هدف تخصیص یافت، **صفر هدف بدون محور
    باقی موند** (تأیید‌شده روی هر ۵ کسب‌وکار)، و هر ۵ نقشه‌ی استراتژیک
    بدون خطا رندر شدن.

    یه دستور لازمه (روی دیتابیس واقعی):
    python manage.py assign_missing_strategy_themes

    فایل جدیدی که باید اضافه بشه:
    - strategic/management/commands/assign_missing_strategy_themes.py

۱۸۶. کوچیک‌کردن باکس‌های «کسب‌وکارها» و «محورهای هر کسب‌وکار» توی
    صفحه‌ی «نقشه استراتژیک» — طبق تصویر و بازخورد کاربر که این دو
    بخش جای زیادی می‌گرفتن.

    - فونت و فاصله‌ی داخلی هر دو نوار کوچیک‌تر شد
    - برچسب «آرکی‌تایپ» کسب‌وکار (مثلاً «صمیمیت با مشتری») که قبلاً
      همیشه کنار اسم کسب‌وکار نمایش داده می‌شد و طولانی‌ترین بخش هر
      باکس بود، الان فقط با **هاور روی باکس** (تولتیپ) نشون داده
      می‌شه — خودِ اطلاعات حذف نشده، فقط نحوه‌ی نمایشش جمع‌وجورتر شد

    نتیجه: هر ۵ باکس کسب‌وکار حالا خیلی راحت‌تر توی یه ردیف جا می‌شن.
    فقط تمپلیت بود (کاملاً ایزوله با کلاس `.sm`، هیچ صفحه‌ی دیگه‌ای
    تحت‌تأثیر قرار نگرفت)، بدون نیاز به دیتابیس.

    فایلی که باید overwrite شود:
    - templates/strategic/stratmap.html

    نیازی به هیچ دستوری نیست — فقط فایل را جایگزین و سرور را ری‌استارت
    کنید.

۱۸۶. فشرده‌تر کردن دو ردیف بالای «نقشه استراتژیک» — طبق تصویر و درخواست
    کاربر (باکس‌های کسب‌وکار و باکس‌های محور، جای زیادی می‌گرفتن و به
    دو خط می‌شکستن):

    الف) تب‌های کسب‌وکار — عبارت تکراری «کسب و کار» فقط از **نمایش**
    این تب‌های کوچیک حذف شد (مثلاً «کسب و کار بازرگانی قطعات» →
    «بازرگانی قطعات») — اسم کامل هنوز با هاور (tooltip) قابل‌دیدنه،
    و هیچ‌جای دیگه‌ی سامانه (دیتابیس، بقیه‌ی صفحات) دست‌نخورد.

    ب) اندازه‌ی فونت و فاصله‌ی داخلی هر دو ردیف (کسب‌وکارها و محورها)
    کمی کوچیک‌تر شد.

    نتیجه: با تست واقعی، تب‌های هر ۵ کسب‌وکار الان توی یه ردیف جا
    می‌شن (به‌جای شکستن به دو خط)، و محورهای «بازرگانی قطعات»/«آپشن»
    (۱۰ محوری، بیشترین تعداد) هم فشرده‌تر شدن. هر ۵ نقشه با سرور زنده
    تست شد.

    فایلی که باید overwrite شود:
    - templates/strategic/stratmap.html

    نیازی به هیچ دستوری نیست — فقط فایل را جایگزین و سرور را ری‌استارت
    کنید.

۱۸۷. دو اصلاح روی صفحه‌ی «تحلیل ذینفعان» — طبق درخواست کاربر:

    الف) **جست‌وجوی زنده** بالای پیکر «پروژه‌های تحول مرتبط» (توی فرم
    مداد هر ذینفع) اضافه شد — چون تعداد پروژه‌ها زیاده و پیدا کردن
    دستی سخت بود. با شبیه‌سازی واقعی مرورگر تست شد: از ۲ گزینه،
    جست‌وجوی «کمیته» دقیقاً همون ۱ گزینه‌ی درست رو نشون داد.

    ب) توی هر دو باکس بالای صفحه (۲۰٪ ریسک بحرانی و ۲۰٪ فرصت کلیدی)،
    الان اگه یه ذینفع پروژه‌ی تحول وصل‌شده داشته باشه، یه بج کوچیک
    (🔗 با شماره‌ی تعداد) کنارش نشون داده می‌شه — با هاور، اسم پروژه‌ها
    رو می‌بینید. با یه ارتباط واقعی («تامین کنندگان قطعه آپشن» ← پروژه
    «برنامه‌های کمیته‌های تاب‌آوری ۱۰۰») تست شد.

    فایل‌هایی که باید overwrite شوند:
    - strategic/views.py
    - templates/strategic/stakeholders.html

    نیازی به هیچ دستوری نیست — فقط فایل‌ها را جایگزین و سرور را ری‌استارت
    کنید.

۱۸۸. افزودن «کد پروژه» قبل از عنوان، توی پیکر «پروژه‌های تحول مرتبط»
    صفحه‌ی ذینفعان — طبق درخواست کاربر، تا هم کد دیده بشه هم جست‌وجوی
    بالا با کد هم کار کنه (چون جست‌وجو روی متن نمایش‌داده‌شده عمل
    می‌کنه). اگه پروژه‌ای کد نداشته باشه، فقط عنوانش نشون داده می‌شه
    (بدون خط تیره‌ی اضافه).

    ⚠️ این تغییر فقط روی همین پیکر مخصوصه (نه روی خودِ مدل Initiative
    یا جای دیگه‌ای که اسم پروژه نشون داده می‌شه) — با تست واقعی (هم
    نمایش، هم جست‌وجو با کد «PRJ-99») تأیید شد.

    فایلی که باید overwrite شود:
    - strategic/forms.py

    نیازی به هیچ دستوری نیست — فقط فایل را جایگزین و سرور را ری‌استارت
    کنید.

۱۸۹. اتصال ذینفعان به پروژه‌های تحول واقعاً مرتبط — روی دیتابیس نهایی
    واقعی‌تون (۱۹۰ ذینفع، ۳۵۰ پروژه)، طبق درخواست کاربر برای تحلیل
    کارشناسی حرفه‌ای:

    **روش کار سه‌مرحله‌ای**:
    ۱) نگاشت سازمانی: کشف شد که فیلد «دپارتمان» ذینفعان صرفاً واحد
       ثبت‌کننده‌ی رکورده، نه هویت واقعی ذینفع (که در فیلد «نام»
       هست) — پس هر هویت واقعی (مثل «شبکه نمایندگی‌ها»، «تامین
       کنندگان») به معاونت(های) محتمل پروژه‌هاش نگاشت شد.
    ۲) درون همون معاونت، امتیازدهی وزنی بر اساس نایاب‌بودن کلمات
       مشترک بین (نیاز+ریسک+فرصت+اقدام ذینفع) و (عنوان پروژه) —
       کلمات عمومی (افزایش/کاهش/فرآیند/...) از امتیازدهی حذف شدن.
    ۳) فقط تطابق‌های با امتیاز بالا نگه داشته شدن؛ چندتا مورد با
       امتیاز بالا ولی موضوعاً بی‌ربط (مثل «مشارکت در بازسازی
       خودروهای جنگ» که فقط کلمه‌ی «مشارکت» مشترک بود) با بررسی
       دستی کنار گذاشته شدن.

    **نتیجه**: ۴۸ ارتباط، برای ۴۹ ذینفع از ۱۹۰ (عمداً نه همه — فقط
    جاهایی که واقعاً مطمئن بودیم). نمونه‌های واقعی:
    - «شبکه نمایندگی‌ها» (نیاز: سبد متنوع آپشن) ← «توسعه فروش قطعات
      آپشن در تعمیرگاه‌های مرکزی»
    - «تامین کنندگان» (نیاز: کاهش زمان ارسال) ← «بهره‌برداری از
      ظرفیت‌های گروه جهت تامین ملزومات بسته‌بندی»
    - «مشتریان» (نیاز: کیفیت خدمات پس از فروش) ← «سامان‌دهی وضعیت
      ثبت شکایات»

    با سرور زنده و دیتابیس واقعی‌تون کامل تست شد.

    فایل جدیدی که باید اضافه بشه:
    - strategic/management/commands/link_stakeholders_to_initiatives.py

    دستور لازم (روی دیتابیس واقعی):
    python manage.py link_stakeholders_to_initiatives

۱۹۰. تکمیل خودکار ستون‌های عددی هنگام انتخاب شاخص از «بانک شاخص‌های
    عملیاتی» توی فرم «اهداف کلان و KPI شرکت» — طبق درخواست کاربر.
    قبلاً فقط نام/واحد/حوزه خودکار پر می‌شد؛ الان با کلیک روی یه
    شاخص عملیاتی توی پیکر جست‌وجو، این ۵ فیلد هم خودکار پر می‌شن:
    هدف سال گذشته، عملکرد سال گذشته، هدف سال جاری، عملکرد سال جاری،
    درصد تحقق.

    ⚠️ فیلد «نام» طبق تصمیم اولیه‌ی کاربر (که می‌خواستن بتونن بعداً
    اسم رو کمی تغییر بدن)، همچنان **بعد از انتخاب هم آزادانه قابل‌
    ویرایشه** — این تغییر نکرد، فقط فیلدهای عددی جدید اضافه شدن.

    با شبیه‌سازی واقعی کلیک در مرورگر (jsdom) و ثبت واقعی از فرم
    تست شد — هر ۸ فیلد (نام، واحد، حوزه، هدف/عملکرد هر دو سال، درصد
    تحقق) دقیقاً مطابق شاخص عملیاتی منبع پر و ذخیره شدن.

    فایل‌هایی که باید overwrite شوند:
    - strategic/views.py
    - templates/strategic/company_goals.html

    نیازی به هیچ دستوری نیست — فقط فایل‌ها را جایگزین و سرور را ری‌استارت
    کنید.

۱۹۱. دکمه‌ی جدید «به‌روزرسانی از بانک عملیاتی» توی صفحه‌ی «اهداف کلان
    و KPI شرکت» — طبق درخواست کاربر. با یه کلیک، **همه‌ی** شاخص‌های
    کلانی که به یه شاخص عملیاتی وصل‌ان، مقادیر زیر رو دوباره از منبع
    عملیاتی‌شون می‌خونن و به‌روز می‌کنن: حوزه، واحد، هدف/عملکرد سال
    گذشته، هدف/عملکرد سال جاری، درصد تحقق.

    ⚠️ طبق تأکید کاربر، دو مورد **کاملاً دست‌نخورده** می‌مونن:
    - «نام» شاخص کلان (چون قبلاً تصمیم گرفته بودیم کاربر بتونه بعداً
      خودش اسمش رو تغییر بده)
    - «اهداف مرتبط» (ارتباط چندبه‌چند با اهداف کلان)

    **تست کامل با سناریوی واقعی**: یه شاخص تست ساختم، عمداً مقادیرش
    رو خراب کردم، بعد مقدار واقعی منبع رو هم عوض کردم، بعد دکمه رو
    زدم — نتیجه: مقادیر عددی به مقدار **جدید** منبع برگشتن، ولی نام
    و اهداف مرتبط دقیقاً دست‌نخورده موندن.

    فایل‌هایی که باید overwrite شوند:
    - strategic/views.py
    - strategic/urls.py
    - templates/strategic/company_goals.html

    نیازی به هیچ دستوری نیست — فقط فایل‌ها را جایگزین و سرور را ری‌استارت
    کنید.

۱۹۲. حذف نمایش خودکار پنجره‌ی «آشنایی با SWOT» — طبق درخواست کاربر
    (بند ۱۸۱ رو برگردوند). الان دیگه با ورود به صفحه، خودکار باز
    نمی‌شه — فقط با کلیک روی دکمه‌ی «آشنایی با SWOT» باز می‌شه. دکمه‌ی
    بستن (ضربدر) و کلیک روی پس‌زمینه‌ی بیرون برای بستن (بند ۱۸۱)
    دست‌نخورده موندن. با سرور زنده تست شد.

    فایلی که باید overwrite شود:
    - templates/strategic/swot.html

    نیازی به هیچ دستوری نیست — فقط فایل را جایگزین و سرور را ری‌استارت
    کنید.

۱۹۳. اتصال پروژه‌های تحول به اهداف استراتژیک واقعاً مرتبط — روی
    دیتابیس نهایی واقعی‌تون (۳۵۰ پروژه، ۱۷۸ هدف)، طبق درخواست کاربر
    برای بررسی کارشناسی حرفه‌ای کل پروژه‌ها.

    **روش کار**: امتیازدهی وزنی بر اساس نایاب‌بودن کلمات مشترک بین
    عنوان هر پروژه و عنوان هر هدف — کلمات عمومی (افزایش/کاهش/فرآیند/
    شبکه/قطعات/...) حذف شدن تا فقط تطابق‌های واقعاً معنادار امتیاز
    بگیرن. چندتا تطابق با امتیاز بالا ولی موضوعاً بی‌ربط (مثل
    «پیش‌بینی نیازهای مشتریان» که فقط با یه پروژه‌ی «پیش‌بینی بودجه‌ی
    پیمانکاران» بر سر کلمه‌ی «پیش‌بینی» جفت شده بود) با بررسی دستی
    کنار گذاشته شدن.

    **نتیجه**: ۳۷ ارتباط، برای ۳۳ هدف از ۱۷۸ (عمداً نه همه). نمونه‌های
    واقعی:
    - «طراحی مدل توزیع منابع مالی» ← «طراحی مدل و مکانیزم توزیع منابع
      مالی» (تطابق تقریباً عینی)
    - «تعیین تکلیف اقلام مازاد» ← «مکاتبه با شرکت‌های گروه در راستای
      تعیین تکلیف اقلام نامنطبق»
    - «استفاده از هوش مصنوعی در عیب‌یابی» ← «راه‌اندازی پلتفرم مشاوره
      فنی و عیب‌یابی آموزشی مجازی»

    این ارتباط دقیقاً همون چیزیه که توی صفحه‌ی «ارکان جهت‌ساز» (پنل
    «پروژه‌های مرتبط») هم نمایش داده می‌شه — بدون نیاز به کار اضافه،
    چون از همون رابطه‌ی `Initiative.objectives` استفاده می‌کنه.

    با سرور زنده و دیتابیس واقعی‌تون کامل تست شد (شامل نقشه‌ی هر ۵
    کسب‌وکار و صفحه‌ی ارکان جهت‌ساز).

    فایل جدیدی که باید اضافه بشه:
    - strategic/management/commands/link_initiatives_to_objectives.py

    دستور لازم (روی دیتابیس واقعی):
    python manage.py link_initiatives_to_objectives

۱۹۴. بازسازی کامل اتصال «ارزش سازمانی ↔ هدف استراتژیک» — طبق تشخیص
    درست کاربر که نسخه‌ی اولیه (بند ۱۸۹) خیلی ناقص بود (مثلاً «نوآوری
    بازار محور» فقط ۲ هدف داشت، در‌حالی‌که ۲۱ هدف با محور «نوآوری» در
    کل ۵ کسب‌وکار وجود داره).

    **روش جدید**: به‌جای انتخاب دستی چندتا نمونه، برای ۵ ارزشی که
    دقیقاً معادل یه محور مشخص نقشه‌ی استراتژیک هستن، **همه‌ی** اهداف
    اون محور (در هر کسب‌وکاری که وجود داشته باشه) وصل شدن:
    - کرامت انسانی ← «مسئولیت‌های قانونی و اجتماعی» (۱۴ هدف)
    - توان‌افزایی کارکنان ← «سرمایه انسانی» (۶ هدف)
    - نوآوری بازار محور ← «نوآوری» (۲۱ هدف — قبلاً فقط ۲)
    - وحدت و کارتیمی ← «سرمایه سازمانی» (۱۶ هدف)
    - مشتری‌مداری ← «مدیریت مشتری» + «مشتری نهایی» + «مدیریت روابط
      با مشتریان» (۴۹ هدف)

    ۳ ارزش دیگه (رقابت سالم، شفافیت و اعتمادآفرینی، اطلاع‌رسانی جامع)
    معادل محور مستقیم توی نقشه ندارن، پس همون تطبیق محتوایی دقیق قبلی
    حفظ شد.

    **نتیجه**: از ۲۰ ارتباط قبلی به **۱۱۲ ارتباط** رسیدیم.

    ⚠️ این دستور جایگزین بند ۱۸۹ می‌شه (نه اضافه‌بر اون) — چون از
    `.set()` استفاده می‌کنه، هر بار اجراش، ارتباط‌های قبلی هر ارزش
    رو با نسخه‌ی جدید و کامل جایگزین می‌کنه.

    ۱۹۵. رفع مشکل ظاهری پیکر «اهداف استراتژیک مرتبط» توی فرم ویرایش
    ارزش — طبق گزارش کاربر. علت پیدا شد: کلاس CSS مربوط به این پیکر
    (`.picker-box`) اصلاً توی این صفحه تعریف نشده بود (نه توی خودِ
    صفحه، نه توی فایل CSS سراسری) — برای همین کاملاً بدون استایل نمایش
    داده می‌شد. **یه مشکل مهم‌تر هم پیدا شد**: منطق جاوااسکریپت
    «تیک‌خوردن با بازخورد بصری» هم اصلاً برای این پیکر وجود نداشت —
    یعنی حتی اگه فقط CSS رو درست می‌کردم، کلیک روی گزینه‌ها بدون هیچ
    بازخورد رنگی می‌موند. هر دو مشکل با استایل و منطق دقیقاً هم‌الگو
    با بقیه‌ی صفحات مشابه (اهداف کلان، ذینفعان) اصلاح شد.

    با شبیه‌سازی واقعی کلیک در مرورگر (jsdom) تست شد: هم استایل درست
    اعمال شد، هم کلیک روی گزینه‌ها بازخورد بصری گرفت.

    فایل‌هایی که باید overwrite/اضافه شوند:
    - templates/strategic/org_identity.html
    - strategic/management/commands/link_org_values_to_objectives_v2.py (فایل جدید)

    دستور لازم (روی دیتابیس واقعی):
    python manage.py link_org_values_to_objectives_v2

۱۹۶. گروه‌بندی اهداف هم‌عنوان در پنل «ارکان جهت‌ساز» — طبق پیشنهاد
    دقیق کاربر (که خیلی به‌جا بود، مخصوصاً بعد از گسترش بند ۱۹۴ که
    بعضی ارزش‌ها الان ده‌ها هدف دارن):

    وقتی چند هدف (از کسب‌وکارهای مختلف) دقیقاً **عنوان یکسان** دارن
    (مثلاً «توسعه فرهنگ بهبود مستمر» که در ۳ کسب‌وکار عیناً تکرار شده
    بود)، الان به‌جای ۳ ردیف جدا، **یک ردیف** با برچسب «(در ۳
    کسب‌وکار)» نشون داده می‌شه — با هاور روی همون برچسب، جزئیات کامل
    هر ۳ مورد (کد+عنوان+کسب‌وکار) دیده می‌شه.

    به‌همون‌ترتیب، ستون «شاخص کلیدی» و «پروژه‌های مرتبط» هم برای این
    گروه، به‌جای تکرار همون شاخص‌ها ۳ بار، فقط **یک‌بار** (با حذف
    خودکار موارد تکراری) نشون داده می‌شن.

    **تست کامل با سناریوی واقعی** («وحدت و کارتیمی»، ۱۶ هدف): بعد از
    گروه‌بندی، ۱۲ ردیف نمایش داده شد (۳ گروه از ۲-۳ هدف هم‌عنوان
    ادغام شدن) — دقیقاً مطابق نمونه‌ای که کاربر خودش توصیف کرده بود.

    فایلی که باید overwrite شود:
    - templates/strategic/org_identity.html

    نیازی به هیچ دستوری نیست — فقط فایل را جایگزین و سرور را ری‌استارت
    کنید.

۱۹۷. اتصال کامل شاخص‌های کلان شرکت (I-ها) به معادلشون در بانک شاخص‌های
    عملیاتی — طبق اولویت‌بندی کاربر (این ردیف از جدول ارزیابی جامع
    ارتباطات سامانه، از ۸٪ به ۹۲٪ ارتقا یافت).

    **روش کار**: تطبیق محتوایی دقیق عنوان‌ها — بیشترشون تطابق تقریباً
    عینی بودن (مثل I21 «میزان رضایت شغلی کارکنان» ↔ HR-01-00 با همون
    عنوان دقیق، یا I10-I13 که یه سری کامل و دقیق CRM-21-00 تا
    CRM-21-03 پیدا شد که دقیقاً معادل «متوسط سه خودروساز/سایپا/پارس
    خودرو/زامیاد» بودن).

    **نتیجه**: از ۲ به **۲۳ از ۲۵** (۹۲٪) رسیدیم. فقط I24 (هزینه‌های
    پشتیبانی) و I25 (هزینه‌های عملیاتی) عمداً بدون اتصال موندن — چون
    هرکدوم ترکیبی از چند قلم هزینه‌ی جدا هستن و هیچ شاخص تکی توی بانک
    عملیاتی کل ترکیبشون رو پوشش نمی‌ده.

    با دکمه‌ی «به‌روزرسانی از بانک عملیاتی» (بند ۱۹۱) هم واقعاً تست
    شد — با کلیک، هر ۲۳ شاخص وصل‌شده مقادیرشون رو از منبع خوندن.

    فایل جدیدی که باید اضافه بشه:
    - strategic/management/commands/link_company_kpis_to_operational.py

    دستور لازم (روی دیتابیس واقعی):
    python manage.py link_company_kpis_to_operational

۱۹۸. اتصال کامل ریسک‌ها به پروژه‌های تحول واقعاً مرتبط — **نسخه‌ی
    اصلاح‌شده و کامل** (جایگزین بند ۱۹۷، چون اون نسخه ناقص و دارای
    یه اشتباه بود).

    ⚠️ **صداقت کامل درباره‌ی چی اشتباه بود**: نسخه‌ی اول به‌خاطر تفاوت
    کاراکتر عربی «ي» در برابر فارسی «ی» توی عناوین پروژه‌ها (مشکلی که
    قبلاً هم توی این پروژه دیده بودیم)، جست‌وجوهام خیلی از تطابق‌های
    واقعی رو از دست داده بود. با ارسال «دفتر ثبت ریسک» واقعی کاربر و
    بررسی دوباره با جست‌وجوی نرمال‌شده، **۱ اتصال اشتباه** (Disaster
    Recovery که به ریسک نادرستی وصل شده بود) پیدا و حذف شد، و **۱۴
    ارتباط جدید** که جا افتاده بودن اضافه شدن.

    **نتیجه‌ی نهایی**: هر **۹ از ۹ ریسک** الان به پروژه‌ی واقعی وصل‌ان
    (قبلاً فقط ۵ تا به‌درستی وصل بودن). هر پروژه دقیقاً از روی متن
    «اقدام کاهش» خودِ همون ریسک پیدا شد — تطابق مستقیم با نیت
    ثبت‌شده، نه حدس محتوایی.

    فایلی که باید overwrite شود:
    - strategic/management/commands/link_risks_to_initiatives.py

    دستور لازم (روی دیتابیس واقعی — این دستور «تفاضلی» است، یعنی
    امن است حتی اگه نسخه‌ی قبلی رو قبلاً اجرا کرده باشید):
    python manage.py link_risks_to_initiatives

۱۹۹. نمایش برعکس «پروژه‌های متصل» توی صفحه‌ی «نقشه ریسک» — طبق درخواست
    کاربر، تکمیل‌کننده‌ی بند ۱۹۸. قبلاً این ارتباط فقط توی صفحه‌ی
    «پروژه‌های تحول» دیده می‌شد؛ الان توی خودِ ستون «اقدامات کلیدی/
    پروژه‌ها»ی صفحه‌ی ریسک هم یه بج بنفش («N پروژه‌ی متصل») نشون داده
    می‌شه — با هاور، عنوان کامل هر پروژه + مسئول + وضعیت + درصد
    پیشرفت دیده می‌شه.

    با سرور زنده تست شد: هر ۹ ریسک بج درست با اطلاعات کامل نشون دادن.

    فایلی که باید overwrite شود:
    - templates/strategic/risk.html

    نیازی به هیچ دستوری نیست — فقط فایل را جایگزین و سرور را ری‌استارت
    کنید.

۲۰۰. دور دوم اتصال ذینفعان به پروژه‌های تحول — با دقت بیشتر، طبق
    درخواست کاربر. این‌بار با نرمال‌سازی کاراکتر عربی/فارسی (ي↔ی)
    که توی ریسک‌ها هم کمک کرده بود، ۱۴۱ ذینفع باقی‌مانده دوباره
    بررسی شد.

    از ۵۴ تطابق پیشنهادی اولیه، بعد از بررسی دستی کیفیت، فقط **۱۶
    مورد** واقعاً قوی و موضوعاً درست نگه داشته شد — بقیه (که فقط
    روی کلمات عمومی مثل «موقع»/«اعمال» یا صرفاً اسم شرکت جفت شده
    بودن، مثل «شرکت پارس خودرو» که با یه پروژه‌ی بی‌ربط فقط چون
    کلمه‌ی «پارس خودرو» توش بود جفت شده بود) کنار گذاشته شدن.

    **نتیجه**: از ۲۶٪ (۴۹ از ۱۹۰) به **۳۴٪ (۶۵ از ۱۹۰)** رسیدیم.

    فایل جدیدی که باید اضافه بشه:
    - strategic/management/commands/link_stakeholders_to_initiatives_v2.py

    دستور لازم (روی دیتابیس واقعی):
    python manage.py link_stakeholders_to_initiatives_v2

۲۰۱. اتصال ذینفعان به آیتم‌های واقعاً مرتبط از «۵ نیروی Porter» — طبق
    پیش‌بینی و درخواست کاربر. برخلاف کارهای قبلی (که تطبیق کلمه‌ای
    بود)، این‌بار روش **دسته‌بندی مفهومی** استفاده شد: ذینفعانی که
    ماهیتاً «بیرونی/بازاری» هستن (تامین‌کننده، مشتری/شبکه، خودروساز)
    به دسته‌ی متناظرشون از ۵ نیرو وصل شدن — مثلاً «تامین کنندگان» به
    آیتم‌های «قدرت چانه‌زنی تأمین‌کنندگان»، «شبکه نمایندگی‌ها» به
    «قدرت چانه‌زنی خریداران»، «شرکت سایپا/پارس‌خودرو/زامیاد» به
    «خواسته‌ها و ملاحظات سایپا به‌عنوان شرکت مادر».

    ذینفعان داخلی/سازمانی (مدیریت آموزش، کیفیت، پرسنل و...) عمداً
    بدون اتصال موندن — چون ۵ نیروی Porter ذاتاً درباره‌ی رقابت و بازار
    بیرونیه، نه امور داخلی سازمان.

    **نتیجه**: از ۱۵٪ (۲۸ از ۱۹۰) به **۲۵٪ (۴۷ از ۱۹۰)** رسیدیم —
    دقیقاً مطابق پیش‌بینی اولیه.

    فایل جدیدی که باید اضافه بشه:
    - strategic/management/commands/link_stakeholders_to_porter.py

    دستور لازم (روی دیتابیس واقعی):
    python manage.py link_stakeholders_to_porter

۲۰۲. اتصال پروژه‌های تحول به شاخص کلان/عملیاتی مبنا — طبق پیش‌بینی
    کاربر که این ارتباط (برخلاف TOWS) باید بهتر جواب بده، چون شاخص‌ها
    مفهوم عددی و قابل‌اندازه‌گیری دارن، نه ایده‌ی آینده‌نگر.

    **روش کار**: امتیازدهی وزنی با نرمال‌سازی کاراکتر عربی/فارسی
    (ي↔ی، مثل ریسک‌ها)، این‌بار با آستانه‌ی سخت‌گیرانه‌تر از پروژه‌های
    قبلی. از ۸۰ پروژه‌ی نامزد اولیه، بعد از بررسی دستی، ۱۶ مورد که
    موضوعاً بی‌ربط بودن (مثلاً «هزینه بازسازی ساختمان تعمیرگاه» که فقط
    چون هر دو عبارت «تعمیرگاه مرکزی» داشتن، به یه شاخص کاملاً نامرتبط
    «درآمد گارانتی» جفت شده بود) کنار گذاشته شدن.

    **نتیجه**: از ۱٪ به **۱۹٪ (۶۶ از ۳۵۰ پروژه)** رسیدیم — بیشترین
    پیشرفت نسبی بین همه‌ی کارهای این نشست، دقیقاً تأیید پیش‌بینی که
    شاخص‌ها بهتر از TOWS جواب می‌دن.

    فایل جدیدی که باید اضافه بشه:
    - strategic/management/commands/link_initiatives_to_kpis.py

    دستور لازم (روی دیتابیس واقعی):
    python manage.py link_initiatives_to_kpis

۲۰۳. افزودن ۳۶ ارتباط جدید پروژه↔شاخص عملیاتی که کاربر شخصاً و دستی
    بررسی و تأیید کرده بود — از ۵۸ جفت ارسالی، ۲۲ تا با کار خودکار
    قبلی (بند ۲۰۲) هم‌پوشانی داشت (تأیید متقابل خوبیه که هر دو روش
    به یه نتیجه رسیدن)، و ۳۶ تای دیگه واقعاً جدید بودن.

    **نتیجه**: از ۱۹٪ به **۲۲٪ (۷۶ از ۳۵۰ پروژه)** رسیدیم.

    فایل جدیدی که باید اضافه بشه:
    - strategic/management/commands/link_initiatives_to_kpis_v2_user.py

    دستور لازم (روی دیتابیس واقعی — امنه، حتی اگه بعضی جفت‌ها از قبل
    ثبت شده باشن):
    python manage.py link_initiatives_to_kpis_v2_user

۲۰۴. اتصال ۱۵۳ ارتباط جدید ذینفع↔پروژه — بر اساس تحلیل خوشه‌ای (۸
    خوشه‌ی استراتژیک) که همکار کاربر ارائه کرده بود.

    **نکته‌ی فنی مهم درباره‌ی نحوه‌ی شناسایی**: اعداد «ریسک ۳۹۲»،
    «فرصت ۱۰۰» و... در اون سند، شناسه‌ی مستقیم ذینفع نبودن، بلکه
    **امتیاز ریسک/فرصت** خودِ ذینفع بودن. با ترکیب (امتیاز ریسک،
    امتیاز فرصت) با هم، اکثر ردیف‌ها به‌صورت یکتا شناسایی شدن؛ ۳
    مورد که امتیازشون بین چند ذینفع مشترک بود، با تطبیق دقیق متن
    ریسک (که در سند هم توضیح مختصرش اومده بود) رفع ابهام شدن.

    **نتیجه**: از ۳۴٪ به **۴۵٪ (۸۵ از ۱۹۰ ذینفع)** رسیدیم — بزرگ‌ترین
    جهش پیشرفت بین همه‌ی کارهای این نشست.

    فایل جدیدی که باید اضافه بشه:
    - strategic/management/commands/link_stakeholders_to_initiatives_v3_clusters.py

    دستور لازم (روی دیتابیس واقعی):
    python manage.py link_stakeholders_to_initiatives_v3_clusters

۲۰۵. افزودن بج «پروژه‌های تحول متصل» روی کارت‌های هدف در «نقشه
    استراتژیک» — طبق گزارش درست کاربر که این قابلیت هیچ‌وقت اینجا
    ساخته نشده بود (فقط توی «پروژه‌های تحول» و «ارکان جهت‌ساز» بود).

    الان روی هر کارت هدفی که پروژه داره، یه بج سبز کوچیک («🔀 N») کنار
    بج TOWS (که از قبل بود) نشون داده می‌شه — با هاور، عنوان کامل هر
    پروژه + کد + مسئول + وضعیت + درصد پیشرفت دیده می‌شه.

    با سرور زنده تست شد — روی نقشه‌ی «بسته خدمت»، هر ۴ ارتباطی که
    الان وجود داره (F2, F5, F7, P1) درست رندر شدن، از جمله کارت F2
    که تأیید مستقیم شد.

    فایلی که باید overwrite شود:
    - templates/strategic/stratmap.html

    نیازی به هیچ دستوری نیست — فقط فایل را جایگزین و سرور را ری‌استارت
    کنید.

۲۰۶. اتصال واقعی ۱۷۲ ارتباط هدف↔پروژه برای کسب‌وکار «بسته خدمت» —
    طبق تحلیل جامع کاربر و همکارشون، بعد از فیلترکردن دقیق (بند قبلی
    که فقط توافق شده بود، ولی اجرا نشده بود — این بند واقعاً اجراش
    کرد).

    ⚠️ **یادداشت شفاف**: قبل از این بند، توافق کرده بودیم این ۱۷۲
    ارتباط اعمال بشه، ولی حین کار روی رفع باگ نمایش نقشه (بند ۲۰۵)،
    خودِ اجرا فراموش شده بود — با سؤال درست کاربر («ارتباط‌ها درست
    هستن؟») پیدا و همین الان تکمیل شد.

    **نتیجه**: از ۴ به **۱۷۶ ارتباط** (بسته خدمت) رسیدیم — ۲۶ از ۳۲
    هدف الان حداقل یه پروژه دارن. با سرور زنده تست شد.

    فایل جدیدی که باید اضافه بشه:
    - strategic/management/commands/link_service_package_objectives_to_initiatives.py

    دستور لازم (روی دیتابیس واقعی):
    python manage.py link_service_package_objectives_to_initiatives

۲۰۷. اصلاح محتوایی ۵ هدف «بسته خدمت» که بعد از بررسی دقیق عنوان‌ها (نه
    فقط اعتبار کد)، پروژه‌های موضوعاً بی‌ربط بهشون وصل شده بود:

    - **C1** («قیمت مناسب») و **P7** («روابط با مشتری») — هر دو ۹
      پروژه‌شون درباره‌ی «تأمین قطعه» بود، نه قیمت یا رابطه با مشتری.
      این ۱۲ پروژه (اشتراکی) حذف و به **P3** («خلق شبکه‌ای از
      تأمین‌کنندگان خدمت» — که دقیقاً معنایی‌شون بود و خالی مونده بود)
      منتقل شدن.

    - **P1** («بسته‌های خدمت متنوع») — پروژه‌هاش کاملاً درباره‌ی
      املاک/مسائل مالی بودن (از جمله همون داده‌ی آزمایشی «تست پروژه
      بسته خدمت» که هم‌زمان حذف شد).

    - **P2** («کیفیت توافق‌شده با تأمین‌کنندگان») — پروژه‌هاش درباره‌ی
      «وصول چک/مطالبات» بودن.

    - **P13** («اخذ مجوز قانونی...») — دقیقاً همون لیست P12 اشتباهی
      کپی شده بود؛ پروژه‌هاش درباره‌ی «شکایات مشتری» بودن، نه مجوز
      قانونی.

    هر ۳ هدف اخیر عمداً **خالی** موندن، چون هیچ‌کدوم از ۹۰ پروژه‌ی
    موجود واقعاً باهاشون مرتبط نبود.

    با سرور زنده تست و تأیید شد.

    فایل جدیدی که باید اضافه بشه:
    - strategic/management/commands/fix_service_package_objective_mismatches.py

    دستور لازم (روی دیتابیس واقعی، **بعد از** اجرای بند ۲۰۶):
    python manage.py fix_service_package_objective_mismatches

۲۰۸. اتصال ۲۰۷ ارتباط هدف↔پروژه برای کسب‌وکار «بازرگانی قطعات» — طبق
    تحلیل جامع کاربر و همکارشون، **بعد از بررسی محتوایی کامل و دقیق
    هر ۳۴ گروه** (نه فقط اعتبار کد، بلکه تک‌تک عنوان هدف در برابر
    عنوان پروژه‌ها).

    برخلاف «بسته خدمت» (که ۵ مشکل بزرگ داشت)، این سند برای «بازرگانی
    قطعات» **کیفیت خیلی بهتری** داشت — چون کدهایی مثل C1/P7/P1/P2
    که برای بسته خدمت کاملاً اشتباه بودن، برای بازرگانی قطعات (با
    معنای متفاوتشون) درست از آب دراومدن.

    فقط ۸ کد بعد از بررسی دقیق مشکل‌دار تشخیص داده و **عمداً خالی**
    گذاشته شدن:
    - **P12 و P13** — الگوی تکرار (لیست کاملاً یکسان برای دو مفهوم
      متفاوت: «نفوذ بر هزینه جابجایی» در برابر «استفاده از تجربه
      مشتری»)
    - **P15 و P16** — همون الگو (لیست یکسان برای «سهولت پلتفرم» و
      «تسهیل برگشت کالا»)
    - **F4** («هزینه پایین پلتفرم») — پروژه‌هاش همه درباره‌ی املاک
      بودن
    - **F6** («درآمد سرمایه‌گذاری/قطعات مشترک/پیش‌فروش») — پروژه‌هاش
      درباره‌ی تمدید گارانتی بودن
    - **P20** («قیمت‌گذاری هوشمند») — فقط ۱ از ۴ پروژه واقعاً درباره‌ی
      قیمت‌گذاری بود
    - **P8** («هم‌افزایی تولیدکنندگان گروه») — بعضی پروژه‌هاش درباره‌ی
      منابع خارج از گروه بودن

    **نتیجه**: جمعاً **۲۱۸ ارتباط**، **۲۷ از ۴۴ هدف** بازرگانی قطعات
    الان حداقل یه پروژه دارن. با سرور زنده تست شد.

    فایل جدیدی که باید اضافه بشه:
    - strategic/management/commands/link_parts_trading_objectives_to_initiatives.py

    دستور لازم (روی دیتابیس واقعی):
    python manage.py link_parts_trading_objectives_to_initiatives

۲۰۹. اتصال ۱۷۶ ارتباط هدف↔پروژه برای کسب‌وکار «آپشن» — طبق تحلیل جامع
    کاربر و همکارشون، بعد از بررسی محتوایی کامل هر ۲۷ گروه (این‌بار
    مشکلات بیشتری نسبت به بازرگانی قطعات پیدا شد).

    **۴ هدف کاملاً خالی** گذاشته شدن (هیچ پروژه‌ی موجود واقعاً
    مناسبشون نبود):
    - **P1** («نصب آسان قطعات جانبی») — ۱۴ پروژه‌اش همه درباره‌ی
      موجودی/MRP/مالی بودن، هیچ‌کدوم درباره‌ی «نصب آسان» نبود
    - **P13** («توسعه استاندارد ارتباطی خودرو با کمک ایراتورها») —
      یه هدف خیلی خاص که فقط پروژه‌های عمومی/مستندسازی بی‌ربط بهش
      وصل شده بود
    - **F4** («هزینه پایین پلتفرم ارتباطی») — همون الگوی قبلی، پروژه‌ها
      درباره‌ی املاک بودن
    - **L4** («پلتفرم تأمین OEM آفلاین فروشگاه») — پروژه‌های هوش
      مصنوعی/تبلیغات ربط مستقیم نداشتن

    **۴ هدف دیگه، فقط پروژه‌های بی‌ربطشون حذف** شد (نه کل هدف):
    - **P12** («ایمنی و بهداشت شغلی») — فقط پروژه‌های واقعی HSE/آتش‌
      نشانی نگه داشته شد
    - **F6، F7، P7** — هرکدوم بخشی از پروژه‌هاشون (که موضوعاً به هدف
      دیگه‌ای تعلق داشتن) حذف شد

    **نتیجه**: جمعاً **۱۸۱ ارتباط**، **۲۴ از ۳۲ هدف** آپشن الان حداقل
    یه پروژه دارن. با سرور زنده تست شد.

    فایل جدیدی که باید اضافه بشه:
    - strategic/management/commands/link_option_objectives_to_initiatives.py

    دستور لازم (روی دیتابیس واقعی):
    python manage.py link_option_objectives_to_initiatives

۲۱۰. جمع‌شدن ستون «پروژه‌های مرتبط» در پنل «ارکان جهت‌ساز» — طبق
    درخواست کاربر (که با گسترش زیاد ارتباطات هدف↔پروژه در بندهای
    اخیر، حالا خیلی از ردیف‌ها ده‌ها پروژه با هم لیست می‌شدن و فضای
    زیادی می‌گرفتن).

    الان وقتی یه ردیف **بیش از یه پروژه** داره، به‌جای لیست‌کردن کامل
    اسم همه، فقط «اتصال به N پروژه مرتبط» نشون داده می‌شه — با هاور
    روی همون متن، اسم کامل هر پروژه دیده می‌شه. اگه فقط **یه پروژه**
    باشه، همون‌جوری مستقیم (بدون خلاصه‌سازی) نشون داده می‌شه.

    با شبیه‌سازی واقعی مرورگر تست شد — روی ارزش «مشتری‌مداری»
    (که بیشترین تعداد ارتباط رو داره)، ردیف‌هایی مثل «اتصال به ۱۳
    پروژه مرتبط» درست با هاور کامل کار کردن، و ردیف‌های تک‌پروژه‌ای
    بدون خلاصه‌سازی اضافه مستقیم نشون داده شدن.

    فایلی که باید overwrite شود:
    - templates/strategic/org_identity.html

    نیازی به هیچ دستوری نیست — فقط فایل را جایگزین و سرور را ری‌استارت
    کنید.

۲۱۱. هاور حرفه‌ای و سلسله‌مراتبی برای «ردیابی خودکار منشأ» در صفحه‌ی
    «پروژه‌های تحول» — طبق گزارش درست کاربر که داده‌ی نمایش‌داده‌شده
    ناقص بود.

    **مشکلی که پیدا شد**: کد پایتون (`traced_origins`) از قبل ۴ سطح
    اطلاعات آماده می‌کرد (راهبرد TOWS ← عامل SWOT ← نوع منبع اصلی ←
    جزئیات منبع اصلی)، ولی نمایش هاور فقط سطح اول (TOWS) رو نشون
    می‌داد — دقیقاً همون چیزی که هدف اصلی «ردیابی منشأ» بود
    (PESTEL/Porter/ذی‌نفع/سناریو/McKinsey 7S/زنجیره ارزش) اصلاً دیده
    نمی‌شد.

    **راه‌حل**: یه پراپرتی جدید (`traced_origins_hover_text`) به مدل
    Initiative اضافه شد که:
    - راهبردهای TOWS رو گروه‌بندی می‌کنه (تا تکراری نشه)
    - برای هرکدوم، مسیر کامل سلسله‌مراتبی رو با فرورفتگی و فلش نشون
      می‌ده: «📍 راهبرد ← └─ برخاسته از (نوع SWOT) ← └─ ریشه در
      (نوع منبع اصلی): جزئیات»
    - نوع منبع اصلی (PESTEL/Porter/ذی‌نفع/...) همیشه واضح و برجسته‌ست

    با داده‌ی واقعی تست شد — مثال واقعی: «پروژه X ← راهبرد SO تهاجمی
    ← نقطه‌قوت SWOT (پلتفرم بازارگاه) ← ریشه در زنجیره ارزش
    (بازاریابی و فروش)» و دو مسیر دیگه تا PESTEL، همه با سرور زنده
    تأیید شد.

    فایل‌هایی که باید overwrite شوند:
    - strategic/models.py
    - templates/strategic/roadmap.html

    نیازی به هیچ دستوری نیست — فقط فایل‌ها را جایگزین و سرور را ری‌استارت
    کنید.

۲۱۲. تخصیص کسب‌وکار برای همه‌ی ۳۵۰ پروژه (فیلد `Initiative.business_unit`
    که از قبل توی مدل بود ولی فقط ۲ پروژه پرشده بود) — طبق درخواست
    کاربر. با ترکیب کلیدواژه‌ی قوی در عنوان + معاونت مسئول به‌عنوان
    قرینه، و برای موارد کاملاً بدون نشونه (عمدتاً کارکردهای ستادی/
    سراسری مثل IT، منابع انسانی، مالی عمومی)، پیش‌فرض منطقی (بازرگانی
    قطعات، کسب‌وکار محوری شرکت). یه فایل اکسل رنگی هم برای بازبینی
    دستی تحویل داده شد؛ کاربر خودش ۵۵ مورد رو دستی اصلاح کرد که همه
    اعمال شدن.

    توزیع نهایی: بازرگانی قطعات ۲۱۰، خدمات تعمیراتی ۹۹، گارانتی خودرو
    ۲۴، بسته خدمت ۹، آپشن ۸.

    ۲۱۳. اتصال ۸ پروژه‌ی «بازرگانی قطعات» به راهبرد TOWS واقعاً مرتبط —
    با استفاده از تخصیص تازه‌ی کسب‌وکار (بند ۲۱۲)، فضای جست‌وجو محدود
    به همون ۱۶ راهبرد TOWS بازرگانی قطعات شد (به‌جای کل ۸۱ راهبرد) —
    که دقت تطبیق رو خیلی بالا برد.

    از ۱۷ نامزد اولیه (تطبیق کلمه‌ای وزن‌دار)، بعد از بررسی دستی، فقط
    ۸ تای واقعاً قوی نگه داشته شد؛ ۹ مورد کاذب (که فقط روی کلمات
    عمومی مثل «بهره‌گیری» جفت شده بودن) کنار گذاشته شد.

    **نتیجه**: پروژه↔TOWS از ۲ به **۱۰ از ۳۵۰** رسید.

    فایل جدیدی که باید اضافه بشه:
    - strategic/management/commands/link_parts_trading_initiatives_to_tows.py

    دستور لازم (روی دیتابیس واقعی):
    python manage.py link_parts_trading_initiatives_to_tows

    فایل جدیدی که باید اضافه بشه:
    - strategic/management/commands/assign_initiative_business_units.py

    دستور لازم (روی دیتابیس واقعی — شامل هم طبقه‌بندی خودکار هم ۵۵
    اصلاح دستی کاربر، هر ۳۵۰ پروژه رو با تست واقعی روی حالت پاک‌شده
    تأیید شد):
    python manage.py assign_initiative_business_units

۲۱۴. رفع فوری مشکل «دوبرابرشدن پروژه‌ها» (۳۵۰ → ۶۹۸) — طبق گزارش
    کاربر. **علت واقعی پیدا شد**: منطق «ورودی اکسل» پروژه‌ها، تشخیص
    «پروژه‌ی موجود» رو بر پایه‌ی ترکیب «عنوان + کسب‌وکار» انجام می‌داد
    (نه کد پروژه). وقتی کاربر کسب‌وکار پروژه‌ها رو (طبق بند ۲۱۲) عوض
    و دوباره از اکسل وارد کرد، سامانه چون «عنوان+کسب‌وکار جدید» رو
    قبلاً نداشت، به‌جای به‌روزرسانی، برای هر ۳۴۸ پروژه یه رکورد کاملاً
    تازه ساخت.

    **راه‌حل دوبخشی**:

    الف) **پاک‌سازی امن با ادغام کامل** (نه فقط حذف) — دستور جدید
    برای هر گروه تکراری، رکورد قدیمی‌تر (کمترین شناسه، که تاریخچه‌ی
    کامل ارتباطات این گفت‌وگو رو داشت) رو نگه داشت، و **قبل از حذف**،
    هر ارتباطی که رو نسخه‌ی تازه بود (از جمله همون ۸ ارتباط TOWS که
    اشتباهاً روی نسخه‌ی خالی نشسته بودن) به نسخه‌ی اصلی منتقل کرد.
    نتیجه: از ۶۹۸ به ۳۵۰ برگشتیم، بدون گم‌شدن هیچ داده‌ای — با تست
    مستقیم روی همون ۸ پروژه‌ی TOWS تأیید شد.

    ب) **رفع خودِ باگ ریشه‌ای** — منطق ورودی اکسل الان اول با «کد»
    پروژه (اگه داشت) پروژه‌ی موجود رو پیدا می‌کنه، نه ترکیب عنوان+
    کسب‌وکار. با شبیه‌سازی دقیق همون سناریوی مشکل‌ساز (تغییر کسب‌وکار
    + دوباره ایمپورت) تست شد — این‌بار به‌جای ساختن رکورد جدید، درست
    به‌روزرسانی کرد.

    فایل‌هایی که باید overwrite/اضافه شوند:
    - strategic/views.py
    - strategic/management/commands/cleanup_duplicate_initiatives.py (فایل جدید)

    ⚠️ **ترتیب اجرا مهمه** — این دستور رو **فقط یه‌بار** و **قبل از**
    جایگزینی views.py بزنید (چون این پاک‌سازی مخصوص همین دیتابیس
    دوبرابرشده‌ست):
    python manage.py cleanup_duplicate_initiatives

۲۱۵. افزودن فایل لاگ جدید «بازدید کاربران» (`logs/users.log`) — جدا از
    `logs/app.log` موجود. برای هر صفحه‌ی واقعی که یه کاربر می‌بینه
    (نه فایل‌های استاتیک مثل CSS/JS/عکس)، یه خط ثبت می‌شه با:

    - تاریخ و ساعت (خودکار)
    - مسیر دقیق صفحه (فقط همون بخشی که رفته، نه آدرس کامل)
    - آی‌پی سیستم
    - نام سیستمی مربوط به همون آی‌پی (با جست‌وجوی DNS معکوس — روی
      شبکه‌ی داخلی سازمان معمولاً درست کار می‌کنه؛ اگه پیدا نشد،
      «—» می‌نویسه، بدون کند‌کردن سامانه)
    - نام کاربری (اگه لاگین بود؛ وگرنه «—»)

    نمونه‌ی واقعی از تست:
    2026-08-22 09:37:20 INFO مسیر=/roadmap/ | آی‌پی=127.0.0.1 |
    سیستم=localhost | کاربر=admin

    با سرور زنده تست شد — فایل‌های استاتیک درست فیلتر شدن، صفحات
    نامعتبر (۴۰۴) هم لاگ شدن (چون کاربر واقعاً بهش سر زده)، و هم
    حالت لاگین‌شده هم لاگین‌نشده درست کار کرد.

    فایل‌هایی که باید overwrite/اضافه شوند:
    - strategic/middleware.py (فایل جدید)
    - config/settings.py

    نیازی به هیچ دستوری نیست — فقط فایل‌ها را جایگزین و سرور را ری‌استارت
    کنید. فایل `logs/users.log` خودکار ساخته می‌شه.

۲۱۶. دو اصلاح روی «لاگ بازدید کاربران» (بند ۲۱۵) — طبق گزارش کاربر
    از اسکرین‌شات واقعی:

    الف) برچسب‌های فارسی (مسیر/آی‌پی/سیستم/کاربر) به انگلیسی
    (path/ip/host/user) تغییر کرد — چون توی Notepad ویندوز، ترکیب
    متن فارسی راست‌به‌چپ با آدرس‌های انگلیسی چپ‌به‌راست، ترتیب خط رو
    به‌هم می‌ریخت و ناخوانا می‌شد.

    ب) برای سیستم‌های دیگر روی شبکه (نه خودِ سروری که سامانه روش
    اجراست)، نام سیستم پیدا نمی‌شد و «—» می‌موند. Timeout جست‌وجو از
    ۰.۶ به ۱.۵ ثانیه افزایش یافت، و یه روش جایگزین (`getfqdn`) هم
    اضافه شد تا اگه روش اول جواب نداد، دوباره امتحان کنه.

    ⚠️ **یه محدودیت واقعی که باید صادقانه بگم**: پیداکردن نام سیستم
    برای کامپیوترهای دیگه‌ی شبکه، به تنظیمات DNS/شبکه‌ی داخلی سازمان
    وابسته‌ست (که خارج از کنترل خودِ این سامانه‌ست) — اگه سرور DNS
    داخلی شرکت، رکورد «PTR» (نام‌گذاری معکوس) رو برای اون کامپیوترها
    ثبت نکرده باشه، حتی با این بهبود هم ممکنه بازم «—» بمونه. لطفاً
    بعد از دپلوی امتحان کنید؛ اگه هنوز مشکل داشت، بگید تا با IT شبکه
    هماهنگ کنیم یا راه‌حل جایگزین (مثل ثبت دستی نام سیستم‌ها) رو
    بررسی کنیم.

    فایلی که باید overwrite شود:
    - strategic/middleware.py

    نیازی به هیچ دستوری نیست — فقط فایل را جایگزین و سرور را ری‌استارت
    کنید.

۲۱۷. افزودن نرخ دلار به ریال (منبع: TGJU.org) به ماژول «هوش بازار» —
    ادامه‌ی بند ۱۸۳ که قبلاً متوقف شده بود.

    **روش کار**: چون TGJU وب‌سرویس رایگان رسمی نداره، از یه نوار
    «قیمت لحظه‌ای» که توی همه‌ی صفحات سایت هست (شامل «دلار ۱,۹۱۵,۰۰۰
    (۱.۱۱٪)») با regex عدد رو استخراج می‌کنیم — نه وابسته به ساختار
    HTML خاص، فقط دنبال کلمه‌ی «دلار» و بعدش یه عدد بزرگ می‌گرده،
    که در برابر تغییرات جزئی طراحی سایت مقاوم‌تره.

    ⚠️ **یه محدودیت صادقانه که باید بدونید**: محیط تست من دسترسی به
    tgju.org نداشت (فقط چند دامنه‌ی محدود مثل GitHub مجازن)، پس
    نتونستم خودِ اتصال واقعی رو این‌جا تست کنم. منطق استخراج (regex)
    رو روی متن واقعی صفحه (که با جست‌وجوی وب دیدم) تست کردم و درست
    کار کرد، ولی **لطفاً بعد از دپلوی، خودتون دکمه‌ی «به‌روزرسانی از
    اینترنت» رو بزنید و مطمئن بشید واقعاً کار می‌کنه** — اگه خطا داد،
    متن دقیق خطا رو بفرستید تا اصلاح کنم.

    فایلی که باید overwrite شود:
    - strategic/views.py

    نیازی به هیچ دستوری نیست — فقط فایل را جایگزین و سرور را ری‌استارت
    کنید.

۲۱۷. دکمه‌ی «به‌روزرسانی از سیستم BSC» توی صفحه‌ی «پروژه‌های تحول» —
    طبق درخواست کامل کاربر.

    **چیکار می‌کنه**: با کلیک، برای هر پروژه‌ای که کد داره، دنبال همون
    کد توی جدول `projects` سیستم BSC (که باید از قبل روی همین سیستم،
    با SQL Server Express، Restore شده باشه) می‌گرده. اگه پیدا شد،
    آخرین مقدار پیشرفت واقعی (`calced_val` از جدول `project_values`،
    آخرین دوره‌ی غیرخالی) رو می‌خونه و جایگزین پیشرفت پروژه می‌کنه.
    اگه پیدا نشد، **پیشرفت فعلی دست‌نخورده می‌مونه** (نه صفر، نه حذف).

    در پایان، یه صفحه‌ی گزارش جدا باز می‌شه: تعداد به‌روزشده، تعداد
    پیدانشده، و لیست کامل کدهای پیدانشده.

    دکمه با همون افکت تپش نور «آشنایی با SWOT» (بند ۱۸۱) ساخته شد،
    فقط با رنگ سبز (به‌جای بنفش) تا با معنای «به‌روزرسانی» هم‌خونی
    داشته باشه.

    ⚠️ **محدودیت مهم تست**: من نتونستم اتصال واقعی به SQL Server رو
    توی محیط خودم تست کنم (چون اونجا SQL Server نصب نیست) — ولی دو
    چیز رو کامل تست کردم:
    ۱) مسیر خطا (وقتی pyodbc/اتصال دردسترس نیست) — پیام واضح می‌ده،
       کرش نمی‌کنه
    ۲) خودِ منطق اصلی (تطبیق کد، به‌روزرسانی، دست‌نخورده‌ماندن موارد
       پیدانشده) — با شبیه‌سازی کامل پاسخ واقعی BSC تست و تأیید شد

    امکان تغییر دستی پیشرفت هر پروژه (که از قبل توی فرم ویرایش پروژه
    وجود داشت) دست‌نخورده و کاملاً فعال می‌مونه.

    فایل‌هایی که باید overwrite/اضافه شوند:
    - strategic/views.py
    - strategic/urls.py
    - templates/strategic/roadmap.html
    - templates/strategic/bsc_sync_report.html (فایل جدید)

    ⚠️ پیش‌نیاز: `pyodbc` باید طبق راهنمای قبلی نصب و به
    `requirements.txt`/`wheels_offline` اضافه شده باشه، و دیتابیس
    `BSC` روی همین سیستم (`localhost\SQLEXPRESS`) Restore شده باشه.

۲۱۸. افزودن ستون «پروژه‌های مرتبط» به جدول شاخص‌های کلان در صفحه‌ی
    «اهداف کلان و KPI شرکت» — طبق سؤال درست یه ارزیاب («چرا ارتباط
    اهداف کلان/شاخص‌ها با پروژه‌ها رو نشون نمی‌دی؟»).

    داده‌ی این ارتباط (۷ از ۲۵ شاخص کلان، از کار قبلی‌مون در بندهای
    ۲۰۲-۲۰۳) از قبل توی دیتابیس بود، ولی هیچ‌جای این صفحه‌ی خاص نمایش
    داده نمی‌شد. الان کنار ستون «اهداف مرتبط»، یه ستون جدید «پروژه‌های
    مرتبط» با همون بج بنفش استاندارد سراسر سامانه اضافه شد — با هاور،
    عنوان کامل هر پروژه + مسئول + وضعیت + پیشرفت دیده می‌شه.

    منطق جست‌وجوی زنده‌ی جدول (که بر پایه‌ی شماره‌ی ستون کار می‌کنه)
    بررسی و تأیید شد که با این ستون جدید تداخلی نداره.

    با سرور زنده تست شد — هر ۷ بج درست رندر شدن، ۱۸ شاخص بدون پروژه
    هم درست «—» نشون دادن.

    فایلی که باید overwrite شود:
    - templates/strategic/company_goals.html

    نیازی به هیچ دستوری نیست — فقط فایل را جایگزین و سرور را ری‌استارت
    کنید.

۲۱۹. جابه‌جایی و کوچیک‌کردن ستون «پروژه‌های مرتبط» در جدول شاخص‌های
    کلان — طبق درخواست کاربر. از انتهای جدول (که عرض زیادی می‌گرفت)
    به بلافاصله بعد از ستون «حوزه» منتقل شد، و عرضش هم دقیقاً برابر
    با عرض ستون «حوزه» (۵۵ پیکسل) تنظیم شد.

    چون منطق جست‌وجوی زنده‌ی جدول بر پایه‌ی شماره‌ی ستون کار می‌کنه،
    شماره‌ی ستون‌های واحد/هدف‌عملکرد/هدف‌جاری/عملکرد‌جاری/درصد‌تحقق/
    اهداف‌مرتبط توی کد جاوااسکریپت هم هماهنگ شدن. با شبیه‌سازی واقعی
    مرورگر تست شد — جست‌وجو روی «واحد» و «حوزه» هر دو با ایندکس‌های
    تازه درست کار کردن.

    فایلی که باید overwrite شود:
    - templates/strategic/company_goals.html

    نیازی به هیچ دستوری نیست — فقط فایل را جایگزین و سرور را ری‌استارت
    کنید.

۲۲۰. افزودن عنوان بالای هر دو ستون در صفحه‌ی «اهداف کلان و KPI شرکت»
    — طبق درخواست مدیر. بالای ستون راست (لیست هدف‌های کلان گروه
    سایپا): «اهداف کلان گروه سایپا». بالای ستون چپ (اهداف خودمون که
    با کلیک روی هرکدوم از راست، نمایش داده می‌شن): «اهداف کلان سایپا
    یدک».

    با سرور زنده تست شد.

    فایلی که باید overwrite شود:
    - templates/strategic/company_goals.html

    نیازی به هیچ دستوری نیست — فقط فایل را جایگزین و سرور را ری‌استارت
    کنید.

۲۲۱. کوچیک‌کردن باکس «چشم‌انداز» بالای نقشه‌ی استراتژیک — طبق درخواست
    کاربر. اندازه‌ی فونت متن چشم‌انداز کاهش یافت، فاصله‌ی داخلی باکس
    فشرده‌تر شد، و کل متن چشم‌انداز الان به‌جای شکستن به چند خط، در
    **یک خط** (کنار همون باکس قرمز «چشم‌انداز») نمایش داده می‌شه —
    برای اطمینان از عدم بهم‌ریختگی روی صفحه‌های خیلی باریک، اگه متن
    واقعاً جا نشد، به‌جای شکستن، اسکرول افقی داخل همون باکس ظاهر
    می‌شه (نه بهم‌ریختن کل صفحه).

    با سرور زنده تست شد — هر ۵ نقشه‌ی کسب‌وکار سالم.

    فایلی که باید overwrite شود:
    - templates/strategic/stratmap.html

    نیازی به هیچ دستوری نیست — فقط فایل را جایگزین و سرور را ری‌استارت
    کنید.

۲۲۲. اصلاح باکس «چشم‌انداز» — طبق گزارش کاربر که نسخه‌ی قبلی (بند ۲۲۱)
    برای خوندن کلمات آخر نیاز به اسکرول افقی داشت. الان به‌جای اسکرول،
    اگه متن جا نشد، **حداکثر به ۲ خط** می‌شکنه (نه بیشتر، نه اسکرول) —
    با فونت و فاصله‌ی همچنان فشرده تا ارتفاع کلی باکس کم بمونه.

    با سرور زنده تست شد — هر ۵ نقشه‌ی کسب‌وکار سالم.

    فایلی که باید overwrite شود:
    - templates/strategic/stratmap.html

    نیازی به هیچ دستوری نیست — فقط فایل را جایگزین و سرور را ری‌استارت
    کنید.

۲۲۳. رفع نمایش «بیش از ۲۰٪» توی دو باکس بالای صفحه‌ی «تحلیل ذینفعان»
    — طبق گزارش درست کاربر.

    **علت پیدا شد**: منطق قبلی از یه «آستانه‌ی امتیاز» استفاده می‌کرد
    (همه‌ی ذینفعانی که امتیازشون از یه عدد مشخص بیشتر/مساوی بود) — که
    وقتی چند ذینفع امتیاز **مشترک** داشتن (تساوی)، همه‌شون با هم
    نمایش داده می‌شدن و از ۲۰٪ رد می‌شد. با داده‌ی واقعی، به‌جای ۳۸
    ذینفع (۲۰٪ واقعی از ۱۸۹)، ۴۹ تا نمایش داده می‌شد.

    **راه‌حل**: منطق به «برش دقیق موقعیتی» (Top-N) تغییر کرد — یعنی
    فهرست رو بر اساس امتیاز مرتب می‌کنه و **دقیقاً** همون تعداد
    محاسبه‌شده (۲۰٪ گردشده) رو برمی‌داره، بدون توجه به تساوی امتیاز.

    همچنین توضیحات «(امتیاز ≥ N)» که کنار عنوان هر دو باکس بود، طبق
    درخواست کاربر **حذف** شد.

    با سرور زنده تست شد — عدد نهایی دقیقاً ۳۸ (ریسک) و ۳۶ (فرصت) شد.

    فایل‌هایی که باید overwrite شوند:
    - strategic/views.py
    - templates/strategic/stakeholders.html

    نیازی به هیچ دستوری نیست — فقط فایل‌ها را جایگزین و سرور را ری‌استارت
    کنید.

۲۲۴. فشرده‌کردن تب‌های کسب‌وکار در صفحه‌ی «تحلیل SWOT» — طبق درخواست
    کاربر (که به دو خط شکسته بود و فضای زیادی می‌گرفت). همون الگوی
    موفق «نقشه استراتژیک» (بند ۱۸۶) اینجا هم اعمال شد:
    - عبارت تکراری «کسب و کار» فقط از نمایش تب‌ها حذف شد (اسم کامل
      + توضیح آرکی‌تایپ هنوز با هاور قابل‌دیدنه)
    - اندازه‌ی فونت و فاصله‌ی داخلی کوچیک‌تر شد
    - برچسب آرکی‌تایپ (مثل «· صمیمیت با مشتری») که قبلاً همیشه کنار
      اسم نمایش داده می‌شد، حذف شد (چون همون توضیح حالا با هاور در
      دسترسه)

    با سرور زنده تست شد — هر ۵ تب کسب‌وکار الان توی یه ردیف جا می‌شن.

    فایلی که باید overwrite شود:
    - templates/strategic/swot.html

    نیازی به هیچ دستوری نیست — فقط فایل را جایگزین و سرور را ری‌استارت
    کنید.

۲۲۵. تغییر رنگ‌های وضعیت پروژه در صفحه‌ی «پروژه‌های تحول» — طبق
    درخواست کاربر:
    - «در حال اجرا»: سبز ← **زرد** (`#C9A227`)
    - «انحراف پروژه»: بدون تغییر، همچنان **قرمز** (`#B0413E`)
    - «تکمیل‌شده»: خاکستری ← **سبز** (`#3E7A52`)

    هم رنگ واقعی روی هر کارت (نوار پیشرفت، برچسب وضعیت)، هم راهنمای
    رنگ (Legend) بالای صفحه که جدا و دستی نوشته شده بود، هر دو هماهنگ
    اصلاح شدن.

    با سرور زنده تست شد.

    فایل‌هایی که باید overwrite شوند:
    - strategic/models.py
    - templates/strategic/roadmap.html

    نیازی به هیچ دستوری نیست — فقط فایل‌ها را جایگزین و سرور را ری‌استارت
    کنید.

۲۲۶. ماژول جدید «مدیریت کاربران» — طبق درخواست کاربر. یه صفحه‌ی کامل
    و برندشده (نه پنل خام جنگو) برای مدیریت کاربران سامانه، توی خودِ
    منوی پورتال، بعد از «هوش بازار» با دسته‌بندی جدا «مدیریت سامانه»
    (فقط برای ادمین قابل‌دیدن).

    **امکانات**:
    - لیست کاربران با نام، ایمیل، نقش، کسب‌وکار مرتبط، آخرین ورود
      (خودکار از فیلد استاندارد جنگو)، وضعیت فعال/غیرفعال
    - افزودن/ویرایش کاربر (شامل تغییر رمز، بدون نیاز به رمز جدید در
      حالت ویرایش اگه لازم نبود)
    - **غیرفعال‌کردن** به‌جای حذف کامل (برای حفظ تاریخچه)؛ کاربر
      نمی‌تونه خودش رو غیرفعال کنه
    - ۳ نقش پیش‌فرض: «ادمین کامل» (`is_superuser`)، «ویرایشگر» (مجوز
      افزودن/ویرایش روی ۷ مدل اصلی محتوایی)، «کارشناس فقط‌مشاهده»
      (بدون هیچ مجوز ویرایش)
    - فیلد اختیاری «کسب‌وکار مرتبط» روی هر کاربر (فعلاً فقط اطلاعاتی/
      نمایشی، محدودکننده‌ی دسترسی واقعی نیست — اگه بعداً خواستید
      واقعاً اجرا بشه، باید جدا طراحی بشه)

    با سرور زنده کامل تست شد: ساخت کاربر، اعمال گروه، ویرایش، تغییر
    نقش، غیرفعال‌کردن — همه با موفقیت. تست امنیتی هم انجام شد: کاربر
    غیرادمین نه به صفحه دسترسی داره، نه لینکش رو توی منو می‌بینه.

    فایل‌هایی که باید overwrite/اضافه شوند:
    - strategic/models.py
    - strategic/views.py
    - strategic/urls.py
    - strategic/migrations/0077_userprofile.py (فایل جدید)
    - strategic/management/commands/setup_user_roles.py (فایل جدید)
    - templates/strategic/base.html
    - templates/strategic/user_list.html (فایل جدید)
    - templates/strategic/user_form.html (فایل جدید)

    دستورهای لازم (به همین ترتیب، فقط یه‌بار):
    python manage.py migrate
    python manage.py setup_user_roles

۲۲۷. اصلاح معماری زنجیره‌ی «ارکان جهت‌ساز» — طبق تشخیص درست کاربر که
    یه اشکال منطقی واقعی توی طراحی وجود داشت.

    **مشکل**: ارزش‌های سازمانی (که کل‌شرکتی‌ان) مستقیم به اهداف نقشه‌ی
    استراتژیک (که هرکدوم مخصوص یه کسب‌وکار خاصه، مثلاً «F1: سود
    عملیاتی بسته خدمت») وصل می‌شدن — یه پرش نامناسب در سلسله‌مراتب
    سازمانی، چون یه لایه‌ی میانی («اهداف کلان» که خودشون کل‌شرکتی‌ان و
    حتی به هدف گروه سایپا هم وصلن) رد می‌شد.

    **راه‌حل**: `OrgValue.related_objectives` از `StrategicObjective`
    (۱۷۸ تا، مخصوص کسب‌وکار) به `CompanyObjective` (۱۷ تا، کل‌شرکتی)
    تغییر کرد. زنجیره‌ی جدید:

    ```
    ارزش سازمانی → هدف کلان (۱۷ تا) → شاخص کلان → پروژه
    ```

    چون شاخص کلان مستقیم به هدف کلان وصله ولی پروژه از طریق **شاخص**
    به هدف می‌رسه (نه مستقیم)، منطق ساخت زنجیره در ویو هم بازنویسی
    شد تا این مسیر رو درست دنبال کنه.

    هر ۸ ارزش دوباره با تطبیق محتوایی دقیق به اهداف کلان وصل شدن (۱۸
    ارتباط، به‌جای ۱۱۲ ارتباط قبلی روی نقشه‌ی استراتژیک — دقیقاً همون
    کاهش حجمی که پیش‌بینی کرده بودیم).

    با شبیه‌سازی واقعی مرورگر، کل زنجیره (روی «مشتری‌مداری») تست شد —
    هدف کلان، شاخص کلیدی، و پروژه‌های مرتبط (شامل همون قابلیت جمع‌شدن
    پروژه‌های چندتایی از بند ۲۱۰) همه درست کار کردن. پیکر انتخاب هدف
    توی فرم ویرایش ارزش هم تأیید شد: الان ۱۷ گزینه (نه ۱۷۸ تا) داره.

    فایل‌هایی که باید overwrite/اضافه شوند:
    - strategic/models.py
    - strategic/views.py
    - strategic/migrations/0078_remove_orgvalue_related_objectives_and_more.py (فایل جدید)
    - strategic/management/commands/link_org_values_to_company_objectives.py (فایل جدید)

    دستورهای لازم (روی دیتابیس واقعی، به همین ترتیب):
    python manage.py migrate
    python manage.py link_org_values_to_company_objectives

۲۲۸. افزودن قابلیت «نوع» (پروژه/اقدام) به صفحه‌ی «پروژه‌های تحول» —
    طبق درخواست کامل کاربر:

    - فیلد جدید «نوع» روی هر آیتم، با ۲ گزینه: پروژه / اقدام
    - روی هر کارت، بج نوع دقیقاً **وسط** بالای کارت نمایش داده می‌شه
      (کد سمت راست، وضعیت سمت چپ — طبق درخواست)
    - توی بخش فیلترهای بالای صفحه، کنار سوییچ دسته‌بندی (کسب‌وکار/
      معاونت/کارگروه)، یه فیلتر جدید «همه / فقط پروژه‌ها / فقط
      اقدام‌ها» اضافه شد — و هنگام تعویض تب دسته‌بندی، این فیلتر
      حفظ می‌مونه (پاک نمی‌شه)
    - **ترتیب نمایش**: پروژه‌ها همیشه قبل از اقدام‌ها میان (صرف‌نظر
      از تاریخ شروع)
    - **خروجی/ورودی اکسل**: ستون جدید «نوع (پروژه/اقدام)» بین «عنوان»
      و «واحد مسئول» اضافه شد؛ منطق ورودی هم برای خوندن این ستون و
      تطبیق با ستون‌های بعدی (که همه یکی جابه‌جا شدن) اصلاح شد

    با سرور زنده، کامل تست شد:
    - یه «اقدام» با تاریخ **خیلی زودتر** از بقیه ساختم — تأیید شد که
      بازم درست **آخر لیست** نمایش داده می‌شه (نه بر اساس تاریخ)
    - فیلتر «فقط اقدام‌ها» درست فقط همون ۱ مورد رو نشون داد
    - چرخه‌ی کامل خروجی→خراب‌کردن دستی→ورودی مجدد از همون فایل، مقدار
      «نوع» رو درست بازگردوند

    فایل‌هایی که باید overwrite/اضافه شوند:
    - strategic/models.py
    - strategic/views.py
    - strategic/forms.py
    - strategic/migrations/0079_initiative_item_type.py (فایل جدید)
    - templates/strategic/roadmap.html

    دستور لازم:
    python manage.py migrate

۲۲۹. بازطراحی کامل نمایش «پروژه‌های تحول» — طبق درخواست کاربر (که با
    ~۳۵۰ آیتم، حالت ۴کارت‌در‌هر‌ردیف دیگه مناسب نبود). سه تغییر با هم:

    ۱) **دسته‌بندی بر اساس وضعیت** (آکاردئون قابل‌جمع‌شدن): «انحراف
       پروژه» و «در حال اجرا» پیش‌فرض باز، «تکمیل‌شده» پیش‌فرض بسته
       — هرکدوم با شمارنده‌ی تعداد کنار عنوانش

    ۲) **دکمه‌ی تعویض نما** (🔲 کارتی / ☰ فهرستی): نمای فهرستی، هر
       آیتم رو توی یه ردیف فشرده (کد، نوع، عنوان، مسئول، نوار پیشرفت
       کوچیک، ارتباطات، دکمه‌های عمل) نشون می‌ده — برای اسکن سریع
       ده‌ها آیتم بدون اسکرول زیاد

    ۳) جست‌وجوی زنده حالا **هم‌زمان روی هر دو نما** کار می‌کنه، و وقتی
       جست‌وجو می‌کنید، هر دسته‌ای که نتیجه داشته باشه (حتی «تکمیل‌شده»ی
       بسته) خودکار باز می‌شه

    **یه باگ واقعی هم حین تست پیدا و رفع شد**: موقع جداکردن کد کارت
    به یه فایل include مجزا (`_initiative_card.html`)، یه `</div>`
    از قلم افتاده بود — که باعث می‌شد کارت‌های دسته‌های بعدی، به‌اشتباه
    داخل کارت آخر دسته‌ی قبلی تودرتو بشن (هرچند ظاهراً درست دیده
    می‌شد، ولی جست‌وجو/شمارش رو به‌هم می‌ریخت). با شبیه‌سازی دقیق
    مرورگر پیدا و با اضافه‌کردن همون یه خط، کامل رفع شد — بعدش تعداد
    آیتم هر دسته (۱۴۲+۶۷=۲۰۹) دقیقاً با جمع کل مطابقت داشت.

    با سرور زنده و شبیه‌سازی کامل مرورگر (روی «بازرگانی قطعات»، ۲۱۰
    آیتم) تست شد: حالت باز/بسته‌ی پیش‌فرض هر دسته، کلیک برای
    باز/بستن، و جست‌وجوی زنده (هم روی نمای کارتی هم فهرستی) — همه
    درست و هماهنگ کار کردن.

    فایل‌هایی که باید overwrite/اضافه شوند:
    - strategic/views.py
    - templates/strategic/roadmap.html
    - templates/strategic/_initiative_card.html (فایل جدید)
    - templates/strategic/_initiative_row.html (فایل جدید)

    نیازی به هیچ دستوری نیست — فقط فایل‌ها را جایگزین و سرور را ری‌استارت
    کنید.
