استقرار یک‌باره روی D:\sysapp\strategic
==========================================

این پکیج دقیقاً همون ساختاری رو داره که باید روی D:\sysapp\strategic بشینه.
چیزی که از قبل دارید (venv، static\vendor، static\fonts، static\js کتابخانه‌ها،
config\) دست‌نخورده می‌مونه — این پکیج فقط این‌ها رو اضافه/جایگزین می‌کنه:

strategic\           ← کل اپ جنگو (views.py, urls.py, apps.py, admin.py, ...)
templates\strategic\ ← همه‌ی ۹ قالب HTML (تمیز و با انکودینگ درست UTF-8)
static\css\           ← فقط ۲ فایل جدید: strategic-fonts.css و strategic.css
static\js\             ← فقط ۱ فایل جدید: strategic-app.js

مراحل (فقط با File Explorer، نه PowerShell — تا انکودینگ فارسی خراب نشه)
----------------------------------------------------------------------------
۱. این زیپ رو دانلود کنید و روش راست‌کلیک → Extract All بزنید.

۲. اگه از قبل پوشه‌ی strategic\ (اپ جنگو) و templates\strategic\ ساخته‌اید
   و می‌خواید کاملاً تمیز جایگزین بشه، اول این دو پوشه رو حذف کنید:
   - D:\sysapp\strategic\strategic
   - D:\sysapp\strategic\templates\strategic

۳. از پوشه‌ی استخراج‌شده، این سه مورد رو کپی کنید و توی
   D:\sysapp\strategic پیست کنید (وقتی سؤال overwrite داد، بزنید Yes):
   - پوشه‌ی strategic
   - پوشه‌ی templates
   - پوشه‌ی static  (فقط css و js جدید رو اضافه می‌کنه، چیزی رو حذف نمی‌کنه)

۴. یه چک سریع بزنید که settings.py شما (D:\sysapp\strategic\config\settings.py)
   همچنان این سه خط رو داشته باشه (اگه قبلاً اضافه کرده بودید، لازم نیست
   دوباره دست بزنید):

   INSTALLED_APPS = [ ..., 'strategic' ]
   TEMPLATES[0]['DIRS'] = [BASE_DIR / 'templates']
   STATICFILES_DIRS = [BASE_DIR / 'static']

   و config\urls.py این خط رو داشته باشه:
   path('strategic/', include('strategic.urls'))

۵. اجرا:
   (venv) PS D:\sysapp\strategic> python manage.py runserver

   بعد http://127.0.0.1:8000/strategic/ رو باز کنید.

۵. مدل‌ها رو به دیتابیس اضافه کنید — چهار مدل جدید داریم (Study, Initiative,
   Risk, SWOTItem) که کتابخانه مطالعات، نقشه راه، نقشه ریسک و SWOT همه واقعاً
   بهشون وصلن (نه دیگه داده‌ی نمایشی):
   (venv) PS D:\sysapp\strategic> python manage.py makemigrations strategic
   (venv) PS D:\sysapp\strategic> python manage.py migrate

۶. سیستم ورود کاربری فعال شده — همه‌ی صفحات الان نیاز به لاگین دارن. دو خط
   زیر رو به config\settings.py اضافه کنید (هرجای فایل، مثلاً زیر STATIC_URL):

   LOGIN_URL = "strategic:login"
   LOGIN_REDIRECT_URL = "strategic:home"

   اگه هنوز کاربری نساختید:
   (venv) PS D:\sysapp\strategic> python manage.py createsuperuser

۷. اجرا:
   (venv) PS D:\sysapp\strategic> python manage.py runserver

   بعد http://127.0.0.1:8000/ رو باز کنید — باید به صفحه ورود هدایت بشید.

۸. نقشه استراتژیک هم الان مدل واقعی داره (StrategicObjective) — همون
   makemigrations/migrate مرحله ۵ این مدل رو هم می‌سازه، نیازی به تکرار نیست
   (اگه فایل‌های جدید رو زودتر کپی کرده بودید، دوباره makemigrations بزنید).

   برای اینکه همون ۱۲ هدف نمونه‌ای که روی سایت دیدید، به‌عنوان الگو توی
   ادمین هم باشه تا دقیق الگو بگیرید، این دستور رو یک‌بار اجرا کنید:

   (venv) PS D:\sysapp\strategic> python manage.py seed_stratmap

   این دستور امن است — اگه دوباره اجرا بشه، رکوردهای تکراری نمی‌سازه.

   بعد از این، توی /admin/ بخش «اهداف استراتژیک» رو باز کنید — همون ۱۲ مورد
   رو می‌بینید با ستون‌های منظر، محور، کد، عنوان، KPI، وضعیت. وضعیت و ترتیب
   نمایش مستقیم توی همون لیست هم قابل ویرایش سریعه (list_editable)، بدون
   نیاز به باز کردن هر رکورد.

۹. رفع باگ دکمه‌های افزودن (کتابخانه مطالعات، نقشه ریسک، نقشه راه، نقشه
   استراتژیک) — علت این بود که کلاس CSS سفارشی ما به اسم «modal» دقیقاً با
   کلاس داخلی خود Bootstrap هم‌نام بود و Bootstrap مقادیر display:none و
   position:fixed رو روی اون تحمیل می‌کرد، برای همین پس‌زمینه تیره می‌شد
   ولی خود کادر سفید هیچ‌وقت نمایش داده نمی‌شد. کلاس رو به «modal-card»
   تغییر دادم — این نسخه دیگه این مشکل رو نداره.

۱۰. نقشه استراتژیک حالا مستقیم توی خود سایت هم به‌طور کامل قابل ویرایشه:
    - دکمه‌ی مداد روی هر کارت → فرم افزودن رو با اطلاعات همون هدف پر می‌کنه
      تا ویرایشش کنید (بدون نیاز به ادمین)
    - برای «جابه‌جا کردن» یک هدف بین منظر/محورهای مختلف، کافیه توی همون
      فرم، فیلد «منظر BSC» یا «محور استراتژیک» رو عوض کنید — کارت خودش به
      خانه‌ی جدید منتقل می‌شه
    - فیلد «ترتیب نمایش» هم تعیین می‌کنه چند هدف داخل یک خانه به چه ترتیبی
      روی هم بچینن

۱۱. رفع سرریز کادر «اقدام کاهشی» — استایل عمومی فیلدها فقط روی input/select
    بود، textarea رو یادش رفته بود؛ الان درست شد.

۱۲. داده‌ی نمونه برای نقشه ریسک — دقیقاً مثل seed_stratmap، این دستور ۶
    ریسک نمونه‌ی نزدیک به عملکرد یک شرکت قطعات یدکی رو ثبت می‌کنه تا الگو
    داشته باشید (امن است، تکراری نمی‌سازد):

    (venv) PS D:\sysapp\strategic> python manage.py seed_risks

۱۳. ماژول‌های «هوش رقابتی و بازار» و «تحلیل PESTEL» هم الان مدل واقعی دارن
    (Competitor و PestelFactor) — همون makemigrations/migrate این‌ها رو هم
    می‌سازه. برای داده‌ی نمونه:

    (venv) PS D:\sysapp\strategic> python manage.py seed_market
    (venv) PS D:\sysapp\strategic> python manage.py seed_pestel

۱۴. سیستم سطح دسترسی — دو گروه کاربری ساخته می‌شه: «ویرایشگران» (اجازه
    افزودن/ویرایش/حذف در همه‌ی ماژول‌ها) و «بینندگان» (فقط مشاهده، دکمه‌های
    افزودن/حذف/ویرایش اصلاً براشون نمایش داده نمی‌شه). این دستور رو یک‌بار
    بزنید:

    (venv) PS D:\sysapp\strategic> python manage.py seed_groups

    بعد برید /admin/ → بخش Users → یوزر مورد نظر رو باز کنید → پایین صفحه
    قسمت «Groups» رو پیدا کنید → یکی از دو گروه رو انتخاب و منتقل کنید به
    ستون «Chosen groups» → Save. کاربر admin (سوپریوزر) نیازی به گروه نداره،
    چون دسترسی کامل به همه‌چیز داره.

۱۵. آماده‌سازی برای اجرای واقعی (production) — این چند مرحله سیستم رو از
    حالت «فقط روی سیستم من» به حالت «قابل اجرا و امن برای کل سازمان» می‌بره:

    الف) نصب پکیج‌های جدید:
    (venv) PS D:\sysapp\strategic> pip install -r requirements.txt

    ب) فایل .env.example رو کپی کنید و اسمش رو بذارید .env، بعد مقادیرش رو
       با یک کلید تصادفی واقعی و آدرس سرورتون پر کنید. برای ساختن یک
       SECRET_KEY تصادفی امن:
       (venv) PS D:\sysapp\strategic> python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"

    ج) توی config\settings.py این تغییرات رو بدید:

       - زیر خط `BASE_DIR = Path(__file__).resolve().parent.parent` این
         دو خط رو اضافه کنید:
         import os
         from dotenv import load_dotenv
         load_dotenv(BASE_DIR / ".env")

       - خط‌های SECRET_KEY، DEBUG، ALLOWED_HOSTS رو با این جایگزین کنید
         (مقدار فعلی SECRET_KEY خودتون رو به‌جای «کلید-قبلی-شما» به‌عنوان
         fallback بذارید تا اگه .env نبود، بازم کار کنه):
         SECRET_KEY = os.environ.get("DJANGO_SECRET_KEY", "کلید-قبلی-شما")
         DEBUG = os.environ.get("DJANGO_DEBUG", "True") == "True"
         ALLOWED_HOSTS = os.environ.get("DJANGO_ALLOWED_HOSTS", "127.0.0.1,localhost").split(",")

       - توی MIDDLEWARE، بلافاصله بعد از
         'django.middleware.security.SecurityMiddleware' این خط رو اضافه کنید:
         'whitenoise.middleware.WhiteNoiseMiddleware',

       - زیر STATIC_URL این دو تنظیم رو اضافه کنید:
         STATIC_ROOT = BASE_DIR / "staticfiles"
         STORAGES = {
             "default": {"BACKEND": "django.core.files.storage.FileSystemStorage"},
             "staticfiles": {"BACKEND": "whitenoise.storage.CompressedManifestStaticFilesStorage"},
         }

    د) جمع‌آوری فایل‌های استاتیک برای حالت production:
       (venv) PS D:\sysapp\strategic> python manage.py collectstatic

    ه) اجرا با سرور واقعی (به‌جای runserver):
       (venv) PS D:\sysapp\strategic> run_production.bat
       یا مستقیم:
       (venv) PS D:\sysapp\strategic> waitress-serve --listen=0.0.0.0:8000 config.wsgi:application

       حالا از هر سیستم دیگه‌ای توی شبکه‌ی داخلی شرکت، با آی‌پی این سیستم
       (مثلاً http://192.168.1.50:8000) قابل دسترسیه، نه فقط از خود سیستم.

    نکته: SQLite برای تیم‌های کوچک (چند نفر) کاملاً کافیه. اگه بعداً تعداد
    کاربران همزمان زیاد شد، مهاجرت به PostgreSQL قدم بعدیه — فعلاً لازم
    نیست نگرانش باشید.

۱۶. داشبورد خانه واقعی شد — گیج‌ها، آمار ماژول‌ها، و «آخرین فعالیت‌ها» همه
    الان مستقیم از دیتابیس محاسبه می‌شن، نه اعداد ثابت. چون به مدل‌های
    Competitor/PestelFactor/StrategicObjective یه فیلد created_at اضافه شد،
    یه migration جدید لازمه:

    (venv) PS D:\sysapp\strategic> python manage.py makemigrations strategic
    (venv) PS D:\sysapp\strategic> python manage.py migrate

    (این فیلد جدید خودکار با زمان الان پر می‌شه برای رکوردهای قبلی، نیازی
    به وارد کردن مقدار دستی نیست.)

۱۷. تقویم شمسی — نصب و تنظیمات:

    الف) پکیج جدید رو نصب کنید:
    (venv) PS D:\sysapp\strategic> pip install -r requirements.txt

    ب) فایل settings.py که این‌بار می‌فرستم رو کامل جایگزین config\settings.py
       کنید (LANGUAGE_CODE به 'fa' و TIME_ZONE به 'Asia/Tehran' تغییر کرده —
       همین باعث می‌شه عبارت‌های داخلی جنگو هم فارسی نمایش داده بشن).

    ج) فایل‌های strategic\jalali_utils.py و پوشه‌ی
       strategic\templatetags\ رو کپی کنید (فایل‌های جدید هستن).

    د) هیچ migration جدیدی لازم نیست — تاریخ‌ها همچنان میلادی توی دیتابیس
       ذخیره می‌شن (چون گانت‌چارت بهش نیاز داره)، فقط فرم ورودی و نمایش
       تبدیل به شمسی شدن.

    محدودیت مهم که باید بدونید: خود نمودار گانت‌چارت (کتابخانه frappe-gantt)
    محورهای بالای نمودارش (اسم ماه‌ها) رو میلادی و انگلیسی نشون می‌ده، چون
    این کتابخانه از تقویم شمسی پشتیبانی نمی‌کنه — این یه محدودیت خود
    کتابخانه‌ست، نه باگ. همه‌جای دیگه‌ی سایت (فرم‌ها، لیست‌ها، هدر بالای
    صفحه) کاملاً شمسیه.

۱۸. نقشه استراتژیک بازطراحی کامل شد — طرح تیره «کاکپیت» با کارت‌های
    درخشان و سیم‌های متحرک بین اهداف که رابطه‌ی علّی واقعی رو نشون می‌دن:

    الف) چون یه رابطه‌ی جدید بین اهداف اضافه شده (اینکه کدوم هدف به کدوم
    هدف دیگه کمک می‌کنه)، یه migration جدید لازمه:
    (venv) PS D:\sysapp\strategic> python manage.py makemigrations strategic
    (venv) PS D:\sysapp\strategic> python manage.py migrate

    ب) توی فرم افزودن/ویرایش هر هدف، یه فیلد چندانتخابی جدید هست: «این
    هدف به کدام هدف(ها) کمک می‌کند». وقتی مشخص کنید، یه سیم متحرک بین
    اون دو کارت روی نقشه کشیده می‌شه. اگه این فیلد رو خالی بذارید،
    کارت بدون سیم نمایش داده می‌شه — کاملاً اختیاریه.

    ج) روی هر هدف کلیک کنید تا زنجیره‌ی علّیش (همه‌ی اهدافی که بهش کمک
    می‌کنن + همه‌ی اهدافی که خودش بهشون کمک می‌کنه) روشن بشه. دکمه‌ی
    «نمای کامل نقشه» همه‌چیز رو برمی‌گردونه.

    د) تعداد اهداف توی هر منظر دیگه محدود به یکی نیست — هر تعداد که
    اضافه کنید، همه زیر هم توی همون منظر نمایش داده می‌شن.

۱۹. نقشه ریسک و تحلیل SWOT بازطراحی کامل شدن، مطابق دو نمونه‌ای که فرستادید:

    الف) مدل ریسک فیلدهای جدید گرفت: دسته‌بندی (عملیات/بازار/مالی/حقوقی/
    فناوری اطلاعات/منابع انسانی)، راهبرد پاسخ (کاهش/انتقال/پذیرش/اجتناب)،
    روند نسبت به دوره قبل، شاخص کلیدی ریسک (KRI)، و ارتباط با هدف
    استراتژیک. یه migration جدید لازمه:

    (venv) PS D:\sysapp\strategic> python manage.py makemigrations strategic
    (venv) PS D:\sysapp\strategic> python manage.py migrate

    ب) مدل SWOT هم «وزن اهمیت» از ۱ تا ۵ گرفت (به‌جای بالا/متوسط قبلی) و
    یه مدل جدید برای راهبردهای TOWS اضافه شد که حالا کاملاً قابل ویرایش
    از خود سایته (نه دیگه متن ثابت).

    ج) موقعیت استراتژیک سازمان (SO/ST/WO/WT) دیگه به‌صورت خودکار از
    میانگین وزن واقعی S/W/O/T شما محاسبه می‌شه، نه ثابت.

    نکته مهم: برخلاف نمونه‌ی داشبورد ریسک، بخش «ریسک ذاتی در برابر
    باقیمانده در برابر هدف» (سه امتیاز جداگانه) رو پیاده نکردم چون نیاز
    به تغییر متدولوژی ریسک سازمان داره — فعلاً همون یک امتیاز فعلی
    (احتمال × اثر) رو نگه داشتم. اگه بعداً خواستید اون سیستم سه‌گانه رو
    هم اضافه کنیم، بگید.

۲۰. عذرخواهی + رفع کامل: نسخه‌ی قبلی نقشه ریسک، ماتریس ذاتی/باقیمانده/
    هدف رو نداشت (فقط یه امتیاز ساده بود) و ماتریس ۴×۴ بود نه ۵×۵. الان
    دقیقاً مطابق آرتیفکتی که فرستادید بازسازی شد:

    - مقیاس احتمال و شدت اثر از ۴ سطح به ۵ سطح تغییر کرد (۱ تا ۵)
    - امتیاز به‌جای جمع (قبلی) حالا ضرب می‌شه (احتمال × شدت اثر)، حداکثر ۲۵
    - هر ریسک سه امتیاز داره: «ذاتی» (پیش از کنترل)، «باقیمانده» (وضعیت
      فعلی)، «هدف» (سطح قابل‌قبول پس از اقدامات برنامه‌ریزی‌شده)
    - دکمه‌ی «نمایش ریسک ذاتی» روی ماتریس، موقعیت ذاتی رو با حباب
      خط‌چین و یه خط نقطه‌چین به موقعیت باقیمانده وصل می‌کنه
    - کارت «اثربخشی کنترل‌ها» اضافه شد (چقدر از ریسک ذاتی، با کنترل‌های
      فعلی کاهش یافته)

    چون این تغییرات، فیلدهای جدید به مدل اضافه می‌کنه (احتمال/اثر ذاتی و
    هدف)، یه migration جدید لازمه:

    (venv) PS D:\sysapp\strategic> python manage.py makemigrations strategic
    (venv) PS D:\sysapp\strategic> python manage.py migrate

    نکته مهم: ۸ ریسکی که قبلاً وارد کرده بودید، برای فیلدهای جدید (ذاتی
    و هدف) یه مقدار پیش‌فرض می‌گیرن (ذاتی=۴×۴=۱۶، هدف=۲×۲=۴) که واقعی
    نیستن — باید هرکدوم رو با دکمه‌ی مداد باز کنید و امتیاز ذاتی/هدف
    واقعیش رو وارد کنید تا کارت «اثربخشی کنترل‌ها» و دکمه‌ی «نمایش ریسک
    ذاتی» درست و معنادار بشن.

۲۱. کسب‌وکارها — نقشه استراتژیک و SWOT حالا به‌ازای هر کسب‌وکار جداست:

    الف) یه مدل جدید «کسب‌وکار» اضافه شد. یه migration جدید لازمه:
    (venv) PS D:\sysapp\strategic> python manage.py makemigrations strategic
    (venv) PS D:\sysapp\strategic> python manage.py migrate

    ب) این دستور ۵ کسب‌وکار واقعی (بسته خدمت، کارگزاری خودرو، بازرگانی
    قطعات یدکی، آپشن، خدمات تعمیراتی) رو می‌سازه و داده‌های فعلی (نقشه
    استراتژیک نمونه + SWOT سناریوی هوای مه‌آلود) رو به «بازرگانی قطعات
    یدکی» وصل می‌کنه:
    (venv) PS D:\sysapp\strategic> python manage.py seed_business_units

    ج) بالای صفحه‌ی «نقشه استراتژیک» و «تحلیل SWOT»، یه ردیف تب برای
    سوییچ بین کسب‌وکارها اضافه شده، به‌همراه دکمه‌ی «کسب‌وکار جدید» برای
    اضافه کردن کسب‌وکار تازه — بدون نیاز به ادمین.

    د) ماژول‌های دیگه (ریسک، ابتکارات، مطالعات، PESTEL، بازار) طبق تصمیمی
    که گرفتیم، سازمانی/مشترک باقی می‌مونن — به کسب‌وکار خاصی وصل نیستن.

    نکته: ۱۲ هدف نمونه‌ی Kaplan-Norton که از اول سایت داشتید، فعلاً به
    «بازرگانی قطعات یدکی» وصل شدن چون محتوای واقعیش رو نداشتم — هر وقت
    خواستید با محتوای واقعی نقشه‌ی همون کسب‌وکار (از روی عکس) جایگزینش
    می‌کنیم.

۲۲. محورهای استراتژیک حالا مخصوص هر کسب‌وکار هستن (نه ۳ محور ثابت
    سراسری). یه مدل جدید «محور نقشه استراتژیک» اضافه شد. یه migration
    جدید لازمه:

    (venv) PS D:\sysapp\strategic> python manage.py makemigrations strategic
    (venv) PS D:\sysapp\strategic> python manage.py migrate

    نکته مهم: چون نوع فیلد «محور» از یک لیست ثابت به یک رابطه (FK) تغییر
    کرد، اهداف استراتژیکی که از قبل ثبت کرده بودید محورشون خالی می‌شه
    (بدون‌محور) — این طبیعیه، چون قراره محتوای واقعی هر کسب‌وکار رو با هم
    وارد کنیم و محور واقعی هرکدوم رو مشخص کنیم؛ چیزی که تا الان ثبت شده
    بود عمداً نمونه/دمو بود.

    توی صفحه‌ی نقشه استراتژیک، زیر ردیف انتخاب کسب‌وکار، یه ردیف جدید
    «محورهای این کسب‌وکار» هست — از همون‌جا محور جدید اضافه یا حذف کنید،
    بدون نیاز به ادمین. رنگ هر محور خودکار و متفاوت انتخاب می‌شه.

۲۳. دسترسی بدون لاگین برای بخش‌های غیرحساس — نیازی به migrate نیست، فقط
    این دو فایل عوض شدن:

    الف) بدون لاگین قابل مشاهده: نقشه استراتژیک، PESTEL، هوش رقابتی و
    بازار، کتابخانه مطالعات، نقشه راه ابتکارات.

    ب) همچنان نیاز به لاگین دارن: خانه، تحلیل SWOT، نقشه ریسک — توی منوی
    سمت راست، وقتی لاگین نکرده باشید، کنار این ۳ مورد یه آیکون قفل کوچیک
    نشون داده می‌شه؛ کلیک روش هم می‌بره به صفحه‌ی ورود.

    ج) دکمه‌های افزودن/ویرایش/حذف همه‌جا (حتی توی صفحات بدون‌لاگین) فقط
    برای کاربرانی که لاگین کرده و دسترسیش رو دارن نمایش داده می‌شه —
    کاربر بدون لاگین فقط می‌بینه، هیچ دکمه‌ی ویرایشی نمی‌بینه.

    د) بالای صفحه، برای کاربر بدون لاگین، یه دکمه‌ی «ورود برای دسترسی
    کامل» نشون داده می‌شه.

۲۴. خروجی PDF زیبا برای نقشه استراتژیک و SWOT — بدون نیاز به نصب هیچ
    کتابخانه‌ی جدیدی. دکمه‌ی «خروجی چاپی» بالای صفحه از قبل بود؛ فقط یه
    استایل مخصوص چاپ اضافه کردم که ظاهر تیره/تعاملی رو موقع چاپ به یه
    گزارش تمیز و سبک تبدیل می‌کنه، با یه سربرگ که اسم کسب‌وکار انتخاب‌شده
    رو نشون می‌ده.

    نحوه‌ی گرفتن PDF:
    الف) توی صفحه‌ی نقشه استراتژیک یا SWOT، کسب‌وکار مورد نظر رو انتخاب کنید
    ب) دکمه‌ی «خروجی چاپی» رو بزنید
    ج) توی پنجره‌ی چاپ مرورگر، گزینه‌ی Printer رو روی «Save as PDF» بگذارید
    د) حتماً گزینه‌ی «More settings → Background graphics» رو تیک بزنید
       تا رنگ‌های ربع‌های SWOT هم توی PDF چاپ بشن

    نیازی به migrate یا نصب پکیج جدید نیست، فقط دو فایل عوض شدن.

۲۵. جایگزینی خروجی چاپی با یه صفحه‌ی مخصوص PDF — نسخه‌ی قبلی (چاپ مستقیم
    همون صفحه‌ی تعاملی تیره) زیبا نبود، پس یه صفحه‌ی کاملاً جدا و ساده
    ساختم: فقط لوگو، اسم کسب‌وکار، و محتوای تمیز (بدون سایدبار، بدون
    سیم‌های متحرک، بدون پس‌زمینه‌ی تیره).

    دو فایل جدید و ۴ فایل تغییریافته اضافه شد. نیازی به migrate نیست.

    نحوه‌ی استفاده: توی صفحه‌ی نقشه استراتژیک یا SWOT، دکمه‌ی جدید
    «خروجی PDF» (نه همون «خروجی چاپی» قبلی بالای صفحه) رو بزنید — یه تب
    جدید با گزارش تمیز باز می‌شه و خودش پنجره‌ی چاپ رو باز می‌کنه؛ کافیه
    Printer رو «Save as PDF» بگذارید.

۲۶. تحلیل PESTEL واقعی جایگزین نمونه شد، و مدل هم گسترش یافت:

    الف) هر عامل حالا یه «نوع» هم داره: عامل محیطی / فرصت / تهدید — چون
    داده‌ی واقعی شما برای هر دسته (سیاسی، اقتصادی...) این سه نوع رو جدا
    داشت. یه migration جدید لازمه:

    (venv) PS D:\sysapp\strategic> python manage.py makemigrations strategic
    (venv) PS D:\sysapp\strategic> python manage.py migrate

    ب) بعد این دستور رو بزنید تا نمونه‌های قبلی پاک و ۲۹ مورد واقعی
    جایگزین بشه:

    (venv) PS D:\sysapp\strategic> python manage.py load_real_pestel

    ج) صفحه‌ی PESTEL بازطراحی شد — هر دسته حالا یه کارت با ۳ ستون
    (عوامل محیطی / فرصت‌ها / تهدیدها) داره، به‌جای یه لیست ساده.

۲۷. ماژول جدید «پنج نیروی پورتر» — بعد از PESTEL توی منو اضافه شد،
    سازمانی/مشترک (نه به‌ازای کسب‌وکار)، بدون لاگین هم قابل مشاهده.

    الف) مدل جدید اضافه شد. یه migration جدید لازمه:
    (venv) PS D:\sysapp\strategic> python manage.py makemigrations strategic
    (venv) PS D:\sysapp\strategic> python manage.py migrate

    ب) داده‌ی واقعی رو بارگذاری کنید:
    (venv) PS D:\sysapp\strategic> python manage.py load_real_porter

    ج) هر ۵ نیرو (رقابت موجود، قدرت مشتریان، قدرت تأمین‌کنندگان، تهدید
    رقبای جدید، تهدید کالای جایگزین) ثابت هستن — فقط قابل ویرایش‌اند
    (نه افزودن/حذف)، چون این ۵ تا کل مدل رو تشکیل می‌دن.

۲۸. صفحه‌ی جدید «چشم‌انداز و ارزش‌ها» — بالای منو، کنار «خانه» اضافه شد.
    سازمانی/مشترک، بدون لاگین هم قابل مشاهده. برخلاف بقیه‌ی ماژول‌ها،
    ویرایشش فقط برای ادمین اصلی (Superuser) مجازه — حتی گروه
    «ویرایشگران» هم نمی‌تونن دستش بزنن، چون این محتوا رسمی و امضاشده‌ی
    مدیرعامله.

    الف) سه مدل جدید اضافه شد (چشم‌انداز/مأموریت، ارزش‌های سازمانی، بندهای
    خط‌مشی کیفیت). یه migration جدید لازمه:
    (venv) PS D:\sysapp\strategic> python manage.py makemigrations strategic
    (venv) PS D:\sysapp\strategic> python manage.py migrate

    ب) داده‌ی واقعی رو بارگذاری کنید:
    (venv) PS D:\sysapp\strategic> python manage.py load_org_identity

    ج) محتوا: چشم‌انداز و مأموریت، چرخ ۷ ارزش سازمانی دور «مشتری‌مداری»،
    و ۷ بند خط‌مشی کیفیت — همه با امکان ویرایش/افزودن/حذف فقط برای ادمین.

۲۹. «نقشه راه ابتکارات» به «پروژه‌های تحول» تغییر نام داد، و مثل نقشه
    استراتژیک/SWOT، حالا **به‌ازای هر کسب‌وکار جداست** و هر پروژه می‌تونه
    به یک یا چند **هدف استراتژیک همان کسب‌وکار** وصل بشه.

    الف) دو فیلد جدید به مدل پروژه اضافه شد (کسب‌وکار + رابطه با اهداف).
    یه migration جدید لازمه:
    (venv) PS D:\sysapp\strategic> python manage.py makemigrations strategic
    (venv) PS D:\sysapp\strategic> python manage.py migrate

    ب) این دستور رو دوباره بزنید تا پروژه‌های قبلی (بدون کسب‌وکار) به
    «بازرگانی قطعات» وصل بشن (مثل بقیه‌ی داده‌های قدیمی):
    (venv) PS D:\sysapp\strategic> python manage.py seed_business_units

    ج) توی صفحه‌ی «پروژه‌های تحول»، بالای صفحه یه ردیف تب برای انتخاب
    کسب‌وکار اضافه شد؛ فرم افزودن/ویرایش پروژه هم یه فیلد چندانتخابی
    «اهداف استراتژیک مرتبط» داره که فقط اهداف همون کسب‌وکار انتخاب‌شده
    رو نشون می‌ده.

۳۰. دفتر ثبت ریسک واقعی سایپا یدک (۹ ریسک) جایگزین نمونه‌های قبلی شد،
    و مدل ریسک هم دو فیلد جدید گرفت (چون داده‌ی واقعی این دو رو داشت و
    مدل قبلی جایی براشون نداشت):

    الف) «منشأ/علت بروز» و «پیامد ریسک» — یه migration جدید لازمه:
    (venv) PS D:\sysapp\strategic> python manage.py makemigrations strategic
    (venv) PS D:\sysapp\strategic> python manage.py migrate

    ب) داده‌ی واقعی رو جایگزین کنید:
    (venv) PS D:\sysapp\strategic> python manage.py load_real_strategic_risks

    ج) روی عنوان هر ریسک توی جدول، هاور کنید تا علت و پیامدش رو به‌صورت
    Tooltip ببینید؛ توی فرم ویرایش هم فیلد جداگانه دارن.

    نکته مهم: فایل منبع فقط یه امتیاز ریسک داشت (نه سه‌تا: ذاتی/باقیمانده/
    هدف)، پس فعلاً هر سه یکی‌ان و «اثربخشی کنترل‌ها» صفر نشون داده می‌شه —
    باید بعداً دستی امتیاز ذاتی و هدف واقعی هر ریسک رو وارد کنید. همچنین
    یه امتیاز اعشاری (۳.۵۰) بود که چون سیستم فقط عدد صحیح قبول می‌کنه، به
    نزدیک‌ترین عدد (۴) گرد شده.

۳۱. ماژول جدید «تحلیل McKinsey 7S» — بعد از «پنج نیروی پورتر» توی منو
    اضافه شد، سازمانی/مشترک، بدون لاگین هم قابل مشاهده.

    الف) مدل جدید اضافه شد. یه migration جدید لازمه:
    (venv) PS D:\sysapp\strategic> python manage.py makemigrations strategic
    (venv) PS D:\sysapp\strategic> python manage.py migrate

    ب) داده‌ی واقعی رو بارگذاری کنید:
    (venv) PS D:\sysapp\strategic> python manage.py load_real_mckinsey7s

    ج) هر ۷ مؤلفه (استراتژی، ساختار، سیستم‌ها، ارزش‌های مشترک، مهارت‌ها،
    کارکنان، سبک) ثابت‌اند — فقط قابل ویرایش‌اند (نه افزودن/حذف)، هرکدوم
    با وضعیت فعلی + نقاط قوت + نقاط ضعف/ریسک.

    د) کارت جدید هم زیر «پنج نیروی پورتر» توی صفحه‌ی خانه اضافه شد.

۳۲. ماژول جدید «زنجیره ارزش پورتر» — بعد از McKinsey 7S توی منو اضافه
    شد (با یادداشت «تحلیل محیطی» جلوش تا ارتباطش با گزارش سازمانی روشن
    باشه)، سازمانی/مشترک، بدون لاگین هم قابل مشاهده.

    الف) مدل جدید اضافه شد. یه migration جدید لازمه:
    (venv) PS D:\sysapp\strategic> python manage.py makemigrations strategic
    (venv) PS D:\sysapp\strategic> python manage.py migrate

    ب) داده‌ی واقعی رو بارگذاری کنید:
    (venv) PS D:\sysapp\strategic> python manage.py load_real_value_chain

    ج) ۹ فعالیت ثابت (۴ پشتیبان: زیرساخت، منابع انسانی، فناوری، تدارکات؛
    ۵ اصلی: لجستیک ورودی، عملیات، لجستیک خروجی، بازاریابی و فروش، خدمات)
    — چیدمان مثل نمودار کلاسیک زنجیره ارزش (نوار پشتیبان بالا، ردیف
    اصلی پایین). فقط قابل ویرایش‌اند، نه افزودن/حذف.

    د) دسترسی ویرایش از طریق سیستم مجوز استاندارد سایت (گروه ویرایشگران)
    است — دقیقاً مثل پورتر و McKinsey 7S، نه محدود به فقط ادمین اصلی.

    نکته: چون فایل مبدأ اسکن/OCR بود، بهتره محتوای هر فعالیت رو یک‌بار
    با متن اصلی خودتون تطبیق بدید.

۳۳. **مهم‌ترین تغییر معماری تا الان:** هر مورد SWOT حالا می‌تونه به منشأ
    واقعی‌اش وصل بشه — فرصت/تهدید به یه عامل PESTEL یا نیروی پورتر،
    قوت/ضعف به یه مؤلفه McKinsey 7S یا فعالیت زنجیره ارزش.

    الف) ۴ فیلد رابطه‌ی جدید به مدل SWOT اضافه شد. یه migration جدید
    لازمه:
    (venv) PS D:\sysapp\strategic> python manage.py makemigrations strategic
    (venv) PS D:\sysapp\strategic> python manage.py migrate

    ب) هیچ داده‌ای پاک نمی‌شه — SWOTهای قبلی همون‌طور بدون منبع می‌مونن،
    مشکلی نیست. این فیلدها کاملاً اختیاری‌ان.

    ج) توی صفحه‌ی SWOT، حالا کنار هر مورد یه دکمه‌ی مداد جدید هست (قبلاً
    فقط حذف داشت) — با کلیک روش، می‌تونید متن/وزن/منبع رو ویرایش کنید.
    اگه منبعی انتخاب بشه، کنار متن اون مورد یه برچسب کوچیک نشون میده که
    از کجا اومده.

۳۳. اتصال ردیابی‌پذیر SWOT به منابع تحلیلی — همون قدم اول از سند
    معماری کامل که باهم مرور کردیم.

    الف) هر مورد SWOT حالا می‌تونه به منبعی که ازش اومده وصل بشه:
    - فرصت/تهدید (O/T) → به یه عامل PESTEL یا یه نیروی پورتر
    - قوت/ضعف (S/W) → به یه مؤلفه McKinsey 7S یا یه فعالیت زنجیره ارزش

    یه migration جدید لازمه:
    (venv) PS D:\sysapp\strategic> python manage.py makemigrations strategic
    (venv) PS D:\sysapp\strategic> python manage.py migrate

    ب) موارد SWOT حالا **قابل ویرایش** هم هستن (قبلاً فقط افزودن/حذف
    داشتن) — دکمه‌ی مداد کنار هر مورد، هم متن/وزن، هم منبع مرتبطش رو
    قابل تغییر می‌کنه.

    ج) اگه یه مورد SWOT به منبعی وصل باشه، زیر متنش یه برچسب کوچیک با
    آیکون زنجیر نشون داده می‌شه (هاور کنید تا نام کامل منبع رو ببینید).

    د) داده‌های قبلی SWOT دست‌نخورده می‌مونن — این فیلد کاملاً اختیاریه،
    فقط برای موارد جدید (یا موقع ویرایش قدیمی‌ها) قابل تنظیمه.

۳۴. راهبردهای TOWS حالا می‌تونن به موارد **مشخص** SWOT وصل بشن (نه فقط
    دسته‌ی کلی) — مثلاً یه راهبرد SO می‌تونه بگه دقیقاً از کدوم قوت +
    کدوم فرصت اومده.

    الف) یه رابطه‌ی جدید به مدل TOWS اضافه شد. یه migration جدید لازمه:
    (venv) PS D:\sysapp\strategic> python manage.py makemigrations strategic
    (venv) PS D:\sysapp\strategic> python manage.py migrate

    ب) کنار هر راهبرد TOWS، یه دکمه‌ی مداد جدید هست — با کلیک روش، یه
    لیست چندانتخابی باز می‌شه که فقط موارد SWOT مرتبط با همون دسته رو
    نشون می‌ده (راهبرد SO فقط قوت‌ها و فرصت‌ها، ST فقط قوت‌ها و
    تهدیدها، و همین‌طور). بعد از انتخاب، کدهای موارد منتخب (مثل «S2،
    O1») کنار خود راهبرد نشون داده می‌شه.

۳۵. **آخرین حلقه‌ی زنجیره‌ی ردیابی:** هر هدف استراتژیک حالا می‌تونه بگه
    از کدوم راهبرد TOWS اومده. با این، زنجیره‌ی کامل شد:
    PESTEL/پورتر/7S/زنجیره‌ارزش → SWOT → TOWS → هدف استراتژیک.

    الف) یه فیلد رابطه‌ی جدید به مدل هدف استراتژیک اضافه شد. یه migration
    جدید لازمه:
    (venv) PS D:\sysapp\strategic> python manage.py makemigrations strategic
    (venv) PS D:\sysapp\strategic> python manage.py migrate

    ب) توی فرم ویرایش/افزودن هدف (نقشه استراتژیک)، یه فیلد جدید «راهبرد
    TOWS مبنا» اضافه شد — فقط راهبردهای همون کسب‌وکار رو نشون می‌ده.

    ج) توی صفحه‌ی SWOT، کنار هر راهبرد TOWS که به یه هدف وصل شده، یه
    برچسب «🎯 N هدف» ظاهر می‌شه — با هاور، کد و عنوان اون اهداف رو
    می‌بینید.

۳۶. ماژول جدید «ذی‌نفعان» — بعد از «کتابخانه مطالعات» توی منو اضافه شد.
    سازمانی/مشترک، بدون لاگین هم قابل مشاهده.

    الف) مدل جدید اضافه شد. یه migration جدید لازمه:
    (venv) PS D:\sysapp\strategic> python manage.py makemigrations strategic
    (venv) PS D:\sysapp\strategic> python manage.py migrate

    ب) داده‌ی واقعی («گزارش خلاصه ذینفعان»، ۱۷۷ ردیف پاک‌سازی‌شده از
    مخزن ذینفعان، طبق خود گزارش حاصل مشارکت ۱۵ واحد/مدیریت) رو
    بارگذاری کنید:
    (venv) PS D:\sysapp\strategic> python manage.py load_real_stakeholders

    ج) صفحه شامل: کارت‌های «مهم‌ترین ریسک‌ها» و «مهم‌ترین فرصت‌های»
    ذی‌نفعان (۶ مورد برتر هرکدوم)، جست‌وجو + فیلتر بر اساس واحد، و
    جدول کامل قابل ویرایش/حذف.

    د) کارت جدید هم زیر «کتابخانه مطالعات» توی صفحه‌ی خانه اضافه شد.

۳۷. ذی‌نفعان هم به زنجیره‌ی ردیابی SWOT وصل شدن — کنار PESTEL و پورتر،
    حالا هر فرصت/تهدید می‌تونه به یه ذی‌نفع خاص هم وصل بشه.

    الف) یه فیلد رابطه‌ی جدید به مدل SWOT اضافه شد. یه migration جدید
    لازمه:
    (venv) PS D:\sysapp\strategic> python manage.py makemigrations strategic
    (venv) PS D:\sysapp\strategic> python manage.py migrate

    ب) توی فرم ویرایش فرصت/تهدید، یه دراپ‌داون سوم «ذی‌نفع مرتبط» اضافه
    شد. توی صفحه‌ی ذی‌نفعان هم کنار اسم هر ذی‌نفع که به SWOT وصل شده، یه
    برچسب کوچیک نشون داده می‌شه.

۳۸. ۵ فیلد جدید آینده‌پژوهی به هر عامل PESTEL اضافه شد — پیش‌نیاز
    اثرات متقابل: میزان اثر، احتمال وقوع، عدم‌قطعیت، افق زمانی، روند
    تغییر. یه migration جدید لازمه:
    (venv) PS D:\sysapp\strategic> python manage.py makemigrations strategic
    (venv) PS D:\sysapp\strategic> python manage.py migrate

    الف) زیر هر ردیف، ۶ برچسب خیلی کوچیک (امتیاز اولویت + ۵ فیلد،
    آیکون/حرف‌محور برای کمترین اشغال فضا) نشون داده می‌شه.

    ب) یه کارت جدید «عوامل کلیدی» بالای صفحه اضافه شد — ۸ عامل با
    بالاترین امتیاز اثر×احتمال، رنگ‌بندی‌شده بر اساس شدت.

    ج) داده‌های قبلی PESTEL شما دست‌نخورده می‌مونن، فقط این ۵ فیلد با
    مقدار پیش‌فرض (۳، ۳، متوسط، میان‌مدت، ثابت) براشون پر می‌شه — باید
    دستی هرکدوم رو با مداد بازبینی و اصلاح کنید.

۳۹. ماژول جدید «تحلیل اثرات متقابل» (روش MICMAC) — بعد از PESTEL توی
    منو اضافه شد، سازمانی/مشترک، بدون لاگین هم قابل مشاهده.

    الف) مدل جدید اضافه شد. یه migration جدید لازمه:
    (venv) PS D:\sysapp\strategic> python manage.py makemigrations strategic
    (venv) PS D:\sysapp\strategic> python manage.py migrate

    ب) داده‌ی واقعی (از سند «ماتریس اثر متقابل» مدیریت برنامه‌ریزی و
    نظارت راهبردی) رو بارگذاری کنید:
    (venv) PS D:\sysapp\strategic> python manage.py load_real_cross_impact

    ج) صفحه یه تخته‌ی ۴ ناحیه‌ای نشون می‌ده (پیشران‌های کلیدی، متغیرهای
    دووجهی، دیده‌بانی، متغیرهای نتیجه) که هر عامل PESTEL بر اساس میزان
    اثرگذاری/وابستگی توش جای می‌گیره. هر عامل می‌تونه به یه عامل PESTEL
    هم وصل بشه (برای ردیابی).

۴۰. محاسبه‌ی خودکار روش MICMAC (ماتریس N×N واقعی) — یه دکمه‌ی جدید
    «ماتریس ورودی و محاسبه» بالای صفحه‌ی «اثرات متقابل» اضافه شد.

    الف) مدل جدید اضافه شد. یه migration جدید لازمه:
    (venv) PS D:\sysapp\strategic> python manage.py makemigrations strategic
    (venv) PS D:\sysapp\strategic> python manage.py migrate

    ب) توی صفحه‌ی جدید، برای هر جفت عامل، امتیاز اثرگذاری مستقیم (۰ تا
    ۳) وارد می‌کنید. سیستم خودش اثرگذاری/وابستگی هر عامل رو حساب کرده و
    ناحیه‌ی پیشنهادی رو نشون می‌ده (کنار ناحیه‌ی فعلی، برای مقایسه).

    ج) با دکمه‌ی «اعمال پیشنهادها روی تخته»، می‌تونید یک‌جا ناحیه‌ی همه‌ی
    عوامل رو با پیشنهاد محاسبه‌شده جایگزین کنید — ولی همچنان می‌تونید
    توی تخته‌ی اصلی هرکدوم رو دستی جابه‌جا/ویرایش کنید.

۴۱. ماژول جدید «سناریوهای راهبردی» — آخرین حلقه‌ی زنجیره‌ی آینده‌پژوهی
    (PESTEL → عوامل کلیدی → اثرات متقابل → سناریوها). بعد از «اثرات
    متقابل» توی منو اضافه شد، سازمانی/مشترک، بدون لاگین هم قابل مشاهده.

    الف) دو مدل جدید اضافه شد (محورهای سناریو + خود سناریوها). یه
    migration جدید لازمه:
    (venv) PS D:\sysapp\strategic> python manage.py makemigrations strategic
    (venv) PS D:\sysapp\strategic> python manage.py migrate

    ب) داده‌ی واقعی (۴ سناریوی کامل سایپا یدک) رو بارگذاری کنید:
    (venv) PS D:\sysapp\strategic> python manage.py load_real_scenarios

    ج) صفحه یه نمودار ۲×۲ (دقیقاً مثل سند اصلی) با ۴ سناریو نشون می‌ده،
    سناریوی «هوای مه‌آلود» به‌عنوان سناریوی مبنا/منتخب علامت‌گذاری شده.
    زیرش، روایت کامل هر سناریو به‌صورت آکاردئون (باز/بسته‌شونده) هست.

۴۲. سناریوها هم به زنجیره‌ی ردیابی SWOT وصل شدن — کنار PESTEL، پورتر و
    ذینفعان، حالا هر فرصت/تهدید می‌تونه به یه سناریوی خاص هم وصل بشه
    (مثلاً یه تهدید که فقط توی سناریوی «هوای مه‌آلود» معناداره).

    الف) یه فیلد رابطه‌ی جدید به مدل SWOT اضافه شد. یه migration جدید
    لازمه:
    (venv) PS D:\sysapp\strategic> python manage.py makemigrations strategic
    (venv) PS D:\sysapp\strategic> python manage.py migrate

    ب) توی فرم ویرایش فرصت/تهدید، یه دراپ‌داون چهارم «سناریوی مرتبط»
    اضافه شد. توی صفحه‌ی سناریوها هم کنار عنوان هر سناریو که به SWOT وصل
    شده، یه برچسب کوچیک نشون داده می‌شه.

۴۳. ماژول جدید «اهداف کلان و KPI شرکت» — سطح کل شرکت (نه یکی از ۵
    کسب‌وکار)، مستقیماً وصل به نقشه استراتژیک ۳ساله گروه سایپا. توی
    گروه «اجرا و پایش» منو، قبل از «نقشه استراتژیک» اضافه شد.

    الف) دو مدل جدید اضافه شد. یه migration جدید لازمه:
    (venv) PS D:\sysapp\strategic> python manage.py makemigrations strategic
    (venv) PS D:\sysapp\strategic> python manage.py migrate

    ب) داده‌ی واقعی (۲۲ هدف کلان + ۲۶ شاخص) رو بارگذاری کنید:
    (venv) PS D:\sysapp\strategic> python manage.py load_real_company_goals

    ج) صفحه شامل: اهداف کلان گروه‌بندی‌شده زیر عنوان هدف کلان گروه
    سایپا، و جدول شاخص‌ها (کد، حوزه Q/D/C/M، هدف/واقعی ۱۴۰۴ با نوار
    پیشرفت، هدف ۱۴۰۵، هدف پیشنهادی، اهداف مرتبط).

۴۴. ماژول جدید «اسناد و دستورالعمل‌ها» — اولین ماژولی که واقعاً فایل
    (PDF/Word/Excel) آپلود می‌کنه، نه فقط متن. بعد از «خانه»، قبل از
    «ارکان جهت‌ساز سازمان» توی منو اضافه شد. **فقط کاربران واردشده
    (login) می‌بینن.**

    الف) مدل جدید اضافه شد. یه migration جدید لازمه:
    (venv) PS D:\sysapp\strategic> python manage.py makemigrations strategic
    (venv) PS D:\sysapp\strategic> python manage.py migrate

    ب) **مهم — باید دستی به settings.py اضافه کنید** (فایل
    `config_settings_reference.py` رو برای مرجع کامل نگاه کنید):
    ```python
    MEDIA_URL = 'media/'
    MEDIA_ROOT = BASE_DIR / 'media'
    ```
    این تنظیم فقط می‌گه فایل‌ها کجا ذخیره بشن — **URL مستقیم عمومی
    نساختیم** (یعنی urls.py رو برای سرو مستقیم media/ تغییر ندادید)،
    چون خواستید دانلود فقط پشت لاگین باشه. دانلود از طریق یه ویوی
    محافظت‌شده (`/documents/download/<id>/`) انجام می‌شه.

    ج) یه پوشه‌ی خالی `media/` کنار `manage.py` بسازید (یا بذارید خودش
    موقع اولین آپلود ساخته بشه).

    د) توی صفحه: دو بخش «اسناد بالادستی» و «دستورالعمل‌ها»، هرکدوم با
    آیکون نوع فایل + دکمه‌ی دانلود. PDF توی تب جدید مرورگر باز می‌شه؛
    Word/Excel دانلود می‌شه (چون مرورگر نمی‌تونه inline نشونشون بده).
    آپلود/ویرایش فقط برای ادیتور و ادمین (دقیقاً مثل بقیه‌ی ماژول‌ها).

۴۵. به هر سند، «کد دستورالعمل/سند» و «تاریخ تصویب» (شمسی، وارد دستی
    شما هنگام آپلود — نه تاریخ ثبت خودکار سامانه) اضافه شد.

    الف) دو فیلد جدید. یه migration جدید لازمه:
    (venv) PS D:\sysapp\strategic> python manage.py makemigrations strategic
    (venv) PS D:\sysapp\strategic> python manage.py migrate

    ب) کد سند حالا کنار عنوانش نمایش داده می‌شه؛ تاریخ زیرش هم شمسی و
    همون «تاریخ تصویب»ی هست که خودتون وارد می‌کنید (نه تاریخ آپلود).
    اسناد قبلاً ثبت‌شده، این دو فیلد رو خالی دارن — با مداد ویرایش کنید.

۴۶. حالا هر هدف نقشه استراتژیک (هر ۱۷۸ هدف توی ۵ کسب‌وکار) می‌تونه چند
    شاخص KPI واقعی داشته باشه — نه فقط اون فیلد متنی قبلی. یه migration
    جدید لازمه:
    (venv) PS D:\sysapp\strategic> python manage.py makemigrations strategic
    (venv) PS D:\sysapp\strategic> python manage.py migrate

    الف) روی هر کارت هدف توی نقشه استراتژیک، یه دکمه‌ی جدید «N شاخص»
    اضافه شد — با کلیک، لیست شاخص‌های همون هدف (نام، هدف/واقعی، روند،
    وضعیت رنگی، مسئول) باز می‌شه، با امکان افزودن/ویرایش/حذف.

    ب) فیلد متنی قبلی «KPI» روی خود هدف دست‌نخورده می‌مونه (برای توضیح
    کلی)؛ این شاخص‌های جدید مکملشن، نه جایگزین.

    ج) چون همه‌ی اهداف قبلی تازه‌ان، فعلاً هیچ شاخصی ثبت نشده — باید
    دستی برای هرکدوم اضافه کنید.

۴۷. جدول شاخص‌های صفحه‌ی «اهداف کلان و KPI شرکت» بازطراحی شد — پیش‌نیاز
    وصل‌کردن این شاخص‌ها به کارت‌های نقشه استراتژیک:

    الف) «هدف پیشنهادی» کامل حذف شد؛ «آخرین وضعیت ۱۴۰۴» به «عملکرد
    ۱۴۰۴» تغییر نام داد؛ یه فیلد جدید «عملکرد ۱۴۰۵» اضافه شد؛ «درصد
    تحقق» (بر پایه‌ی هدف/عملکرد ۱۴۰۵) خودکار محاسبه و رنگی نمایش داده
    می‌شه.

    ب) یه migration جدید لازمه:
    (venv) PS D:\sysapp\strategic> python manage.py makemigrations strategic
    (venv) PS D:\sysapp\strategic> python manage.py migrate
    (venv) PS D:\sysapp\strategic> python manage.py load_real_company_goals

    ج) چون «عملکرد ۱۴۰۵» فیلد کاملاً جدیدیه، برای هر ۲۶ شاخص خالیه —
    باید دستی با مداد پر کنید تا «درصد تحقق» محاسبه بشه.

۴۸. رفع باگ: ذخیره نشدن ویرایش شاخص‌های «اهداف کلان و KPI شرکت» —
    فیلد «ترتیب نمایش» توی فرم ویرایش اصلاً input نداشت، پس فرم همیشه
    نامعتبر تشخیص داده می‌شد (بدون نمایش خطا). حالا درست شد.

    همچنین «درصد تحقق» از حالت خودکار به **ورودی دستی** تغییر کرد — یه
    فیلد جدید. یه migration جدید لازمه:
    (venv) PS D:\sysapp\strategic> python manage.py makemigrations strategic
    (venv) PS D:\sysapp\strategic> python manage.py migrate

۴۹. هر کارت نقشه استراتژیک حالا می‌تونه به شاخص‌های استراتژیک مشترک
    شرکت (I1-I26) هم وصل بشه — نه فقط شاخص‌های اختصاصی. یه migration
    جدید لازمه:
    (venv) PS D:\sysapp\strategic> python manage.py makemigrations strategic
    (venv) PS D:\sysapp\strategic> python manage.py migrate

    الف) توی فرم ویرایش هر هدف، یه بخش جدید «شاخص‌های استراتژیک مرتبط»
    اضافه شد — چک‌لیستی از همون ۲۶ شاخص، دستی انتخاب می‌کنید کدوم
    واقعاً به این هدف خاص مربوطه.

    ب) روی خود کارت، به‌جای دکمه‌ی متنی قبلی، حالا یه ردیف **دایره‌ی
    رنگی** می‌بینید — یکی برای هر شاخص (چه اختصاصی، چه مشترک). رنگ بر
    اساس درصد تحقق: زیر ۸۰٪ قرمز، ۸۰-۹۰٪ زرد، ۹۰٪+ سبز. هاور روی هر
    دایره، جزئیات کامل (نام/هدف/عملکرد/درصد) رو نشون می‌ده.

    ج) درصد تحقق شاخص‌های مشترک، از همون فیلد متنی «درصد تحقق» که توی
    صفحه‌ی «اهداف کلان و KPI شرکت» وارد می‌کنید خونده می‌شه — **حتماً
    باید دقیقاً به‌شکل «۸۵٪» (عدد + علامت درصد چسبیده) وارد بشه**، وگرنه
    سیستم نمی‌تونه عدد رو تشخیص بده و دایره خاکستری می‌مونه.

    د) مقدار شاخص‌های مشترک فقط از صفحه‌ی «اهداف کلان و KPI شرکت» قابل
    ویرایشه (نه از نقشه استراتژیک) — چون این شاخص‌ها بین چند کارت
    مشترکن.

۵۰. ورود/خروجی اکسل برای شاخص‌های کلیدی شرکت (KPI) — فقط برای مدیر
    سیستم (Superuser).

    الف) یه بسته‌ی پایتون جدید لازمه (توی requirements.txt اضافه شد):
    (venv) PS D:\sysapp\strategic> pip install openpyxl==3.1.5
    (یا: pip install -r requirements.txt)

    ب) بدون migration جدید — فقط تغییر views.py/urls.py/template.

    ج) توی صفحه‌ی «اهداف کلان و KPI شرکت»، بالای جدول شاخص‌ها، دو دکمه‌ی
    جدید («خروجی اکسل» و «ورود از اکسل») فقط برای مدیر سیستم دیده
    می‌شود. خروجی اکسل شامل همه‌ی فیلدهاست؛ ورود اکسل، ردیف‌ها را بر
    اساس ستون «کد» تطبیق می‌دهد (موجود → به‌روزرسانی، جدید → ساخته
    می‌شود). ستون «اهداف مرتبط» باید به‌شکل O1-O2-O3 وارد شود.

۵۱. نقشه ریسک هم به سناریوها وصل شد — هر ریسک می‌تونه بگه از تحقق کدوم
    سناریو مستقیماً میاد (مثلاً چندتا ریسک موجود، تبعات مستقیم سناریوی
    «هوای مه‌آلود»ان).

    یه فیلد رابطه‌ی جدید. یه migration جدید لازمه:
    (venv) PS D:\sysapp\strategic> python manage.py makemigrations strategic
    (venv) PS D:\sysapp\strategic> python manage.py migrate

    توی فرم ویرایش هر ریسک، فیلد جدید «سناریوی مرتبط» اضافه شد — دقیقاً
    کنار همون فیلد قبلی «هدف استراتژیک تهدیدشده».

۵۲. ارتباط ریسک↔سناریو حالا دوطرفه نمایش داده می‌شود — بدون migration،
    فقط تغییر views.py و ۲ قالب:

    الف) توی نقشه ریسک، کنار عنوان هر ریسکی که به سناریو وصل شده، یه
    برچسب کوچیک نارنجی (اسم سناریو) نشون داده می‌شه.

    ب) توی صفحه‌ی سناریوها، کنار عنوان هر سناریو، اگه ریسکی بهش وصل
    باشه، یه برچسب («⚠ N») نشون می‌ده — با هاور، اسم ریسک‌ها رو می‌بینید.

۵۳. «سناریوی مرتبط» روی هر ریسک، از تک‌انتخابی به چندانتخابی تغییر
    کرد — الان هر ریسک می‌تونه به چند سناریو هم‌زمان وصل بشه.

    ⚠️ هشدار مهم: چون نوع فیلد عوض شده (نه فقط گسترش)، migration این
    تغییر، **ارتباط سناریویی که قبلاً روی هر ریسک تست/ثبت کرده بودید
    رو پاک می‌کنه** (چون داره از یه نوع رابطه به نوع دیگه تبدیل می‌شه).
    بعد از migrate، باید هر ریسکی که قبلاً به یه سناریو وصل کرده بودید
    رو دوباره دستی وصل کنید (این‌بار با چک‌باکس، نه دراپ‌داون).

    (venv) PS D:\sysapp\strategic> python manage.py makemigrations strategic
    (venv) PS D:\sysapp\strategic> python manage.py migrate

    توی فرم ویرایش ریسک، فیلد «سناریوهای مرتبط» الان یه لیست چک‌باکسی
    (کادر با اسکرول، مثل بقیه‌ی فیلدهای چندانتخابی سایت) هست.

۵۴. ورود/خروجی اکسل برای ماتریس اثر متقابل — فقط برای مدیر سیستم.
    بدون migration، فقط تغییر views.py/urls.py/template.

    بالای صفحه‌ی «ماتریس ورودی و محاسبه»، دو دکمه‌ی جدید («خروجی اکسل»
    و «ورود از اکسل») فقط برای مدیر سیستم دیده می‌شه. خروجی، یه ماتریس
    N×N کامل با اسم عامل‌ها روی سطر/ستون اوله؛ ورود اکسل هم ردیف/ستون‌ها
    رو بر اساس **متن دقیق عامل** تطبیق می‌ده (نه شماره یا کد) — پس اگه
    متن یه عامل رو عوض کرده باشید، سطر/ستونش نادیده گرفته می‌شه.

۵۵. داده‌ی واقعی SWOT/TOWS برای ۳ کسب‌وکار (آپشن، بسته خدمت، خدمات
    تعمیراتی) از فایل‌های اکسل واقعی بارگذاری شد.

    الف) ضمن بررسی، متوجه شدیم بعضی راهبردهای TOWS طولانی‌تر از حد
    مجاز فعلی (۳۰۰ نویسه) بودن — فیلد «متن راهبرد» به بدون‌محدودیت
    تغییر کرد. یه migration جدید لازمه:
    (venv) PS D:\sysapp\strategic> python manage.py makemigrations strategic
    (venv) PS D:\sysapp\strategic> python manage.py migrate

    ب) داده‌ی واقعی رو بارگذاری کنید (فقط SWOT/TOWS همین ۳ کسب‌وکار رو
    پاک و جایگزین می‌کند؛ به بقیه‌ی کسب‌وکارها کاری ندارد):
    (venv) PS D:\sysapp\strategic> python manage.py load_real_swot_3units

    ج) هر راهبرد TOWS، در صورتی که متن اصلی‌اش کد منبع (مثل «S1, S3,
    O3») داشت، خودکار به همون موارد SWOT وصل شد — قابل بازبینی و
    اصلاح دستی از خود صفحه‌ی SWOT.

۵۶. ترتیب و عنوان ۱۳ عامل ماتریس اثر متقابل، طبق ترتیب نهایی تأییدشده
    بازچینی شد — هم توی «ماتریس ورودی و محاسبه»، هم توی جدول نتیجه،
    هم توی خروجی/ورودی اکسل. (تخته‌ی ۴ ناحیه‌ای دست‌نخورده موند، چون
    اونجا هنوز بر اساس ناحیه گروه‌بندی می‌شه.)

    بدون migration، فقط تغییر views.py و یه دستور جدید:
    (venv) PS D:\sysapp\strategic> python manage.py reorder_cross_impact_factors

    این دستور فقط ترتیب و عنوان (چند کلمه اصلاح‌شده مثل «قطعات
    غیراصلی» به‌جای «قطعات تقلبی») رو به‌روز می‌کنه؛ خود داده‌ها (ناحیه،
    اتصال به PESTEL، امتیازهای ماتریس ورودی) دست‌نخورده می‌مونن.

۵۷. مخزن ذینفعان به داده‌ی کامل (۱۸ ستون، از فایل «مخزن ذینفعان
    تحلیلی») ارتقا یافت. یه migration جدید لازمه:
    (venv) PS D:\sysapp\strategic> python manage.py makemigrations strategic
    (venv) PS D:\sysapp\strategic> python manage.py migrate

    الف) داده‌ی واقعی و کامل رو بارگذاری کنید (این کار مخزن قبلی رو پاک
    و با نسخه‌ی کامل‌تر — ۲۰۷ ردیف — جایگزین می‌کند):
    (venv) PS D:\sysapp\strategic> python manage.py load_real_stakeholders_full

    ب) جدول اصلی صفحه دست‌نخورده موند (همون ۶ ستون قبلی: واحد، ذینفع،
    نیاز/انتظار، ریسک، فرصت، اقدام). فیلدهای جدید (احتمال وقوع، شدت،
    تشخیص، امتیاز اهمیت/تأثیر فرصت، حوزه، نوع نیاز/انتظار) فقط توی فرم
    ویرایش هر ذینفع اضافه شدن.

    ج) کارت‌های بالای صفحه («ریسک‌های بحرانی» و «فرصت‌های کلیدی») دیگه
    «۶ تای برتر» نیستن — الان همه‌ی مواردی که عدد ریسکشون بالای ۴۰ یا
    عدد فرصتشون بالای ۶۴ باشه رو نشون می‌دن (تعدادشون متغیره).

    د) دکمه‌ی «خروجی اکسل کامل» (همه‌ی ۱۸ ستون) و «ورود از اکسل» بالای
    صفحه اضافه شد. **توجه: ورود از اکسل، کل مخزن فعلی رو پاک و
    جایگزین می‌کند** (نه افزودن) — قبل از استفاده، یه پیام تأیید نشون
    داده می‌شه.

۵۸. ماژول جدید «الزامات قانونی» — بانک کامل الزامات قانونی و سازمانی
    سایپا یدک. توی منو، درست قبل از «تحلیل ذینفعان» اضافه شد (بدون
    لاگین هم قابل مشاهده). دقیقاً همون امکانات ذینفعان: جست‌وجوی زنده
    روی ۶ ستون اصلی، فیلتر واحد، و خروجی/ورودی اکسل کامل (۱۱ ستون).

    الف) مدل جدید اضافه شد. یه migration جدید لازمه:
    (venv) PS D:\sysapp\strategic> python manage.py makemigrations strategic
    (venv) PS D:\sysapp\strategic> python manage.py migrate

    ب) داده‌ی واقعی رو بارگذاری کنید (۱۳۴ الزام واقعی):
    (venv) PS D:\sysapp\strategic> python manage.py load_real_legal_requirements

    ج) جدول اصلی فقط ۶ ستون کلیدی نشون می‌ده (عنوان، مأخذ، محل کاربرد،
    ریسک، فرصت، واحد/معاونت)؛ بقیه‌ی فیلدها (قانونی/سازمانی، تاریخ
    ویرایش، مستندات مرتبط، توضیحات) فقط توی فرم ویرایش و اکسل هستن.

    د) **توجه: «ورود از اکسل» کل بانک فعلی رو پاک و جایگزین می‌کند**
    (نه افزودن) — دقیقاً مثل ماژول ذینفعان.

۵۹. ماژول جدید «بانک عوامل محیطی» — پایگاه شناسایی و امتیازدهی ۶۷ عامل
    محیطی (ترکیب PESTEL و پنج نیروی رقابتی). توی گروه «تحلیل محیط
    بیرونی»، درست قبل از PESTEL اضافه شد (بدون لاگین هم قابل مشاهده).

    الف) مدل جدید اضافه شد. یه migration جدید لازمه:
    (venv) PS D:\sysapp\strategic> python manage.py makemigrations strategic
    (venv) PS D:\sysapp\strategic> python manage.py migrate

    ب) داده‌ی واقعی رو بارگذاری کنید (۶۷ عامل واقعی):
    (venv) PS D:\sysapp\strategic> python manage.py load_real_environmental_factors

    ج) جدول بر اساس میانگین امتیاز مرتب و رنگی نمایش داده می‌شه (۸+
    قرمز، ۶ تا ۸ نارنجی، زیر ۶ سبز). روی هر ردیف کلیک کنید تا توضیح
    تفصیلی و راهنمای امتیازدهی باز بشه.

    د) اگه متن یه عامل با یکی از ۱۳ عامل «ماتریس اثر متقابل» تطابق
    داشته باشه (یا زیرمجموعه‌ی متنی هم باشن)، خودکار برچسب «🔗 در
    ماتریس اثر متقابل» نشون داده می‌شه — این تطبیق زنده محاسبه می‌شه
    (بدون نیاز به ذخیره‌ی دستی رابطه).

    ه) جست‌وجوی زنده، خروجی/ورودی اکسل کامل — دقیقاً مثل ذینفعان و
    الزامات قانونی (ورود اکسل هم کل بانک قبلی رو جایگزین می‌کند).

۵۶. هر پروژه‌ی تحول حالا می‌تواند بگوید از کجا آمده — سه فیلد اختیاری
    جدید: «شکاف شاخص مبنا» (وصل به یک شاخص کلان شرکت)، «راهبرد TOWS
    مبنا» (فقط از همان کسب‌وکار پروژه)، و «ریسک مبنا». هر سه اختیاری‌اند
    و می‌توان هر ۱، ۲ یا هر ۳ را با هم انتخاب کرد. یه migration جدید
    لازمه:
    (venv) PS D:\sysapp\strategic> python manage.py makemigrations strategic
    (venv) PS D:\sysapp\strategic> python manage.py migrate

    روی خود کارت پروژه، اگه این فیلدها پر شده باشن، یه ردیف «منشأ:»
    با برچسب‌های رنگی متناظر نشون داده می‌شه.

۵۷. سه فیلد منشأ پروژه (شاخص/راهبرد TOWS/ریسک) از تک‌انتخابی به
    چندانتخابی تبدیل شدن — الان هر پروژه می‌تونه به چند شاخص/راهبرد/
    ریسک هم‌زمان وصل بشه (فرم ویرایش، چک‌باکس‌های تیک‌پذیر).

    ⚠️ اگه توی همون فاصله‌ی کوتاه بعد از فیچر قبلی (منشأ پروژه)، برای
    هر پروژه‌ای این فیلدها رو پر کرده بودید، migration این تبدیل ممکنه
    اون مقدار رو پاک کنه — باید دوباره دستی وصلشون کنید.

    (venv) PS D:\sysapp\strategic> python manage.py makemigrations strategic
    (venv) PS D:\sysapp\strategic> python manage.py migrate

    همچنین هاور روی برچسب‌ها غنی‌تر شد:
    - هاور روی کد هدف: حالا اگه اون هدف از یه راهبرد TOWS اومده باشه،
      اون زنجیره رو هم نشون می‌ده.
    - هاور روی برچسب «راهبرد»: نوع دقیق (SO/ST/WO/WT) رو هم می‌گه.
    - هاور روی برچسب «شکاف شاخص»: حوزه + هدف/عملکرد سال جاری رو نشون می‌ده.
    - هاور روی برچسب «ریسک»: دسته + احتمال/اثر فعلی + مسئول رو نشون می‌ده.

۵۷. سه فیلد منشأ پروژه (شکاف شاخص، راهبرد TOWS، ریسک) از تک‌انتخابی به
    چندانتخابی تبدیل شد — هر پروژه می‌تونه هم‌زمان به چند شاخص/راهبرد/
    ریسک وصل بشه. هاور روی هر برچسب (هم اهداف، هم این سه‌تا) هم غنی‌تر
    شد — جزئیات کامل (نوع، حوزه، هدف/عملکرد، مسئول و...) رو نشون می‌ده.

    ⚠️ چون نوع فیلد عوض شده، migration این تغییر ممکنه ارتباطاتی که
    قبلاً (اگه) تست کرده بودید رو پاک کنه — باید دوباره دستی وصل کنید.

    (venv) PS D:\sysapp\strategic> python manage.py makemigrations strategic
    (venv) PS D:\sysapp\strategic> python manage.py migrate

۵۸. خروجی اکسل کامل نقشه استراتژیک — ۴ شیت برای هر کسب‌وکار (دکمه‌ی
    جدید «خروجی اکسل کامل» کنار بقیه‌ی دکمه‌های خروجی):
    ۱) داشبورد استراتژیک (خلاصه‌ی هر منظر + شمارش کلی)
    ۲) نقشه استراتژیک (هر هدف یک ردیف، با KPI/TOWS/SWOT/ارتباطات/منشأ
       تحلیل کامل داخل هر سلول)
    ۳) Strategic Traceability (زنجیره‌ی کامل هدف ← TOWS ← SWOT ← منشأ
       تحلیل ← KPI)
    ۴) همه‌ی KPIهای مرتبط با این کسب‌وکار (مشترک شرکت + اختصاصی هر هدف)

    ضمناً یه فیلد بی‌استفاده و تکراری (`linked_company_kpis`) که از یه
    ویرایش قبلی باقی مونده بود، حذف شد. یه migration جدید لازمه:
    (venv) PS D:\sysapp\strategic> python manage.py makemigrations strategic
    (venv) PS D:\sysapp\strategic> python manage.py migrate

۵۹. ماتریس اثر متقابل حالا علاوه بر PESTEL، به «نیروی پورتر مرتبط»
    هم می‌تونه وصل بشه (هر عامل، اختیاری، می‌تونه هم‌زمان به هردو یا
    فقط یکی وصل باشه). یه migration جدید لازمه:
    (venv) PS D:\sysapp\strategic> python manage.py makemigrations strategic
    (venv) PS D:\sysapp\strategic> python manage.py migrate

    مثل PESTEL، این ارتباط هم دوطرفه نمایش داده می‌شه: روی کارت اثر
    متقابل یه آیکون جدید (شمشیر/شطرنج)، و روی صفحه‌ی پورتر یه برچسب
    نارنجی «استفاده در اثرات متقابل».

۶۰. داده‌ی واقعی SWOT/TOWS کسب‌وکار «گارانتی خودرو» از فایل اکسل واقعی
    بارگذاری شد — ۶ قوت، ۷ ضعف، ۶ فرصت، ۹ تهدید، ۲۳ راهبرد TOWS (۳
    SO، ۹ ST، ۵ WO، ۶ WT). بدون migration، فقط یه فایل جدید:

    (venv) PS D:\sysapp\strategic> python manage.py load_real_swot_warranty

    درست مثل ۳ کسب‌وکار قبلی، هر راهبرد TOWS که متن اصلی‌اش کد منبع
    داشت، خودکار به همون موارد SWOT وصل شد.

۶۱. دو دستور جدید و ایمن (هیچ‌کدوم چیزی رو حذف نمی‌کنه، فقط آپدیت):

    الف) اتصال راهبردهای TOWS «بازرگانی قطعات» به موارد SWOT واقعی (این
    تنها کسب‌وکاری بود که این اتصال رو نداشت؛ کدهای منبع مثل «S3+O1+O2»
    از قبل داخل متن بودن، فقط لینک نشده بودن):
    (venv) PS D:\sysapp\strategic> python manage.py link_swot_parts_trading

    ب) تنظیم «وزن اهمیت» (۱ تا ۵) همه‌ی موارد SWOT هر ۵ کسب‌وکار — بر
    اساس تحلیل واقعی محتوای هر مورد (نه پیش‌فرض ثابت ۳). این عدد مستقیم
    روی محاسبه‌ی موقعیت راهبردی (SO/ST/WO/WT) هر کسب‌وکار اثر می‌ذاره:
    (venv) PS D:\sysapp\strategic> python manage.py set_swot_weights_all_bu

    ⚠️ توصیه: این وزن‌ها بر اساس تحلیل متن انجام شده، نه ارزیابی
    میدانی تیم شما — بعد از اجرا حتماً یه‌بار روی هر کسب‌وکار مرور کنید
    و هرجا لازم بود دستی اصلاح کنید.

چیزی که این‌بار عوض شده
--------------------------
- چهار ماژول (کتابخانه مطالعات، نقشه راه ابتکارات، نقشه ریسک، SWOT) همه
  الان مدل واقعی Django دارن — افزودن/حذف از فرم واقعاً توی دیتابیس ذخیره
  می‌شه و از پنل ادمین (/admin/) هم قابل مدیریتن.
- سیستم ورود کاربری با auth داخلی خود جنگو اضافه شد — بدون لاگین هیچ
  صفحه‌ای در دسترس نیست. برای خروج، دکمه «خروج» بالای صفحه هست.
- نقشه راه ابتکارات حالا از تاریخ‌های میلادی واقعی (start_date/end_date)
  استفاده می‌کنه که با فیلد تاریخ معمولی HTML وارد می‌شن (نه شمسی) — چون
  گانت چارت به تاریخ میلادی نیاز داره؛ اگه بخواید ورودی شمسی باشه و پشت
  صحنه به میلادی تبدیل بشه، بگید تا اضافه کنم.
- نقشه استراتژیک (BSC) و هوش رقابتی/PESTEL هنوز محتوای ثابت دارن — این‌ها
  رو در قدم بعدی می‌تونیم به مدل وصل کنیم.

اگه بازم متن فارسی خراب دیدید
--------------------------------
یعنی جایی توی مسیر کپی از یه ابزار غیر بایت-به‌بایت (مثل ترمینال یا Notepad
ساده) استفاده شده. راه مطمئن همیشه: کپی/پیست مستقیم فایل از طریق File
Explorer یا استخراج زیپ — هیچ‌وقت محتوای فارسی رو دستی توی PowerShell
تایپ/پیست نکنید.

۶۲. بازطراحی کامل صفحه‌ی «ارکان جهت‌ساز سازمان» (چشم‌انداز/مأموریت/ارزش‌ها/
    خط‌مشی) مطابق طرح جدیدی که کاربر فرستاد:

    الف) عنوان بخش ارزش‌ها به «نظام ارزشیابی سایپا یدک (مشکات نور)» تغییر کرد.

    ب) چرخ ارزش‌ها رنگی و آیکون‌دار شد (هر ارزش یک رنگ و آیکون اختصاصی).
    روی هر ارزش (از جمله «مشتری‌مداری» در مرکز) کلیک کنید تا یک پنل جزئیات
    زیر چرخ باز شود: تعریف، رفتارهای مورد انتظار، و نمونه‌های اجرایی در
    سازمان. کلیک دوباره روی همون ارزش، پنل رو می‌بنده. این پنل واقعی و
    قابل‌ویرایش است (نه فقط ظاهری) — سه مدل جدید دیتابیس اضافه شد: آیکون،
    رنگ، تعریف، رفتارهای مورد انتظار، نمونه‌های اجرایی، همه روی مدل
    OrgValue موجود. یه migration جدید لازمه:
    (venv) PS D:\sysapp\strategic> python manage.py makemigrations strategic
    (venv) PS D:\sysapp\strategic> python manage.py migrate

    ج) محتوای واقعی (آیکون/رنگ/تعریف/رفتار/نمونه) برای هر ۸ ارزش (۷
    پیرامونی + مشتری‌مداری در مرکز) نوشته و توی دستور بارگذاری قرار گرفت.
    این دستور را اجرا کنید تا این محتوا ثبت شود:
    (venv) PS D:\sysapp\strategic> python manage.py load_org_identity

    نکته‌ی مهم امنیتی: این دستور با متن هر ارزش تطبیق پیدا می‌کند، نه فقط
    آپدیت کورکورانه — اگر همان ارزش با همان متن (با چشم‌پوشی از نیم‌فاصله)
    از قبل در دیتابیس شما بود، فقط فیلدهای جدید (آیکون/رنگ/تعریف/رفتار/
    نمونه) به آن اضافه می‌شود و متن قبلی‌اش دست‌نخورده می‌ماند — چیزی حذف یا
    بازنویسی کامل نمی‌شود. (این دستور را من روی یک کپی آزمایشی از بکاپ
    دیروزی شما اجرا و تأیید کردم که هیچ ارزش تکراری ساخته نمی‌شود.)

    د) مدیر سیستم می‌تواند از همان فرم ویرایش ارزش قبلی (دکمه‌ی مداد روی هر
    حباب)، حالا آیکون، رنگ، تعریف، رفتارهای مورد انتظار و نمونه‌های اجرایی
    را هم ویرایش کند — از یک لیست ثابت آیکون/رنگ انتخاب می‌شود (نه ورودی
    آزاد) تا ظاهر چرخ همیشه یکدست بماند.

    ه) بخش خط‌مشی سیستم‌های مدیریتی از حالت گرید ثابت به یک کاروسل افقی
    قابل‌اسکرول (با دو دکمه‌ی فلش) تبدیل شد — بدون تغییر در داده یا مدل.

    و) یک بخش جدید «استانداردهای سیستم مدیریت» اضافه شد — ۵ کارت ISO
    (9001/10002/10004/10015/14001) با عنوان و دامنه‌ی کاربرد، بدون تاریخ
    ممیزی (طبق درخواست صریح کاربر). این بخش کاملاً نمایشی و ثابت است، به
    مدل یا دیتابیسی وصل نیست — نیازی به migration ندارد.

    ز) یک بخش جدید «ارتباط ارزش‌ها با اهداف و نتایج سازمان» اضافه شد که
    زنجیره‌ی «ارزش سازمانی ← هدف استراتژیک ← شاخص کلیدی (KPI) ← پروژه‌های
    مرتبط» را با چند ردیف نمونه نشان می‌دهد. این بخش هم کاملاً نمایشی است
    و به داده‌ی واقعی نقشه استراتژیک وصل نیست (طبق تصمیم صریح کاربر) — یک
    برچسب راهنما هم بالای جدول این را روشن می‌کند.

    ح) بالای صفحه چند بج وضعیت («فعال»، تصویب‌کننده، تاریخ تصویب) اضافه شد
    که از همان اطلاعات موجود مدل OrgIdentity (امضاکننده/سمت/تاریخ) خوانده
    می‌شود — چیز جدیدی به دیتابیس اضافه نشده.

    فایل‌هایی که باید overwrite شوند:
    - strategic/models.py
    - strategic/views.py
    - strategic/migrations/0045_orgvalue_color_orgvalue_definition_orgvalue_examples_and_more.py (فایل جدید)
    - strategic/management/commands/load_org_identity.py
    - templates/strategic/org_identity.html

    ترتیب دقیق دستورها بعد از overwrite کردن فایل‌ها:
    (venv) PS D:\sysapp\strategic> python manage.py makemigrations strategic
    (venv) PS D:\sysapp\strategic> python manage.py migrate
    (venv) PS D:\sysapp\strategic> python manage.py load_org_identity

۶۳. اصلاح ظاهر چرخ ارزش‌ها و کارت‌های ISO مطابق دو تصویر نمونه‌ای که کاربر فرستاد
    (بدون تغییر در مدل یا دیتابیس — فقط views.py و تمپلیت، بدون migration):

    الف) چرخ ارزش‌ها از چیدمان حباب‌های دایره‌ای قبلی، به یک پای‌چارت واقعی
    (SVG donut) تبدیل شد — دقیقاً مثل تصویر مرجع: ۷ قطعه‌ی رنگی مجزا دور
    مرکز سفید «مشتری‌مداری»، با آیکون کوچک سفید و برچسب هر ارزش داخل قطعه‌ی
    خودش، و ۷ آیکون کوچک اتصال‌دهنده (چرخشی) روی مرز بین قطعات، دقیقاً مثل
    نمونه. هندسه‌ی دقیق مسیرهای SVG (زاویه، شعاع داخلی/خارجی هر قطعه) در
    یک تابع پایتون جدید (`_wheel_geometry` در views.py) محاسبه می‌شود، نه
    با تقریب CSS — پس همیشه دقیق و متقارن خواهد بود، حتی اگر بعداً تعداد
    ارزش‌ها تغییر کند.

    ب) کارت‌های استاندارد ISO از حالت عمودی/متقارن قبلی، به ردیف افقی
    مطابق تصویر دوم تبدیل شد: آیکون کنار کد استاندارد (نه بالای آن)، خط
    جداکننده، و سپس «دامنه:» پایین کارت — بدون بج وضعیت (طبق تصمیم قبلی
    کاربر برای عدم نمایش تاریخ/وضعیت).

    فایل‌هایی که باید overwrite شوند:
    - strategic/views.py
    - templates/strategic/org_identity.html

    نیازی به migrate یا هیچ دستور دیگری نیست — فقط فایل‌ها را جایگزین و
    سرور را ری‌استارت کنید.

۶۴. سه اصلاح درخواستی کاربر روی صفحه‌ی «ارکان جهت‌ساز سازمان» و کل سایت
    (بدون تغییر مدل یا migration):

    الف) لوگوی واقعی سایپا یدک (فایلی که کاربر فرستاد) جایگزین لوگوی ساده‌ی
    قبلی شد — در تمام ۸ جایی که لوگو استفاده می‌شود (سایدبار، صفحه‌ی ورود،
    ارکان جهت‌ساز، خروجی‌های چاپی SWOT و نقشه استراتژیک). پس‌زمینه‌ی سفید
    لوگوی اصلی حذف و شفاف شد تا روی پس‌زمینه‌ی سرمه‌ای سایدبار درست دیده
    شود. دو نسخه ساخته شد:
    - `static/images/logo-mark.png` — فقط نشان (بدون متن)، برای جاهای
      کوچک مربعی‌شکل مثل سایدبار و صفحه‌ی ورود
    - `static/images/logo.png` — لوگوی کامل (نشان + متن سایپا یدک)، برای
      هدر بزرگ‌تر ارکان جهت‌ساز و خروجی‌های چاپی

    ب) پنل جزئیاتی که با کلیک روی هر ارزش زیر چرخ باز می‌شود، دیگر متن
    تعریف/رفتار/نمونه‌ی تولیدشده نشان نمی‌دهد — به‌جایش همان ۷ بند واقعی
    خط‌مشی سیستم‌های مدیریتی (همونی که پایین‌تر در صفحه و در پوستر رسمی
    هست) نمایش داده می‌شود. با کلیک روی هر ارزش، فقط آیکون/عنوان بالای
    پنل عوض می‌شود؛ خود لیست ۷ بندی برای همه‌ی ارزش‌ها یکسان و ثابت است.

    ج) بخش «ارتباط ارزش‌ها با اهداف و نتایج سازمان» از ۳ ردیف نمونه به
    فقط ۱ ردیف کاهش یافت (طبق درخواست صریح کاربر که دو ردیف اضافی بودند).

    فایل‌هایی که باید overwrite شوند:
    - strategic/views.py
    - templates/strategic/base.html
    - templates/strategic/login.html
    - templates/strategic/org_identity.html
    - templates/strategic/swot.html
    - templates/strategic/swot_print.html
    - templates/strategic/stratmap.html
    - templates/strategic/stratmap_print.html
    - templates/strategic/stratmap_print_full.html
    - static/images/logo.png (جدید)
    - static/images/logo-mark.png (جدید)

    فایل قدیمی static/images/logo.svg دیگر توسط هیچ تمپلیتی استفاده
    نمی‌شود؛ می‌توانید نگهش دارید یا حذف کنید، فرقی نمی‌کند.

    نیازی به migrate یا هیچ دستور دیگری نیست — فقط فایل‌ها را جایگزین و
    سرور را ری‌استارت کنید.

۶۵. رفع دو اشکالی که کاربر بعد از تست بند ۶۴ گزارش داد:

    الف) لوگو هنوز دیده نمی‌شد چون سایت با Waitress + WhiteNoise اجرا
    می‌شود و فایل‌های استاتیک باید جمع‌آوری (collect) شوند؛ فقط قرار دادن
    فایل جدید در static/images/ کافی نیست. این نکته در بند ۶۴ جا افتاده
    بود. **بعد از جایگزینی هر فایل استاتیک (عکس/css/js)، همیشه این دستور
    را هم اجرا کنید:**
    python manage.py collectstatic --noinput

    ب) پنل جزئیاتی که با کلیک روی هر ارزش باز می‌شود، به‌جای نمایش «همه‌ی
    ۷ بند خط‌مشی» (که در بند ۶۴ اشتباه پیاده‌سازی شده بود)، حالا فقط
    **یک بند مرتبط** با همان ارزش را نشان می‌دهد. یک فیلد جدید و
    قابل‌ویرایش به مدل OrgValue اضافه شد: `related_policy` (ارتباط به یکی
    از ۷ بند QualityPolicyPoint). یه migration جدید لازمه:
    python manage.py makemigrations strategic
    python manage.py migrate

    نگاشت اولیه (پیش‌فرض) بر اساس ترتیب انجام شده — ارزش ۱‌ام به بند ۱،
    ارزش ۲‌ام به بند ۲ و... تا ارزش ۷‌ام به بند ۷؛ برای «مشتری‌مداری» در
    مرکز، بند ۵ انتخاب شد (چون صراحتاً از «مشتری‌مداری» نام می‌برد). این
    نگاشت را دستور زیر ثبت می‌کند:
    python manage.py load_org_identity

    ⚠️ این فقط یک پیش‌فرض شروع است، نه لزوماً نگاشت درست از نظر محتوا.
    مدیر سیستم می‌تواند از همان فرم ویرایش هر ارزش (دکمه‌ی مداد روی هر
    قطعه‌ی چرخ)، یک دراپ‌داون جدید «بند خط‌مشی مرتبط» می‌بیند و می‌تواند
    با یک انتخاب ساده، جفت درست را برای هر ارزش تنظیم کند — بدون نیاز به
    من یا دپلوی دوباره.

    فایل‌هایی که باید overwrite شوند:
    - strategic/models.py
    - strategic/views.py
    - strategic/migrations/0046_orgvalue_related_policy.py (فایل جدید)
    - strategic/management/commands/load_org_identity.py
    - templates/strategic/org_identity.html
    - static/images/logo.png
    - static/images/logo-mark.png

    ترتیب دقیق دستورها بعد از overwrite کردن فایل‌ها:
    python manage.py makemigrations strategic
    python manage.py migrate
    python manage.py load_org_identity
    python manage.py collectstatic --noinput

۶۶. دو اصلاح دیگر که کاربر بعد از تست بند ۶۵ گزارش داد:

    الف) بخش «ارتباط ارزش‌ها با اهداف و نتایج سازمان» دوباره به‌اشتباه ۳
    ردیف نمونه نشون می‌داد (به‌خاطر یه اشتباه sync در بند قبلی، نسخه‌ی
    قدیمی دوباره جایگزین شده بود). الان قطعی و همیشگی به ۱ ردیف نمونه
    برگشت.

    ب) پیام مدیریت ارشد («مدیریت ارشد سایپایدک، اجرای این خط‌مشی را در
    تمامی سطوح سازمان و شبکه خدمات پس از فروش الزامی دانسته و بهبود
    مستمر آن را تضمین می‌نماید.») که در پوستر رسمی اولیه بود ولی جا افتاده
    بود، اضافه شد — دقیقاً بالای نام مدیرعامل (رحیم علیشاهی) در پایین
    صفحه نمایش داده می‌شود. یک فیلد جدید و قابل‌ویرایش به مدل OrgIdentity
    اضافه شد: `management_statement`. یه migration جدید لازمه:
    python manage.py makemigrations strategic
    python manage.py migrate

    این پیام از دکمه‌ی مداد روی کارت «چشم‌انداز» (همون مودال ویرایش
    چشم‌انداز/مأموریت) هم قابل ویرایش است.

    فایل‌هایی که باید overwrite شوند:
    - strategic/models.py
    - strategic/views.py
    - strategic/migrations/0047_orgidentity_management_statement.py (فایل جدید)
    - strategic/management/commands/load_org_identity.py
    - templates/strategic/org_identity.html

    ترتیب دقیق دستورها بعد از overwrite کردن فایل‌ها:
    python manage.py makemigrations strategic
    python manage.py migrate
    python manage.py load_org_identity

۶۷. رفع باگ لرزش/جابه‌جایی دایره‌ی مرکز چرخ («مشتری‌مداری») هنگام hover
    ماوس. علتش این بود: دایره‌ی مرکز با تکنیک margin منفی وسط‌چین شده
    (نه transform)، ولی افکت hover قبلی یک `transform:translate(-50%,-50%)`
    اضافه هم بهش اعمال می‌کرد — یعنی هر بار موس روی آن می‌رفت، یک‌بار
    دیگر هم به اندازه‌ی نصف عرض/ارتفاعش (۷۵ پیکسل) جابه‌جا می‌شد. فقط
    CSS اصلاح شد (`transform:scale(1.04)` به‌تنهایی)، بدون تغییر مدل یا
    migration.

    فایل‌هایی که باید overwrite شود:
    - templates/strategic/org_identity.html

    نیازی به هیچ دستوری نیست — فقط فایل را جایگزین و سرور را ری‌استارت
    کنید (برای مرورگر هم یک بار Ctrl+F5 بزنید تا کش CSS پاک شود).

۶۸. دو اصلاح دیگر روی چرخ ارزش‌ها که کاربر با مقایسه‌ی مستقیم با پوستر
    رسمی گزارش داد (بدون migration جدید، چون فیلدها از قبل بودند):

    الف) ترتیب ۷ ارزش پیرامونی چرخ اشتباه بود (با «رقابت سالم» شروع
    می‌شد). حالا دقیقاً مطابق پوستر رسمی سایپا یدک بازچینی شد — شروع از
    بالای چرخ، در جهت عقربه‌های ساعت:
    شفافیت و اعتمادآفرینی ← کرامت انسانی ← اطلاع‌رسانی ← توان‌افزایی ←
    نوآوری بازار محور ← وحدت و کارتیمی ← رقابت سالم

    چون نگاشت «بند خط‌مشی مرتبط» (بند ۶۵) هم بر همین ترتیب مبتنی بود،
    خودکار درست شد: شفافیت ← بند ۱، کرامت انسانی ← بند ۲، ... رقابت سالم
    ← بند ۷.

    ب) «مشتری‌مداری» (مرکز چرخ) دیگر به هیچ بند خط‌مشی‌ای وصل نیست —
    چون خودش هسته‌ی مرکزی همه‌ی ارزش‌هاست، نه معادل یک بند خاص. با کلیک
    روی مرکز، پنل فقط آیکون و نام «مشتری‌مداری» را نشان می‌دهد، بدون هیچ
    توضیح یا بند اضافه‌ای.

    دستور load_org_identity دوباره باید اجرا شود تا ترتیب و نگاشت جدید
    در دیتابیس شما اعمال شود (امن است — چیزی حذف نمی‌کند، فقط ترتیب و
    اتصال هر ارزش به بند مربوطه را به‌روزرسانی می‌کند):
    python manage.py load_org_identity

    فایل‌هایی که باید overwrite شوند:
    - strategic/views.py
    - strategic/management/commands/load_org_identity.py
    - templates/strategic/org_identity.html

    نیازی به makemigrations/migrate نیست، فقط دستور بالا و ری‌استارت سرور.

۶۹. به‌روزرسانی کامل «بانک الزامات قانونی» از فایل جدید کاربر
    («بانک_الزامات_قانونی_تحلیلی_نسخه_یکتا.xlsx»، شیت «داده کامل»):

    الف) دو ستون جدید «درون سازمانی» و «برون سازمانی» (کنار ستون «سازمانی»)
    به مدل LegalRequirement اضافه شد: `is_internal`، `is_external`. یه
    migration جدید لازمه:
    python manage.py makemigrations strategic
    python manage.py migrate

    این دو ستون همه‌جا اضافه شدن: جدول اصلی صفحه (بج جدید رنگی کنار
    عنوان)، فرم افزودن/ویرایش (چک‌باکس)، و خروجی/ورودی اکسل (دو ستون
    جدید، دقیقاً کنار ستون «سازمانی»).

    ب) داده‌ی فایل جدید کاربر (۱۳۴ ردیف خام) با دقت بررسی و ۲۳ ردیف
    تکراری واقعی حذف شد، قبل از ورود به سامانه:
    - ۲۰ ردیف زیر معاونت «منابع انسانی - ایمنی و بهداشت» که کاملاً
      تکراری ردیف‌های موجود زیر «ایمنی و بهداشت» بودن (طبق دستور صریح
      کاربر)
    - ۳ ردیف که بعد از یکی‌کردن دو معاونت «تامین خارج گروه» و «بازرگانی
      خارجی» زیر یک نام مشترک، دقیقاً هم‌عنوان و هم‌مأخذ شدن (طبق دستور
      صریح کاربر)

    ج) سه معاونت بر اساس دستور کاربر یکی/تغییرنام شدند:
    - «ایمنی و بهداشت» + «منابع انسانی» (با هر دو تایپ «منایع انسانی» و
      «منابع انسانی») ← «معاونت منابع انسانی» (۳۴ ردیف نهایی)
    - «تامین خارج گروه» + «بازرگانی خارجی» ← «معاونت بازرگانی» (که با
      ۴ ردیف قبلی همین معاونت هم ادغام شد؛ ۱۵ ردیف نهایی)

    نتیجه‌ی نهایی: ۱۱۱ ردیف یکتا (بدون تکراری)، در دستور بارگذاری
    load_real_legal_requirements.py کاملاً بازنویسی شد:
    python manage.py load_real_legal_requirements

    ⚠️ این دستور طبق روال همیشگی این بانک خاص، کل بانک قبلی الزامات را
    پاک و با این ۱۱۱ ردیف جایگزین می‌کند (نه افزودن) — همون‌طور که دستور
    قبلی هم این‌کار رو می‌کرد.

    فایل‌هایی که باید overwrite شوند:
    - strategic/models.py
    - strategic/views.py
    - strategic/forms.py
    - strategic/migrations/0048_legalrequirement_is_external_and_more.py (فایل جدید)
    - strategic/management/commands/load_real_legal_requirements.py
    - templates/strategic/legal_requirements.html

    ترتیب دقیق دستورها بعد از overwrite کردن فایل‌ها:
    python manage.py makemigrations strategic
    python manage.py migrate
    python manage.py load_real_legal_requirements

    (این تغییرات با یک import/export آزمایشیِ کامل اکسل هم تست شد — بدون
    خطا و بدون افت داده.)

۷۰. بازچینی منوی سایدبار طبق درخواست کاربر (فقط templates/strategic/base.html،
    بدون تغییر مدل/migration):

    الف) عنوان «تحلیل PESTEL» → «تحلیل Pestel (محیط بیرون دور)»
    ب) عنوان «پنج نیروی رقابتی Porter» → «تحلیل Porter (محیط بیرون نزدیک)»
    ج) عنوان بخش «آینده‌پژوهی» → «آینده‌پژوهی (سناریوپردازی)»
    د) «نقشه استراتژیک» و «نقشه ریسک» از بخش «اجرا و پایش» به بخش «تدوین
    راهبرد» منتقل شدند، درست بعد از «تحلیل SWOT» (ترتیب جدید تدوین راهبرد:
    تحلیل SWOT ← نقشه استراتژیک ← نقشه ریسک). بخش «اجرا و پایش» فقط
    «اهداف کلان و KPI شرکت» و «پروژه‌های تحول» رو نگه داشت.

    نکته: بخش‌های «تحلیل محیط بیرونی» (الزامات قانونی/ذینفعان/بانک عوامل
    محیطی/Pestel/Porter) و «تحلیل محیط داخلی» (McKinsey 7S/زنجیره ارزش)
    از قبل هم به همین شکل (عنوان اصلی + زیرمجموعه) در منو بودن؛ فقط
    عنوان‌های داخلشون طبق بالا عوض شد.

    فایلی که باید overwrite شود:
    - templates/strategic/base.html

    نیازی به هیچ دستوری نیست — فقط فایل را جایگزین و سرور را ری‌استارت
    کنید.

۷۱. تبدیل «تحلیل محیط بیرونی» و «تحلیل محیط داخلی» به آکاردئون واقعی
    قابل‌کلیک (باز/بسته‌شونده) — قبلاً فقط یک عنوان ثابت بودن که همیشه
    زیرمجموعه‌هاشون نمایش داده می‌شد؛ حالا با کلیک باز/بسته می‌شن، و
    پیش‌فرض بسته‌ان (منو کوتاه‌تر و مرتب‌تر می‌شه). وقتی کاربر توی یکی از
    صفحات زیرمجموعه باشه (مثلاً Pestel)، همون بخش خودکار باز و رنگی نشون
    داده می‌شه که بدونه کجاست.

    فایل‌هایی که باید overwrite شوند:
    - templates/strategic/base.html
    - static/css/strategic.css

    نیازی به هیچ دستوری نیست — فقط فایل‌ها را جایگزین و سرور را ری‌استارت
    کنید (یک بار Ctrl+F5 هم بزنید تا کش CSS پاک بشه).

۷۲. رفع مشکلی که کاربر بعد از تست بند ۷۱ گزارش داد: دکمه‌های آکاردئون
    («تحلیل محیط بیرونی»/«تحلیل محیط داخلی») به شکل خام و بدون استایل
    (دکمه‌ی پیش‌فرض مرورگر، نه هماهنگ با قالب تیره‌ی سایدبار) دیده
    می‌شدن، و زیرمنوها هم همیشه باز می‌موندن، حتی توی صفحه‌ی خانه.

    علت: دقیقاً همون نکته‌ی بند ۶۵ — فایل static/css/strategic.css که
    توی بند ۷۱ عوض شده بود، بدون collectstatic روی سرور شما اعمال نشده
    بود؛ **من فراموش کردم این‌بار هم دستور collectstatic رو بگم، عذر
    می‌خوام.**

    برای اینکه این دسته از مشکل دیگه هیچ‌وقت باعث خرابی واقعی نشه (حتی
    اگه یه‌بار دیگه collectstatic یادم بره)، جاوااسکریپت باز/بسته‌شدن
    منو رو مستقل از CSS خارجی کردم — حالا با style="display:flex/none"
    مستقیم روی خود عنصر کار می‌کنه، نه فقط کلاس CSS. یعنی حتی اگه CSS
    جمع‌آوری نشده باشه، منو درست باز/بسته می‌شه (فقط ظاهرش زشت‌تر می‌مونه
    تا collectstatic بزنید).

    فایلی که باید overwrite شود:
    - templates/strategic/base.html

    ⚠️ **این‌بار حتماً این دستور رو هم بزنید** (فایل CSS بند ۷۱ رو هم اگه
    هنوز نزدید، همین الان اعمال می‌کنه):
    python manage.py collectstatic --noinput

    بعد سرور رو ری‌استارت کنید و توی مرورگر Ctrl+F5 بزنید.

۷۳. دو مورد دیگر که کاربر گزارش داد:

    الف) آیکون کنار «تحلیل ذینفعان» (fa-people-group) و «ماتریس اثر
    متقابل» (fa-arrows-turn-to-dots) توی نسخه‌ی FontAwesome سرور شما لود
    نمی‌شدن (احتمالاً این دو آیکون خاص توی نسخه‌ی محلی/آفلاین
    FontAwesome شما موجود نیستن). جایگزین شدن با:
    - تحلیل ذینفعان: fa-user
    - ماتریس اثر متقابل: fa-table-cells (که جالبه همون آیکونیه که خود
      صفحه‌ی خانه از قبل برای همین ماژول استفاده می‌کرد — الان همه‌جا
      یکدست شد)
    این دو آیکون توی ۵ فایل عوض شدن (هر جا استفاده شده بودن، نه فقط
    سایدبار): base.html، home.html، pestel.html، porter.html،
    environmental_factors.html.

    ب) آکاردئون سایدبار حالا فقط یکی از دو گروه («تحلیل محیط بیرونی» /
    «تحلیل محیط داخلی») هم‌زمان باز می‌مونه — با باز کردن یکی، اون یکی
    خودکار بسته می‌شه.

    فایل‌هایی که باید overwrite شوند:
    - templates/strategic/base.html
    - templates/strategic/home.html
    - templates/strategic/pestel.html
    - templates/strategic/porter.html
    - templates/strategic/environmental_factors.html

    نیازی به هیچ دستوری نیست — فقط فایل‌ها را جایگزین و سرور را ری‌استارت
    کنید (چون فقط تمپلیت عوض شده، نه CSS/JS مجزا، نیازی به collectstatic
    هم نیست).

۷۴. بازطراحی کامل بخش PESTEL و بارگذاری داده‌ی واقعی جدید (۳۸ عامل) از
    فایل «ورودی_سیستم.xlsx» (شیت pestel):

    الف) بخش‌های «فرصت‌ها» و «تهدیدها» که قبلاً به‌صورت دو ستون جدا کنار
    «عوامل محیطی» نمایش داده می‌شدن، طبق درخواست صریح کاربر کاملاً حذف
    شدن. الان هر بُعد (سیاسی/اقتصادی/...) فقط یک لیست واحده. «نوع اثر»
    (فرصت/تهدید/فرصت‌و‌تهدید) دیگه یک بخش جدا نیست، فقط یک برچسب رنگی
    کنار خود عامله.

    ب) سه فیلد جدید به مدل PestelFactor اضافه شد: `effect_type` (نوع اثر
    — برچسب کنار متن)، `related_standard` (استاندارد مرتبط — برچسب کنار
    متن)، `detailed_description` و `scoring_guide` (هر دو فقط با نگه‌داشتن
    ماوس روی متن/تگ «راهنما» نمایش داده می‌شن، نه به‌صورت دائمی — طبق
    درخواست کاربر). یه migration جدید لازمه:
    python manage.py makemigrations strategic
    python manage.py migrate

    ج) فیلدهای قدیمی (احتمال، عدم‌قطعیت، افق زمانی، روند) دست‌نخورده
    موندن توی مدل و فرم ویرایش، ولی چون فایل جدید این داده‌ها رو نداشت،
    برای هر ۳۸ عامل تازه با مقدار خنثای پیش‌فرض (احتمال=۳، عدم‌قطعیت=
    متوسط، افق=میان‌مدت، روند=ثابت) پر می‌شن — خودتون بعداً هرکدوم رو
    لازم بود دستی اصلاح کنید. ستون‌های «میانگین هر سطر» و «فراوانی اثر
    بالا/بسیاربالا» فایل اکسل، طبق تصمیم صریح کاربر، اصلاً وارد سامانه
    نشدن.

    د) داده‌ی قبلی PESTEL (۳۸ مورد قدیمی) کاملاً پاک و با این ۳۸ مورد
    واقعی جدید جایگزین شد:
    python manage.py load_real_pestel

    ⚠️ **نکته‌ی مهم:** روی کپی آزمایشی از بکاپ شما بررسی کردم و ۲ آیتم
    SWOT و ۲ عامل ماتریس اثر متقابل به عوامل قدیمی PESTEL وصل بودن. بعد
    از این جایگزینی، خودِ آن ۲+۲ مورد پاک نمی‌شن (فقط ارتباطشون با
    PESTEL قطع می‌شه، چون این رابطه از نوع SET_NULL است، نه CASCADE) —
    اگه لازم بود، بعداً می‌تونید از همون صفحات SWOT/ماتریس اثر متقابل
    دوباره به عامل جدید مرتبط‌شون کنید.

    فایل‌هایی که باید overwrite شوند:
    - strategic/models.py
    - strategic/views.py
    - strategic/forms.py
    - strategic/migrations/0049_pestelfactor_detailed_description_and_more.py (فایل جدید)
    - strategic/management/commands/load_real_pestel.py (فایل جدید)
    - templates/strategic/pestel.html

    ترتیب دقیق دستورها بعد از overwrite کردن فایل‌ها:
    python manage.py makemigrations strategic
    python manage.py migrate
    python manage.py load_real_pestel

۷۵. مدیریت فضای مصرفی صفحه‌ی PESTEL (فقط CSS/HTML، بدون تغییر مدل):

    الف) بُعدهای شش‌گانه (سیاسی/اقتصادی/...) از حالت تک‌ستونه (هرکدوم کل
    عرض صفحه) به گرید دوستونه تبدیل شدن — دو بُعد کنار هم.

    ب) ردیف تگ‌های تحلیل (اولویت/اثر/احتمال/عدم‌قطعیت/افق/روند) که قبلاً
    زیر هر عامل با فاصله‌ی جدا می‌اومد، الان داخل همون بلوک متن ادغام شد
    (بدون خط اضافه، فقط با یک wrap طبیعی) — در مجموع فضای مصرفی صفحه
    تقریباً نصف شد.

    فایلی که باید overwrite شود:
    - templates/strategic/pestel.html

    نیازی به هیچ دستوری نیست — فقط فایل را جایگزین و سرور را ری‌استارت
    کنید.

۷۶. اصلاح شلوغی جلوی هر عامل PESTEL طبق بازخورد کاربر: الان فقط برچسب
    «نوع اثر» (فرصت/تهدید/فرصت‌وتهدید) کنار خود متن عامل می‌مونه؛ همه‌ی
    بقیه‌ی تگ‌ها (استاندارد مرتبط، اولویت، اثر، احتمال، عدم‌قطعیت، افق
    زمانی، روند، راهنما، و بج‌های «استفاده در SWOT/ماتریس اثر متقابل»)
    به یک ردیف مجزا و جمع‌وجور زیر متن منتقل شدن (نه دیگه همه با هم روی
    یک خط).

    فایلی که باید overwrite شود:
    - templates/strategic/pestel.html

    نیازی به هیچ دستوری نیست — فقط فایل را جایگزین و سرور را ری‌استارت
    کنید.

۷۷. حذف فیلد بلااستفاده و گیج‌کننده «نوع» از فرم ویرایش PESTEL: این فیلد
    قدیمی قبلاً برای تقسیم به سه بخش «عوامل/فرصت/تهدید» بود که در بند
    ۷۴ حذف شدن؛ از اون موقع این فیلد هیچ اثری روی نمایش نداشت (کارش رو
    «نوع اثر» گرفته بود) و فقط باعث سردرگمی می‌شد. از فرم حذف شد (نه از
    مدل — رکوردهای جدید همچنان با مقدار پیش‌فرض «factor» ذخیره می‌شن،
    بدون نیاز به دخالت دستی).

    فایل‌هایی که باید overwrite شوند:
    - strategic/forms.py
    - templates/strategic/pestel.html

    نیازی به هیچ دستوری نیست (مدل تغییر نکرده) — فقط فایل‌ها را جایگزین
    و سرور را ری‌استارت کنید.

۷۸. تعداد عوامل هر بُعد PESTEL به‌صورت یک بج کوچیک سمت چپ همون ردیفی که
    اسم بُعد (سیاسی/اقتصادی/...) هست اضافه شد (مثلاً «۹ عامل»).

    فایلی که باید overwrite شود:
    - templates/strategic/pestel.html

    نیازی به هیچ دستوری نیست — فقط فایل را جایگزین و سرور را ری‌استارت
    کنید.

۷۹. بج «استاندارد مرتبط» هم به ردیف اول (کنار متن، بعد از نوع اثر)
    منتقل شد؛ ردیف دوم زیر متن الان فقط شامل بقیه‌ی تگ‌ها (اولویت/اثر/
    احتمال/عدم‌قطعیت/افق/روند/راهنما/استفاده در SWOT و ماتریس اثر متقابل).

    فایلی که باید overwrite شود:
    - templates/strategic/pestel.html

    نیازی به هیچ دستوری نیست — فقط فایل را جایگزین و سرور را ری‌استارت
    کنید.

۸۰. بازطراحی کامل بخش Porter (پنج نیروی رقابتی) و بارگذاری داده‌ی واقعی
    جدید (۳۱ عامل) از فایل «ورودی_سیستم.xlsx» (شیت porter):

    الف) مدل PorterForce از حالت قدیمی (۵ رکورد ثابت، یکی به‌ازای هر
    نیرو، فقط با «شدت/دلایل/نتیجه») به ساختار غنی و چندعاملی مثل PESTEL
    تبدیل شد — هر نیرو می‌تواند چند عامل جداگانه داشته باشد، هرکدام با:
    متن، نوع اثر (فرصت/تهدید/هردو، کنار متن)، استاندارد مرتبط (کنار
    متن)، توضیح تفصیلی و راهنمای امتیازدهی (هر دو با هاور)، و اثر/
    احتمال/عدم‌قطعیت/افق/روند (فیلدهای قدیمی، روی مقدار خنثی، قابل‌ویرایش).
    یه migration جدید لازمه:
    python manage.py makemigrations strategic
    python manage.py migrate

    ب) طراحی بصری جدید: یک چرخ SVG با ساختار کلاسیک پورتر — «قدرت رقبای
    موجود» در مرکز (چون طبق خود مدل پورتر، ۴ نیروی دیگر روی شدت رقابت
    داخلی فشار می‌آورند)، و ۴ نیروی دیگر دورش با فلش‌های رو به داخل که
    همین فشار را نشان می‌دهند — نه یک چرخ چرخه‌ای ساده مثل ارکان جهت‌ساز.
    زیر چرخ، کارت جداگانه برای هر نیرو با همون سبک فشرده‌ی دوخطی PESTEL
    (متن+نوع‌اثر+استاندارد در خط اول، بقیه‌ی تگ‌ها خط دوم). کلیک روی هر
    بخش چرخ، به کارت مربوطه اسکرول می‌کند.

    ج) داده‌ی قبلی Porter (۵ رکورد ثابت قدیمی) کاملاً پاک و با ۳۱ عامل
    واقعی جدید جایگزین شد:
    python manage.py load_real_porter

    ⚠️ **نکته‌ی مهم (مثل PESTEL):** روی کپی آزمایشی بررسی کردم، ۳ آیتم
    SWOT و ۴ عامل ماتریس اثر متقابل به نیروهای قدیمی Porter وصل بودن.
    بعد از جایگزینی، خودِ آن موارد پاک نمی‌شن (رابطه از نوع SET_NULL
    است)، فقط ارتباطشون با Porter قطع می‌شه — در صورت نیاز از همون
    صفحات دوباره وصلشون کنید.

    د) صفحه‌ی SWOT هم به‌روزرسانی شد: رادیوی «نیروی پورتر مرتبط» حالا
    متن هر عامل را هم نشان می‌دهد (نه فقط اسم نیرو)، چون هر نیرو الان
    چند عامل مجزا دارد — دقیقاً مثل الگوی موجود برای PESTEL.

    فایل‌هایی که باید overwrite شوند:
    - strategic/models.py
    - strategic/views.py
    - strategic/forms.py
    - strategic/admin.py
    - strategic/urls.py
    - strategic/migrations/0050_alter_porterforce_options_and_more.py (فایل جدید)
    - strategic/management/commands/load_real_porter.py (فایل جدید)
    - templates/strategic/porter.html
    - templates/strategic/_porter_item.html (فایل جدید — پارشیال مشترک)
    - templates/strategic/swot.html

    ترتیب دقیق دستورها بعد از overwrite کردن فایل‌ها:
    python manage.py makemigrations strategic
    python manage.py migrate
    python manage.py load_real_porter

۸۱. اصلاح کامل طراحی چرخ Porter طبق بازخورد کاربر (بند ۸۰ اشتباه بود):

    الف) طرح قبلی «هاب-اسپوک نامتقارن» (۴ نیرو دور یک مرکز) حذف شد؛ حالا
    دقیقاً همون الگوی چرخ ارکان جهت‌ساز (که قبلاً تأیید شده بود) با ۵
    قطعه‌ی کاملاً مساوی برای هر ۵ نیرو (شامل «قدرت رقبای موجود»)، به‌همراه
    آیکون‌های اتصال‌دهنده‌ی چرخشی بین قطعات، دقیقاً مثل ارکان جهت‌ساز.

    ب) «قدرت رقبای موجود» دیگر در مرکز نیست — یکی از ۵ قطعه‌ی پیرامونی
    مثل بقیه است. مرکز چرخ الان فقط یک عنوان ثابت و غیرقابل‌کلیک است:
    «پنج نیروی رقابتی پورتر / Porter's Five Forces» — هیچ داده یا لینکی
    به مرکز وصل نیست.

    ج) باگ لرزش/جابه‌جایی هنگام hover روی دایره‌ی مرکز (دقیقاً همون باگ
    بند ۶۷ در ارکان جهت‌ساز) رفع شد — چون مرکز دیگر قابل‌کلیک/hover هم
    نیست (`pointer-events:none`)، اصلاً این باگ امکان بروز ندارد.

    فایل‌هایی که باید overwrite شوند:
    - strategic/views.py
    - templates/strategic/porter.html

    نیازی به هیچ دستوری نیست — فقط فایل‌ها را جایگزین و سرور را ری‌استارت
    کنید.

۸۲. بازچینی صفحه‌ی Porter طبق درخواست کاربر:

    الف) جدول «عوامل کلیدی (بر اساس اثر × احتمال وقوع)» بالای صفحه کاملاً
    حذف شد.

    ب) چیدمان جدید دوستونه: چرخ سمت راست، یک پنل جزئیات خالی سمت چپ. با
    کلیک روی هر قطعه از چرخ، عوامل همون نیرو بلافاصله توی همون پنل کنار
    چرخ نمایش داده می‌شه (بدون اسکرول به پایین صفحه).

    ج) کارت‌های کامل هر نیرو که پایین صفحه بودن، دست‌نخورده و پایین صفحه
    باقی موندن (طبق درخواست صریح کاربر) — یعنی هر عامل الان توی صفحه دو
    بار رندر می‌شه (یک‌بار توی پنل کنار چرخ وقتی کلیک می‌کنید، یک‌بار
    توی گرید کامل پایین) — کاملاً بی‌خطر و بدون اثر روی داده.

    فایل‌هایی که باید overwrite شوند:
    - strategic/views.py
    - templates/strategic/porter.html

    نیازی به هیچ دستوری نیست — فقط فایل‌ها را جایگزین و سرور را ری‌استارت
    کنید.

۸۳. دو اصلاح ظاهری Porter که کاربر گزارش داد:

    الف) هدر پنل جزئیاتی که کنار چرخ باز می‌شه (وقتی روی یک قطعه کلیک
    می‌کنید)، رنگ و شکلش با هدر کارت‌های پایین صفحه یکی نبود (بی‌رنگ/سفید
    بود). علتش این بود که قانون رنگ‌آمیزی فقط برای کارت‌های پایین تعریف
    شده بود، نه برای پنل کنار چرخ. الان هر دو دقیقاً یک شکل و رنگ دارن.

    ب) آیکون «قدرت چانه‌زنی تأمین‌کنندگان» (fa-truck-field) لود نمی‌شد —
    مثل سه آیکون قبلی (بند ۷۳)، این هم از نسل جدید FontAwesome ۶ بود که
    توی نسخه‌ی آفلاین سرور شما نیست. جایگزین شد با fa-truck (کلاسیک و
    مطمئن). با احتیاط، آیکون «تهدید کالاها/خدمات جایگزین» (fa-shuffle، از
    همون نسل جدید) هم پیش‌دستانه با fa-retweet عوض شد تا دوباره گزارش
    نشه.

    این آیکون‌ها فقط توی کد (views.py از طریق models.py) تعریف شدن، نه
    توی دیتابیس — پس نیازی به migration یا بارگذاری مجدد داده نیست.

    فایل‌هایی که باید overwrite شوند:
    - strategic/models.py
    - templates/strategic/porter.html

    نیازی به هیچ دستوری نیست — فقط فایل‌ها را جایگزین و سرور را ری‌استارت
    کنید.

۸۴. اسکرول داخلی برای پنل جزئیات کنار چرخ Porter: وقتی تعداد عوامل یک
    نیرو زیاد بود، پنل به‌جای اسکرول داخلی، کل ارتفاعش رو زیاد می‌کرد و
    کارت‌های پایین صفحه رو هول می‌داد پایین‌تر. الان پنل یک ارتفاع ثابت
    (هم‌قد چرخ) داره و اگه عوامل بیشتر جا نشن، فقط خودِ پنل اسکرول
    می‌شه، نه کل صفحه.

    فایلی که باید overwrite شود:
    - templates/strategic/porter.html

    نیازی به هیچ دستوری نیست — فقط فایل را جایگزین و سرور را ری‌استارت
    کنید.

۸۵. بازطراحی کامل صفحه‌ی McKinsey 7S طبق طرحی که کاربر تأیید کرد (بعد از
    یک پیش‌نمایش HTML جداگانه که قبل از پیاده‌سازی روی کد واقعی نشونش
    دادم):

    الف) دیاگرام شبکه‌ای واقعی (نه چرخ رنگی مثل ارکان جهت‌ساز/PESTEL/Porter)
    — دقیقاً همون شکل معروف خودِ مدل مکنزی: «ارزش‌های مشترک» در مرکز، ۶
    مؤلفه‌ی دیگر دورش به‌شکل شش‌ضلعی، با خط‌های SVG واقعی که هم هرکدوم
    را به مرکز وصل می‌کنند هم به همسایه‌های کناری‌شان (نه قطعات پای‌چارت).

    ب) داخل هر گره، به‌جای برچسب «سخت»/«نرم» که در نسخه‌ی اول پیشنهادی
    بود، نام انگلیسی مصوب خودِ مدل مکنزی نمایش داده می‌شود (Strategy,
    Structure, Systems, Shared Values, Skills, Staff, Style).

    ج) کارت‌های پایین صفحه (که محتوای واقعی — وضعیت فعلی + نقاط قوت/ضعف —
    را نشان می‌دهند و کاملاً دست‌نخورده از قبل حفظ شدند) به سه بخش
    دسته‌بندی شدند: «ارزش‌های مشترک» (هسته)، «عناصر سخت» (استراتژی/
    ساختار/سیستم‌ها)، «عناصر نرم» (مهارت‌ها/کارکنان/سبک). با کلیک روی هر
    گره از دیاگرام، کارت مربوطه پایین صفحه هایلایت و به آن اسکرول می‌شود.

    این تغییرات فقط ظاهری‌اند — مدل McKinsey7S فقط یک دیکشنری ثابت رنگ/
    آیکون/نام‌انگلیسی (`STYLE`) در کد گرفت، نه فیلد پایگاه‌داده‌ی جدید —
    پس نیازی به migration نیست.

    فایل‌هایی که باید overwrite شوند:
    - strategic/models.py
    - strategic/views.py
    - templates/strategic/mckinsey7s.html
    - templates/strategic/_s7_card.html (فایل جدید — پارشیال کارت مشترک)

    نیازی به هیچ دستوری نیست — فقط فایل‌ها را جایگزین و سرور را ری‌استارت
    کنید.

۸۶. بازچینی صفحه‌ی McKinsey 7S طبق درخواست کاربر: دیاگرام شبکه‌ای سمت
    راست، کارت «ارزش‌های مشترک» (که قبلاً یک بخش جدای تمام‌عرض زیر
    دیاگرام بود) کنارش سمت چپ منتقل شد — استفاده‌ی بهتر از فضا، و
    بخش‌های «عناصر سخت» و «عناصر نرم» بالاتر میان (نیاز به اسکرول کمتر).

    فایلی که باید overwrite شود:
    - templates/strategic/mckinsey7s.html

    نیازی به هیچ دستوری نیست — فقط فایل را جایگزین و سرور را ری‌استارت
    کنید.

۸۷. به‌روزرسانی کامل «تحلیل ذینفعان» از فایل جدید کاربر
    («ورودی_سیستم.xlsx»، شیت «داده کامل»، ۱۹۰ ردیف واقعی با نام
    معاونت‌های یکپارچه‌شده):

    الف) دو ستون جدید «درون سازمانی» و «برون سازمانی» (دقیقاً مثل بند
    ۶۹ برای الزامات قانونی) به مدل Stakeholder اضافه شد: `is_internal`،
    `is_external`. یه migration جدید لازمه:
    python manage.py makemigrations strategic
    python manage.py migrate

    ب) طبق درخواست صریح کاربر، این دو تگ (برخلاف الزامات قانونی که جلوی
    عنوان الزام بودن) اینجا جلوی «نام ذینفع» در جدول اصلی نمایش داده
    می‌شن. فرم افزودن/ویرایش هم دو چک‌باکس جدید گرفت.

    ج) خروجی و ورودی اکسل ذینفعان (که از قبل توی همین صفحه بود) هم مثل
    الزامات قانونی به‌روزرسانی شد — دو ستون جدید دقیقاً کنار «نام ذینفع»
    اضافه شدن، هم در خروجی هم در پردازش ورودی. چرخه‌ی کامل (نمایش →
    خروجی → ورود دوباره) تست شد، بدون افت داده.

    د) داده‌ی قبلی ذینفعان (۲۰۶ ردیف، شامل چند ردیف خراب/تکراری از یک
    ایمپورت قبلی) کاملاً پاک و با ۱۹۰ ردیف واقعی و تمیز جدید جایگزین شد
    — نام معاونت‌ها هم در همین فرآیند یکپارچه شدن (۱۰ معاونت یکتا، بدون
    فاصله‌ی اضافی تکراری):
    python manage.py load_real_stakeholders_full

    فایل‌هایی که باید overwrite شوند:
    - strategic/models.py
    - strategic/views.py
    - strategic/forms.py
    - strategic/migrations/0051_stakeholder_is_external_stakeholder_is_internal.py (فایل جدید)
    - strategic/management/commands/load_real_stakeholders_full.py
    - templates/strategic/stakeholders.html

    ترتیب دقیق دستورها بعد از overwrite کردن فایل‌ها:
    python manage.py makemigrations strategic
    python manage.py migrate
    python manage.py load_real_stakeholders_full

۸۸. تکمیل هاور «سناریوی هوای مه‌آلود» در صفحه‌ی سناریوهای راهبردی: این
    قابلیت (نمایش لیست کامل کسب‌وکارها با برچسب «فرصت و تهدید» موقع
    نگه‌داشتن ماوس روی کارت «هوای مه‌آلود») از قبل توی کد شما وجود
    داشت و کاملاً داینامیک است — مستقیم از جدول کسب‌وکارها می‌خونه، پس
    اگه بعداً کسب‌وکار جدیدی اضافه کنید، خودکار توی این هاور هم اضافه
    می‌شه، بدون نیاز به تغییر کد.

    مشکل این بود: این دو فایل (که تغییری هم توشون نداده بودم) توی
    بسته‌های قبلی که بهتون دادم syncنشده بودن — یکی کامل جا افتاده بود
    (scenarios.html)، یکی نسخه‌ی قدیمی‌تری بود (views.py). الان هر دو
    کامل و به‌روز اضافه شدن.

    فایل‌هایی که باید overwrite شوند:
    - strategic/views.py
    - templates/strategic/scenarios.html

    نیازی به هیچ دستوری نیست — فقط فایل‌ها را جایگزین و سرور را ری‌استارت
    کنید.

۸۹. جایگزینی کامل صفحه‌ی خانه با «سامانه هوشمند مدیریت عملکرد» — یک
    اینفوگرافی تعاملی و تک‌صفحه که کل فرایند سامانه رو از ارکان جهت‌ساز
    تا اهداف و KPI شرکت، در ۶ ایستگاه به‌هم‌متصل (با فلش‌های SVG بین
    مراحل) نشون می‌ده. بعد از چند دور پیش‌نمایش و بازخورد مستقیم کاربر
    (که قبل از پیاده‌سازی روی کد واقعی انجام شد) طراحی نهایی شد.

    الف) هر ۱۸ ماژول ریز داخل ۶ ایستگاه، یه لینک واقعی و قابل‌کلیکه که
    مستقیم به همون صفحه می‌بره؛ کنارشون هم عدد واقعی و زنده از دیتابیس
    (نه عدد نمونه) نشون داده می‌شه — مثلاً «الزامات قانونی: ۱۱۱»،
    «ذینفعان: ۱۹۰». این اعداد هر بار صفحه باز بشه دوباره محاسبه می‌شن،
    پس همیشه به‌روزن.

    ب) بالای صفحه یه نوار آمار کلی هم هست: تعداد کل ماژول‌ها (۱۷، ثابت)،
    مجموع کل رکوردهای ثبت‌شده در کل سامانه (جمع پویا از همه‌ی ماژول‌ها)،
    و درصد پیشرفت اهداف استراتژیک — همون شاخص واقعی و از قبل موجود
    (میانگین وزنی وضعیت اهداف: بر مسیر=۱، زیرنظر=۰.۵، در خطر=۰).

    ج) توی سایدبار، جلوی «خانه» یه برچسب کوچیک «(اینفوگرافی)» اضافه شد
    تا مشخص باشه این صفحه دیگه یه داشبورد ساده نیست.

    د) صفحه‌ی خانه‌ی قبلی (گیج‌های SVG، فید فعالیت‌های اخیر، نوار جستجو)
    کاملاً حذف شد — طبق درخواست صریح کاربر برای جایگزینی کامل.

    فایل‌هایی که باید overwrite شوند:
    - strategic/views.py
    - templates/strategic/base.html
    - templates/strategic/home.html

    نیازی به هیچ دستوری نیست (فقط context جدید به ویو موجود اضافه شد، نه
    فیلد پایگاه‌داده‌ی جدید) — فقط فایل‌ها را جایگزین و سرور را ری‌استارت
    کنید.

۹۰. سه اصلاح ظاهری صفحه‌ی خانه‌ی جدید طبق بازخورد کاربر:

    الف) نوار بالای صفحه (breadcrumb سفید که با سایر بخش‌های خانه که تیره
    بودن همخوانی نداشت) — الان فقط توی همین صفحه (و فقط همین صفحه، جای
    دیگه دست‌نخورده) هم‌رنگ پس‌زمینه‌ی تیره شد. این کار با یک کلاس داینامیک
    جدید روی خود body انجام شد (`page-{{ active_page }}`) — یک تغییر کوچک
    و قابل‌استفاده‌ی مجدد در base.html که به هر صفحه اجازه می‌ده بعداً
    راحت‌تر استایل اختصاصی خودش رو داشته باشه، بدون اثر روی صفحات دیگه.

    ب) لوگوی سایپا یدک بالای عنوان اضافه شد.

    ج) عرض کارت‌ها و طول فلش‌های اتصال کم شد تا هر ۶ ایستگاه بدون
    اسکرول افقی توی عرض صفحه (حتی روی مانیتورهای کوچک‌تر) جا بشن.

    فایل‌هایی که باید overwrite شوند:
    - templates/strategic/base.html
    - templates/strategic/home.html

    نیازی به هیچ دستوری نیست — فقط فایل‌ها را جایگزین و سرور را ری‌استارت
    کنید (یک Ctrl+F5 هم بزنید).

۹۱. رفع یک باگ واقعی و قدیمی (نه چیزی که در همین گفت‌وگو اضافه شده بود):
    شاخص «ریسک با شدت بالا» توی صفحه‌ی خانه‌ی جدید همیشه صفر نشون
    می‌داد، چون کد دنبال یه سطح به‌اسم `"red"` می‌گشت که سیستم ریسک اصلاً
    همچین مقداری تولید نمی‌کنه (سطوح واقعی: کم/متوسط/زیاد/بحرانی). حالا
    درست شمارش می‌کنه (ریسک‌های سطح «زیاد» یا «بحرانی» را می‌شمارد).

    فایلی که باید overwrite شود:
    - strategic/views.py

    نیازی به هیچ دستوری نیست — فقط فایل را جایگزین و سرور را ری‌استارت
    کنید.

۹۲. برقراری ارتباط بین «تحلیل ذینفعان» و «Porter» طبق درخواست مدیریت:

    الف) یک فیلد جدید و اختیاری به هر ذینفع اضافه شد: «عامل Porter مرتبط»
    (دراپ‌داون، هر ۳۱ عامل پورتر زیر ۵ نیرو). یه migration جدید لازمه:
    python manage.py makemigrations strategic
    python manage.py migrate

    ب) نمایش دوطرفه:
    - توی صفحه‌ی ذینفعان، جلوی نام هر ذینفعی که وصل باشه، یه بج بنفش
      «مرتبط با Porter» نشون داده می‌شه (با هاور، اسم دقیق عامل و نیرو).
    - توی صفحه‌ی Porter، جلوی هر عامل، یه بج جدید «ذینفعان مرتبط» با
      تعداد و اسم دقیق ذینفعانی که بهش وصلن نشون داده می‌شه — دقیقاً مثل
      بج‌های موجود «استفاده در SWOT» و «استفاده در اثرات متقابل».

    ج) فرم افزودن/ویرایش ذینفع هم یه دراپ‌داون جدید گرفت تا مدیر بتونه
    این ارتباط رو دستی برای هر ذینفع تنظیم کنه.

    فایل‌هایی که باید overwrite شوند:
    - strategic/models.py
    - strategic/views.py
    - strategic/forms.py
    - strategic/migrations/0052_stakeholder_related_porter.py (فایل جدید)
    - templates/strategic/stakeholders.html
    - templates/strategic/_porter_item.html

    ترتیب دقیق دستورها بعد از overwrite کردن فایل‌ها:
    python manage.py makemigrations strategic
    python manage.py migrate

۹۲. ارتباط دوطرفه‌ی ذینفعان با Porter (طبق درخواست مدیریت): هر ذینفع الان
    می‌تواند به یکی از ۳۱ عامل Porter وصل شود (فیلد جدید و اختیاری
    `related_porter` روی مدل Stakeholder). یه migration جدید لازمه:
    python manage.py makemigrations strategic
    python manage.py migrate

    نمایش دوطرفه، دقیقاً طبق درخواست:
    - توی صفحه‌ی ذینفعان، جلوی هر ذینفع وصل‌شده، یک بج بنفش «مرتبط با
      Porter» نشان داده می‌شود (با هاور، اسم دقیق عامل Porter).
    - توی صفحه‌ی Porter (هم توی پنل کنار چرخ، هم توی کارت‌های پایین
      صفحه)، جلوی هر عامل، یک بج «ذینفعان مرتبط» با تعداد ذینفعانی که
      بهش وصل‌اند نشان داده می‌شود — دقیقاً هم‌شکل بج‌های موجود «استفاده
      در SWOT» و «استفاده در ماتریس اثر متقابل».

    فرم افزودن/ویرایش ذینفع هم یک دراپ‌داون جدید «عامل Porter مرتبط
    (اختیاری)» گرفت.

    فایل‌هایی که باید overwrite شوند:
    - strategic/models.py
    - strategic/views.py
    - strategic/forms.py
    - strategic/migrations/0052_stakeholder_related_porter.py (فایل جدید)
    - templates/strategic/stakeholders.html
    - templates/strategic/porter.html
    - templates/strategic/_porter_item.html

    ترتیب دقیق دستورها بعد از overwrite کردن فایل‌ها:
    python manage.py makemigrations strategic
    python manage.py migrate

۹۳. نوار بالای صفحه (breadcrumb سفید) طبق درخواست کاربر، حالا توی **همه‌ی
    صفحات سامانه** (نه فقط خانه) هم‌رنگ همون گرادینت تیره‌ای شد که قبلاً
    فقط برای خانه تنظیم کرده بودم. تغییر اصلی توی فایل مشترک
    static/css/strategic.css اعمال شد (سراسری، روی کل سایت)، و بلوک
    تکراری/اختصاصی که قبلاً فقط برای خانه بود از home.html حذف شد (چون
    دیگه لازم نبود).

    فایل‌هایی که باید overwrite شوند:
    - static/css/strategic.css
    - templates/strategic/home.html

    ⚠️ چون فایل CSS عوض شده، این‌بار حتماً بعد از overwrite این دستور را
    هم بزنید:
    python manage.py collectstatic --noinput

    بعد سرور را ری‌استارت کنید و توی مرورگر Ctrl+F5 بزنید.

۹۴. ارتباط دوطرفه‌ی الزامات قانونی با PESTEL (طبق درخواست کاربر): هر
    الزام قانونی الان می‌تواند به یکی از ۳۸ عامل PESTEL وصل شود (فیلد
    جدید و اختیاری `related_pestel` روی مدل LegalRequirement). یه
    migration جدید لازمه:
    python manage.py makemigrations strategic
    python manage.py migrate

    یه پراپرتی جدید هم به مدل PestelFactor اضافه شد: `letter` (حرف
    اختصاصی هر بُعد: P/E/S/T/E/L) — برای استفاده‌ی یکدست همه‌جا.

    نمایش دوطرفه، دقیقاً طبق فرمتی که کاربر خواسته بود:
    - توی صفحه‌ی الزامات قانونی، جلوی هر الزام وصل‌شده، یک بج بنفش
      «PESTEL» نشان داده می‌شود؛ با هاور روی آن دقیقاً این فرمت را
      می‌بینید: «از بخش L (قانونی) — قوانین، استانداردها و تنظیم‌گری»
      (حرف اختصاصی + نام کامل بُعد + عنوان دقیق عامل PESTEL).
    - توی صفحه‌ی PESTEL، جلوی هر عامل، یک بج «استفاده در الزامات قانونی»
      با تعداد الزاماتی که بهش وصل‌اند نشان داده می‌شود — دقیقاً هم‌شکل
      بج‌های موجود «استفاده در SWOT» و «استفاده در ماتریس اثر متقابل».

    فرم افزودن/ویرایش الزام قانونی هم یک دراپ‌داون جدید «عامل PESTEL
    مرتبط (اختیاری)» گرفت.

    فایل‌هایی که باید overwrite شوند:
    - strategic/models.py
    - strategic/views.py
    - strategic/forms.py
    - strategic/migrations/0053_legalrequirement_related_pestel.py (فایل جدید)
    - templates/strategic/legal_requirements.html
    - templates/strategic/pestel.html

    ترتیب دقیق دستورها بعد از overwrite کردن فایل‌ها:
    python manage.py makemigrations strategic
    python manage.py migrate

۹۵. دو اصلاح روی بج «استفاده در الزامات قانونی» در صفحه‌ی PESTEL طبق
    بازخورد کاربر:

    الف) این بج از ردیف دوم (پایین، کنار بقیه‌ی تگ‌های اثر/احتمال/...)
    به ردیف اول منتقل شد — دقیقاً بعد از بج «استاندارد مرتبط»، کنار متن
    خود عامل.

    ب) متن هاور کامل‌تر شد — قبلاً فقط عنوان الزام رو نشون می‌داد، الان
    برای هر الزام، مأخذ و محل کاربرد رو هم توی یه خط جدا نشون می‌ده:
    «عنوان الزام — مأخذ: ... — محل کاربرد: ...» (اگه چند الزام به یک
    عامل وصل باشن، هرکدوم توی خط جدا).

    فایلی که باید overwrite شود:
    - templates/strategic/pestel.html

    نیازی به هیچ دستوری نیست — فقط فایل را جایگزین و سرور را ری‌استارت
    کنید.

۹۶. سه اصلاح روی ارتباط ذینفعان با Porter، طبق بازخورد کاربر بعد از
    تست بند ۹۲:

    الف) **تغییر ساختاری مهم**: ارتباط ذینفع با Porter از «تک‌انتخابی»
    (یک ذینفع فقط به یک عامل) به «چندانتخابی» تبدیل شد (یک ذینفع می‌تواند
    هم‌زمان به چند عامل Porter مرتبط باشد) — چون منطقاً بیشتر ذینفعان
    روی چند نیروی رقابتی هم‌زمان اثر دارند. این یه تغییر ساختاری واقعیه؛
    با احتیاط و در ۴ migration جدا انجام شد تا هیچ داده‌ای گم نشه (حتی
    اگه قبلاً دستی ارتباطی ثبت کرده باشید، خودکار به فرمت جدید منتقل
    می‌شه، نه پاک). فرم افزودن/ویرایش ذینفع هم از دراپ‌داون تک‌انتخابی
    به لیست چک‌باکسی چندانتخابی (پیکرباکس، طبق همون الگوی استاندارد
    نقشه ریسک) تبدیل شد.

    ب) توی صفحه‌ی Porter، بج «ذینفعان مرتبط» از ردیف دوم (پایین) به ردیف
    اول منتقل شد — دقیقاً بعد از بج «استاندارد مرتبط»، کنار متن خود
    عامل.

    ج) هاور بج ذینفعان (هم توی صفحه‌ی ذینفعان، هم توی Porter) کامل‌تر
    شد: علاوه بر نام ذینفع/عامل، حالا «واحد/معاونت» و «نیاز/انتظار»
    ذینفع رو هم توی یه خط جدا نشون می‌ده.

    ⚠️ فیلد قدیمی `related_porter` (تک‌انتخابی) کاملاً از مدل حذف شد —
    اگه جایی دیگه (مثلاً یه ریپورت یا اسکریپت دستی) بهش ارجاع داده
    بودید، باید به `related_porters` (چندانتخابی) به‌روزش کنید.

    فایل‌هایی که باید overwrite شوند:
    - strategic/models.py
    - strategic/forms.py
    - strategic/views.py
    - strategic/migrations/0054_alter_stakeholder_related_porter.py (فایل جدید)
    - strategic/migrations/0055_stakeholder_related_porters.py (فایل جدید)
    - strategic/migrations/0056_copy_related_porter_to_m2m.py (فایل جدید — انتقال امن داده)
    - strategic/migrations/0057_remove_stakeholder_related_porter.py (فایل جدید)
    - templates/strategic/stakeholders.html
    - templates/strategic/_porter_item.html

    ترتیب دقیق دستورها بعد از overwrite کردن فایل‌ها (این ۴ migration
    باید دقیقاً به همین ترتیب اجرا بشن، پس فقط همین دو دستور معمولی رو
    بزنید و جنگو خودش ترتیب رو رعایت می‌کنه):
    python manage.py makemigrations strategic
    python manage.py migrate

۹۷. ظاهر باکس انتخاب عوامل Porter توی فرم ذینفعان از چک‌باکس‌های خام
    مرورگر به همون سبک استاندارد «پیکرباکس» سامانه تبدیل شد (دقیقاً
    همون الگویی که توی نقشه ریسک برای انتخاب سناریوهای مرتبط استفاده
    شده): چک‌باکس مخفی، کل ردیف کلیک‌پذیر، پس‌زمینه‌ی نارنجی کم‌رنگ برای
    موارد انتخاب‌شده.

    فایلی که باید overwrite شود:
    - templates/strategic/stakeholders.html

    نیازی به هیچ دستوری نیست — فقط فایل را جایگزین و سرور را ری‌استارت
    کنید.

۹۸. ارتباط دوطرفه‌ی PESTEL با ذینفعان (طبق درخواست کاربر): هر عامل
    PESTEL می‌تواند به چند ذینفع مرتبط باشد (فیلد جدید و چندانتخابی
    `related_stakeholders` روی مدل PestelFactor). یه migration جدید
    لازمه:
    python manage.py makemigrations strategic
    python manage.py migrate

    برخلاف ارتباط ذینفعان-Porter (که ثبتش از صفحه‌ی ذینفعان بود)، طبق
    درخواست صریح کاربر، ثبت این ارتباط از **صفحه‌ی PESTEL** انجام
    می‌شود — چون عامل PESTEL محیطی است و ذینفع تحت‌تأثیر آن قرار
    می‌گیرد. فرم ویرایش هر عامل PESTEL یه پیکرباکس چک‌باکسی جدید
    «ذینفعان مرتبط» گرفت (همون سبک استاندارد پیکرباکس که برای Porter هم
    اضافه شد).

    نمایش دوطرفه، با هاور کامل:
    - توی صفحه‌ی PESTEL، جلوی هر عامل (ردیف اول، کنار بج الزامات قانونی)،
      یه بج سبز «ذینفعان مرتبط» با تعداد — هاورش برای هر ذینفع، نام +
      واحد/معاونت + نیاز/انتظار رو نشون می‌ده.
    - توی صفحه‌ی ذینفعان (فقط نمایشی، چون ثبتش از PESTEL انجام می‌شه)،
      جلوی هر ذینفع، یه بج سبز «مرتبط با PESTEL» با تعداد — هاورش برای
      هر عامل، حرف اختصاصی + نام کامل بُعد + متن دقیق عامل رو نشون می‌ده
      (مثلاً «E (اقتصادی) — حذف ارز نیمایی...»).

    فایل‌هایی که باید overwrite شوند:
    - strategic/models.py
    - strategic/forms.py
    - strategic/views.py
    - strategic/migrations/0058_pestelfactor_related_stakeholders.py (فایل جدید)
    - templates/strategic/pestel.html
    - templates/strategic/stakeholders.html

    ترتیب دقیق دستورها بعد از overwrite کردن فایل‌ها:
    python manage.py makemigrations strategic
    python manage.py migrate

۹۸. ارتباط دوطرفه‌ی PESTEL با ذینفعان (طبق درخواست کاربر، مشابه بند ۹۴
    برای الزامات قانونی، ولی این‌بار ثبت ارتباط از سمت PESTEL انجام
    می‌شود، نه از سمت ذینفع — چون مفهوماً «عامل PESTEL» محیطی است و
    «ذینفع» گروهی است که از آن اثر می‌گیرد):

    الف) هر عامل PESTEL می‌تواند به چند ذینفع مرتبط باشد (فیلد جدید
    `related_stakeholders`، چندانتخابی از ابتدا — نه مثل Porter که بعداً
    اصلاح شد). یه migration جدید لازمه:
    python manage.py makemigrations strategic
    python manage.py migrate

    ب) ثبت ارتباط فقط از فرم ویرایش PESTEL انجام می‌شود (پیکرباکس
    استاندارد چندانتخابی، دقیقاً همون سبک نقشه ریسک/Porter).

    ج) نمایش دوطرفه:
    - توی PESTEL: بج سبز «ذینفعان مرتبط» با تعداد؛ هاور کامل شامل نام،
      واحد/معاونت، و نیاز/انتظار هر ذینفع (هرکدوم در یک خط جدا).
    - توی صفحه‌ی ذینفعان: بج سبز «مرتبط با PESTEL» با تعداد (فقط
      نمایشی، غیرقابل‌ویرایش از این صفحه)؛ هاور نشان می‌دهد این فقط از
      صفحه‌ی PESTEL قابل ثبت است، و برای هر عامل: حرف اختصاری + نام کامل
      بُعد + عنوان دقیق («L (قانونی) — قوانین، استانداردها و تنظیم‌گری»).

    این تغییرات با یک تست کامل (ثبت واقعی ارتباط از فرم PESTEL، بررسی
    نمایش هر دو بج و متن دقیق هاورها) روی یک کپی تازه از بکاپ اصلی تأیید
    شد.

    فایل‌هایی که باید overwrite شوند:
    - strategic/models.py
    - strategic/forms.py
    - strategic/views.py
    - strategic/migrations/0058_pestelfactor_related_stakeholders.py (فایل جدید)
    - templates/strategic/pestel.html
    - templates/strategic/stakeholders.html

    ترتیب دقیق دستورها بعد از overwrite کردن فایل‌ها:
    python manage.py makemigrations strategic
    python manage.py migrate

۹۹. ثبت خودکار ارتباط‌های منطقی و مطمئن بین PESTEL/Porter و ذینفعان/
    الزامات قانونی (طبق درخواست کاربر) — یک دستور جدید و بدون migration:
    python manage.py link_pestel_porter_relations

    این دستور **فقط** مواردی رو وصل می‌کنه که از نظر محتوا کاملاً روشن
    و بدون ابهامه، نه هر ۱۹۰ ذینفع/۱۱۱ الزام رو به‌زور به یه عامل وصل
    می‌کنه:

    الف) ۴۹ الزام قانونی → PESTEL: مثلاً همه‌ی الزامات ایمنی/بهداشت کار
    زیر معاونت منابع انسانی (۲۶ مورد) → بند PESTEL «مقررات ایمنی و
    بهداشت کار/بیمه»؛ کدهای ISO9001/10002/10004/10015/14001 → بند
    «استانداردهای مدیریتی...»؛ الزامات تجارت بین‌المللی (UCP600، اینکو
    ترمز، گمرک) → بند «ارتباطات بین‌المللی»؛ و چند مورد مشابه دیگر.

    ب) ۳۹ ذینفع → Porter: تأمین‌کنندگان و پیمانکاران → «نقش
    تأمین‌کنندگان» / «پایبندی به تعهدات مالی»؛ شبکه‌ی نمایندگی‌ها →
    «عدم تطابق اعتبار عوامل فروش قطعه شبکه»؛ مشتریان → عوامل مرتبط با
    خریداران.

    ج) ۵ ذینفع → PESTEL: سازمان‌های نظارتی/ممیزی/حسابرسی/بالادستی →
    بند «قوانین، استانداردها و تنظیم‌گری».

    ⚠️ تطبیق‌ها بر اساس **متن دقیق** انجام می‌شه، نه شماره‌ی ردیف —
    یعنی اگه بعداً متن یه عامل یا نام یه ذینفع رو تغییر بدید، دیگه اون
    مورد خودکار پیدا نمی‌شه (و دستور فقط گزارش می‌ده، خطا نمی‌ده). این
    دستور «افزودنی» است، نه «پاک‌وجایگزین» — اجرای دوباره‌ش رکورد
    تکراری نمی‌سازه (چک می‌کنه از قبل وصل نباشه).

    فایل جدیدی که باید اضافه بشه:
    - strategic/management/commands/link_pestel_porter_relations.py

    دستور لازم بعد از اضافه‌کردن فایل (migration لازم نیست):
    python manage.py link_pestel_porter_relations

۱۰۰. ارزیابی کارشناسیِ اثر/احتمال/عدم‌قطعیت/افق‌زمانی/روند برای هر ۶۹
    عامل (۳۸ PESTEL + ۳۱ Porter) — طبق درخواست کاربر — یک دستور جدید و
    بدون migration:
    python manage.py assess_pestel_porter_factors

    تا این‌جا این ۵ فیلد برای همه‌ی عوامل روی مقدار خنثای پیش‌فرض بودن
    (اثر=۳، احتمال=۳، عدم‌قطعیت=متوسط، افق=میان‌مدت، روند=ثابت) چون
    فایل اکسل اصلی کاربر این داده‌ها رو نداشت. حالا هر عامل، بر اساس
    متن خودش، نوع اثر (فرصت/تهدید) و شناخت از صنعت قطعات یدکی خودرو در
    ایران (تورم بالای مزمن، تحریم، بازار غیررسمی قطعات، رقابت پلتفرم‌های
    آنلاین)، مقدار واقعی و متفاوت گرفت — نه یک عدد یکسان برای همه.

    نمونه: «تحریم‌ها» و «تغییرات نرخ ارز و تورم» بالاترین اولویت (۲۵)
    گرفتن؛ «عرضه قطعات غیراصلی/قاچاق» و «گرایش مشتری به قطعات جایگزین
    بدلیل قیمت پایین» بالاترین اولویت سمت Porter (۲۵). توزیع نهایی
    اولویت‌ها بین ۴ تا ۲۵ پخش شده (قبلاً همه دقیقاً ۹ بودن).

    ⚠️ **این یک ارزیابی اولیه‌ی کارشناسی هوش مصنوعیه، نه داده‌ی خامی که
    از فایل اصلی شما اومده باشه** — قابل و لازم است تیم شما این مقادیر
    رو مرور و در صورت نیاز دستی اصلاح کنه (از همون دکمه‌ی مداد روی هر
    عامل). تطبیق بر اساس متن دقیق انجام می‌شه، پس اجرای دوباره‌ی این
    دستور بی‌خطره و مقادیر رو فقط آپدیت می‌کنه.

    فایل جدیدی که باید اضافه بشه:
    - strategic/management/commands/assess_pestel_porter_factors.py

    دستور لازم بعد از اضافه‌کردن فایل (migration لازم نیست):
    python manage.py assess_pestel_porter_factors

۱۰۱. جداسازی بهتر آیتم‌های هاور برای بج‌های ارتباطی چندموردی (طبق
    بازخورد کاربر): وقتی یک عامل PESTEL/Porter یا ذینفع، به چند مورد
    مرتبط بود (مثلاً ۱۰ ذینفع مرتبط با یک عامل Porter)، توی هاور همه
    پشت‌سرهم بدون جداکننده‌ی روشن می‌اومدن. الان جلوی هر مورد یک نشانه‌ی
    ● (چون tooltip خام مرورگر رنگ واقعی/HTML رو پشتیبانی نمی‌کنه، این
    نزدیک‌ترین معادل متنیه) اضافه شد تا هر ردیف واضح از بقیه جدا بشه.
    این تغییر روی هر ۵ بج ارتباطی که تا الان ساختیم اعمال شد: Porter←
    ذینفعان، PESTEL←الزامات‌قانونی، PESTEL←ذینفعان، ذینفعان←Porter،
    ذینفعان←PESTEL.

    فایل‌هایی که باید overwrite شوند:
    - templates/strategic/_porter_item.html
    - templates/strategic/pestel.html
    - templates/strategic/stakeholders.html

    نیازی به هیچ دستوری نیست — فقط فایل‌ها را جایگزین و سرور را ری‌استارت
    کنید.

۱۰۲. هاور کامل‌تر برای بج «استفاده در SWOT» توی صفحه‌ی McKinsey 7S، طبق
    درخواست کاربر: قبلاً فقط «کسب‌وکار (نوع)» رو نشون می‌داد، بدون کد یا
    متن دقیق. الان برای هر آیتم مرتبط:
    خط اول: «SWOT [نام کسب‌وکار] ([نوع — مثلاً نقطه قوت])»
    خط دوم: «[کد دقیق مثل S1]: [متن کامل همون بند SWOT]»

    نکته‌ی فنی: کد S1/W1/O1/T1 هیچ‌جا توی دیتابیس ذخیره نمی‌شه — خودِ
    صفحه‌ی SWOT هم اینو لحظه‌ای، بر اساس ترتیب نمایش هر دسته داخل هر
    کسب‌وکار می‌سازه. برای این‌که کدها با صفحه‌ی SWOT دقیقاً یکی باشن،
    همون منطق ترتیب‌دهی (بر اساس وزن اهمیت و تاریخ ثبت) عیناً توی ویو
    McKinsey 7S هم تکرار شد — تست شد و با کدهای واقعی صفحه‌ی SWOT مطابقت
    داره.

    فایل‌هایی که باید overwrite شوند:
    - strategic/views.py
    - templates/strategic/_s7_card.html

    نیازی به هیچ دستوری نیست — فقط فایل‌ها را جایگزین و سرور را ری‌استارت
    کنید.

۱۰۳. نمایش خودکار «ذینفع مرتبط» و «الزام قانونی مرتبط» توی صفحه‌ی «بانک
    عوامل محیطی» — طبق درخواست کاربر و بحث مفصلی که با هم داشتیم درباره‌ی
    نحوه‌ی کارش. **بدون رابطه‌ی دیتابیسی جدید و بدون نیاز به ثبت دستی
    دوباره**:

    الف) دو پراپرتی پایتونی جدید به مدل EnvironmentalFactor اضافه شد
    (`related_stakeholders_display`، `related_legal_requirements_display`)
    که دقیقاً با همون تکنیک تطبیق متنی‌ای که از قبل برای برچسب «در
    ماتریس اثر متقابل» وجود داشت (`linked_cross_impact_factors`) کار
    می‌کنن: متن هر عامل توی این بانک (که معمولاً یه پسوند «(ادغام با...)»
    اضافه داره) با متن عامل‌های PESTEL/Porter مقایسه می‌شه؛ وقتی معادلش
    پیدا شد، ذینفعان/الزاماتی که قبلاً (توی خود صفحه‌ی PESTEL یا Porter)
    بهش وصل کرده بودید، همینجا هم نشون داده می‌شه.

    ب) توی جدول، ستون «ارتباط» حالا علاوه بر بج قبلی «در ماتریس اثر
    متقابل»، دو بج جدید هم داره: 🟣 **«ذینفع (تعداد)»** و 🟢 **«الزام
    قانونی (تعداد)»**. هاور هر کدوم، دقیقاً مثل بج‌های مشابه توی PESTEL/
    Porter/ذینفعان، جزئیات کامل (نام+واحد+نیاز برای ذینفع؛ عنوان+مأخذ+
    محل‌کاربرد برای الزام) با نشانه‌ی ● بین هر مورد نشون می‌ده.

    ⚠️ چون هیچ فیلد جدیدی به دیتابیس اضافه نشد (فقط پراپرتی پایتونی)،
    **migration لازم نیست**، و هیچ کار دستی جدیدی هم لازم نیست — همون
    ارتباط‌هایی که قبلاً (بند ۹۹) روی PESTEL/Porter ثبت کردید، خودکار
    اینجا هم دیده می‌شن.

    فایل‌هایی که باید overwrite شوند:
    - strategic/models.py
    - templates/strategic/environmental_factors.html

    نیازی به هیچ دستوری نیست — فقط فایل‌ها را جایگزین و سرور را ری‌استارت
    کنید.

۱۰۴. جستجوی زنده برای ۳ ستون باقی‌مانده‌ی جدول «بانک عوامل محیطی» طبق
    درخواست کاربر — قبلاً فقط دسته‌بندی/شرح عامل/نوع اثر سرچ‌باکس
    داشتن، الان «میانگین امتیاز»، «فراوانی اثر بالا» و «ارتباط» (شامل
    بج‌های جدید ذینفع/الزام قانونی که تازه اضافه شدن) هم قابل جستجو شدن.

    فایلی که باید overwrite شود:
    - templates/strategic/environmental_factors.html

    نیازی به هیچ دستوری نیست — فقط فایل را جایگزین و سرور را ری‌استارت
    کنید.

۱۰۵. بازطراحی نمایش ارتباط‌های «پروژه‌های تحول» طبق درخواست کاربر و
    بحثی که با هم داشتیم:

    الف) ۴ نوع رابطه‌ی موجود (اهداف استراتژیک، شاخص‌های KPI، راهبردهای
    TOWS، ریسک‌های مبنا) از حالت «پیل جدا برای هر مورد» (که برای پروژه‌های
    پرارتباط شلوغ می‌شد) به همون الگوی یکدست سراسری تبدیل شد: یک بج
    جمع‌وجور با شمارنده به‌ازای هر نوع رابطه، با هاور تک که همه‌ی موارد
    همون نوع رو با ● از هم جدا و کامل نشون می‌ده.

    ب) یک بج کاملاً جدید و **خودکار** هم اضافه شد: «ریشه‌ی راهبردی».
    این بج هیچ فیلد یا رابطه‌ی دیتابیسی جدیدی نداره — فقط زنجیره‌ی از
    قبل موجود رو دنبال می‌کنه: پروژه ← راهبرد TOWS مبنا ← موارد SWOT
    مبنای همون راهبرد ← منبع اصلی هرکدوم (PESTEL/Porter/ذینفع/McKinsey
    7S/زنجیره ارزش/سناریو). روی داده‌ی واقعی تست شد — مثلاً پروژه‌ی
    «برنامه‌های کمیته‌های تاب‌آوری» درست تا «سناریوی هوای مه‌آلود»
    ردیابی شد.

    ⚠️ طبق گفته‌ی خود کاربر: اگه این بج «ریشه‌ی راهبردی» بعد از دیدن
    عملی، خوب/مفید به‌نظر نرسید، فقط کافیه بگه تا حذفش کنم — چون رابطه‌ی
    دیتابیسی جدیدی نساخته، حذفش هم بی‌خطر و سریعه (فقط حذف یه پراپرتی و
    یه بج).

    فایل‌هایی که باید overwrite شوند:
    - strategic/models.py
    - strategic/views.py
    - templates/strategic/roadmap.html

    نیازی به هیچ دستوری نیست — فقط فایل‌ها را جایگزین و سرور را ری‌استارت
    کنید.

۱۰۵. بازطراحی نمایش ارتباطات صفحه‌ی «پروژه‌های تحول» + یه لایه‌ی ردیابی
    خودکار جدید:

    الف) چهار نوع رابطه‌ای که هر پروژه می‌تونه داشته باشه (اهداف
    استراتژیک، شاخص‌های مبنا، راهبردهای TOWS، ریسک‌های مبنا) که قبلاً
    به‌شکل چند پیل جدا و پراکنده نشون داده می‌شدن، الان با همون الگوی
    یکدست سراسری سامانه (بج کوچیک با شمارنده + هاور واحد با ● بین هر
    مورد) نمایش داده می‌شن — دقیقاً هم‌شکل Porter/PESTEL/ذینفعان/الزامات.

    ب) یه بج کاملاً جدید و **خودکار** هم اضافه شد: «ریشه‌ی راهبردی».
    این بدون هیچ ثبت دستی جدید، زنجیره‌ی از قبل موجود توی سامانه رو
    دنبال می‌کنه: پروژه ← راهبرد TOWS مبنا ← موارد SWOT مبنای اون راهبرد
    ← منبع اصلی هرکدوم (PESTEL/Porter/ذینفع/McKinsey 7S/زنجیره ارزش/
    سناریو) — و روی یه پروژه‌ی واقعی توی دیتابیس تست شد (ریشه‌اش رو تا
    ۲ عامل PESTEL درست ردیابی کرد).

    ⚠️ این ردیابی فقط برای پروژه‌هایی کار می‌کنه که از راهبرد TOWS
    ساخته شدن و اون راهبرد به مورد SWOT وصله و اون مورد SWOT هم منبعش
    مشخص باشه — اگه جایی از این زنجیره خالی باشه، طبیعتاً همون‌جا قطع
    می‌شه (نه خطا، فقط چیزی برای اون پروژه نشون داده نمی‌شه).

    فایل‌هایی که باید overwrite شوند:
    - strategic/models.py
    - templates/strategic/roadmap.html

    نیازی به هیچ دستوری نیست (فقط پراپرتی پایتونی، بدون فیلد جدید) —
    فقط فایل‌ها را جایگزین و سرور را ری‌استارت کنید.

۱۰۵. یکدست‌سازی نمایش ارتباطات صفحه‌ی «پروژه‌های تحول» + یه لایه‌ی
    ردیابی خودکار جدید، طبق درخواست کاربر:

    الف) ۴ نوع رابطه‌ی پروژه (اهداف استراتژیک، شاخص‌های مبنا، راهبردهای
    TOWS، ریسک‌های مبنا) از حالت «چند پیل جدا و پخش‌شده» به همون الگوی
    یکدست سراسری («بج با شمارنده + یک هاور جمع‌وجور با ●») تبدیل شد —
    دقیقاً هم‌شکل PESTEL/Porter/ذینفعان/الزامات/بانک عوامل محیطی.

    ب) یه بج کاملاً جدید هم اضافه شد: **«ریشه‌ی راهبردی»** — که خودکار
    (بدون هیچ رابطه‌ی دیتابیسی جدید یا کار دستی) زنجیره‌ی
    پروژه←TOWS←SWOT←منبع اصلی (PESTEL/Porter/ذینفع/McKinsey 7S/زنجیره
    ارزش/سناریو) رو دنبال می‌کنه و نشون می‌ده این پروژه در نهایت از کجای
    تحلیل استراتژیک ریشه گرفته. این کاملاً از رابطه‌های از قبل موجود
    (source_tows، source_items، source_pestel/...) ساخته شده، پس فقط
    برای پروژه‌هایی نشون داده می‌شه که این زنجیره کامل باشه.

    این ویژگی با یک سناریوی واقعی کامل (پروژه ← راهبرد TOWS ← مورد
    SWOT ← عامل PESTEL «تحریم‌ها») روی کپی تازه‌ای از بکاپ اصلی تست شد
    و هاور دقیقاً زنجیره‌ی درست رو نشون داد.

    ⚠️ کاربر گفته اگه این بخش «ریشه‌ی راهبردی» رو نپسندید، فقط بگید تا
    حذفش کنم — بدون اثر روی بقیه‌ی تغییرات این بند.

    فایل‌هایی که باید overwrite شوند:
    - strategic/models.py
    - templates/strategic/roadmap.html

    نیازی به هیچ دستوری نیست (بدون فیلد جدید دیتابیس) — فقط فایل‌ها را
    جایگزین و سرور را ری‌استارت کنید.

۱۰۶. اطلاعات کامل‌تر برای هاور بج «ریشه‌ی راهبردی» طبق درخواست کاربر —
    قبلاً فقط دسته‌ی TOWS و یه برچسب کوتاه منبع نشون می‌داد؛ الان برای
    هر ریشه، ۳ خط کامل:
    خط ۱: متن کامل راهبرد TOWS (نه فقط دسته‌بندیش)
    خط ۲: متن کامل مورد SWOT مبنا
    خط ۳: جزئیات کامل منبع اصلی — برای PESTEL/Porter شامل حرف/نام‌بُعد
    یا نیرو + متن + نوع اثر + استاندارد مرتبط (اگه داشت)؛ برای ذینفع
    شامل نام + واحد + نیاز/انتظار؛ برای McKinsey 7S/زنجیره ارزش/سناریو
    هم عنوان کامل.

    این تغییرات با همون سناریوی تست قبلی (پروژه ← TOWS ← SWOT ← PESTEL
    «تحریم‌ها») روی کپی تازه‌ای از بکاپ اصلی دوباره تست شد و خروجی کامل
    و درست بود.

    فایل‌هایی که باید overwrite شوند:
    - strategic/models.py
    - templates/strategic/roadmap.html

    نیازی به هیچ دستوری نیست — فقط فایل‌ها را جایگزین و سرور را ری‌استارت
    کنید.

۱۰۷. ارتباط «ریسک ← SWOT» (طبق بحث مفصلی که با هم داشتیم درباره‌ی جایگاه
    درست ریسک در زنجیره‌ی استراتژیک — طبق استاندارد ISO 31000، فقط
    تهدید/ضعف SWOT می‌تونن منشأ ریسک باشن، نه فرصت/قوت، و نه TOWS چون
    TOWS خودش «پاسخ» است نه «منشأ»):

    الف) فیلد جدید و اختیاری `related_swot_items` (چندانتخابی) به مدل
    Risk اضافه شد — فقط از بین موارد «تهدید» و «ضعف» SWOT قابل‌انتخابه
    (فرصت/قوت اصلاً توی لیست نمیان). یه migration جدید لازمه:
    python manage.py makemigrations strategic
    python manage.py migrate

    ب) یه بج کاملاً خودکار جدید هم اضافه شد: **«ریشه»** — دقیقاً مثل
    «ریشه‌ی راهبردی» پروژه‌های تحول، از موارد SWOT وصل‌شده، خودکار منبع
    اصلی (PESTEL/Porter/ذینفع/...) رو دنبال و نشون می‌ده — بدون کار
    دستی اضافه.

    ج) یه دستور جدید (`link_risks_to_swot`) هم بر اساس تحلیل محتوایی،
    ۱۱ ارتباط منطقی بین ۴ تا از ۹ ریسک کلان شرکت و موارد تهدید/ضعف
    SWOT مرتبط (در ۵ کسب‌وکار) برقرار می‌کنه — فقط جاهایی که موضوع
    کاملاً روشن بود؛ ۵ ریسک دیگه بدون ارتباط موندن چون هیچ مورد SWOT
    با موضوع مشابه پیدا نشد.

    ⚠️ **نکته‌ی مهم و صادقانه:** بج «ریشه» فعلاً برای اکثر ریسک‌ها خالی
    می‌مونه — نه چون خراب باشه، بلکه چون از ۱۳۶ مورد SWOT واقعی سامانه،
    فقط ۳ تاش «منبع» (PESTEL/Porter/...) مشخص‌شده دارن. برای این‌که این
    ردیابی خودکار واقعاً مفید بشه، باید تیم شما بره توی خود صفحه‌ی SWOT
    و برای موارد بیشتری منبعشون رو مشخص کنه.

    ⚠️ همون‌طور که توافق کردیم: این ۱۱ ارتباط، تحلیل اولیه‌ی هوش مصنوعیه
    — لطفاً بازبینی کنید و هر مورد اشتباه رو دستی اصلاح کنید.

    فایل‌هایی که باید overwrite شوند:
    - strategic/models.py
    - strategic/forms.py
    - strategic/migrations/0059_risk_related_swot_items.py (فایل جدید)
    - strategic/management/commands/link_risks_to_swot.py (فایل جدید)
    - templates/strategic/risk.html

    ترتیب دقیق دستورها بعد از overwrite کردن فایل‌ها:
    python manage.py makemigrations strategic
    python manage.py migrate
    python manage.py link_risks_to_swot

۱۰۸. اتصال محتوایی موارد SWOT به منبع اصلی‌شان (طبق درخواست کاربر) —
    یه دستور جدید و بدون migration:
    python manage.py link_swot_sources

    از ۱۳۶ مورد SWOT (در ۵ کسب‌وکار)، **۱۱۴ مورد** بر اساس تحلیل محتوایی
    به منبع اصلی وصل شدن — طبق قانون کلی سامانه:
    - فرصت‌ها و تهدیدها (۵۲ مورد) → عوامل PESTEL/Porter (بیرونی)
    - قوت‌ها و ضعف‌ها (۶۲ مورد) → مؤلفه‌های McKinsey 7S یا زنجیره ارزش
      (درونی)

    این کار باعث شد بج‌های «ریشه‌ی خودکار» که قبلاً برای پروژه‌های تحول
    (بند ۱۰۵-۱۰۶) و ریسک‌ها (بند ۱۰۷) ساخته بودیم، حالا واقعاً محتوا
    نشون بدن — تست شد و روی ریسک «تحریم‌ها» تأیید شد که زنجیره‌ی کامل
    (ریسک ← SWOT ← PESTEL «تحریم‌ها») درست کار می‌کنه.

    ⚠️ طبق توافق همیشگی: این تحلیل اولیه‌ی هوش مصنوعیه، لطفاً بازبینی
    کنید. ۲۲ مورد باقی‌مانده عمداً بدون منبع موندن چون محتوایی که با
    اطمینان بشه بهش وصل کرد پیدا نشد.

    فایل جدیدی که باید اضافه بشه:
    - strategic/management/commands/link_swot_sources.py

    دستور لازم بعد از اضافه‌کردن فایل (migration لازم نیست):
    python manage.py link_swot_sources

۱۰۹. هاور «منبع» توی صفحه‌ی SWOT کامل و حرفه‌ای شد، طبق درخواست کاربر —
    قبلاً فقط اسم کلی بُعد/نیرو رو نشون می‌داد؛ الان:
    - برای PESTEL: دقیقاً «حرف انگلیسی (نام فارسی بُعد) — متن کامل عامل
      — نوع اثر — استاندارد مرتبط (اگه داشت)» — مثلاً «E (اقتصادی) —
      تغییرات نرخ ارز و تورم — نوع اثر: تهدید — استاندارد: ۱۰۰۰۴ و۹۰۰۱»
    - برای Porter: دقیقاً «نام نیرو — متن کامل همون عامل مشخص داخل اون
      نیرو — نوع اثر» — مثلاً «قدرت چانه‌زنی تأمین‌کنندگان — محدود بودن
      تولیدکنندگان داخلی با کیفیت — نوع اثر: فرصت»

    این با افزودن یک پراپرتی جدید (`source_label_full`) به مدل SWOTItem
    انجام شد که از همون پراپرتی غنی `source_full_detail` (که قبلاً برای
    ردیابی خودکار پروژه‌ها/ریسک‌ها ساخته بودیم) استفاده می‌کنه — خود بج
    کوچیک (متن قابل‌مشاهده روی صفحه) کوتاه و جمع‌وجور موند، فقط هاورش
    کامل شد.

    فایل‌هایی که باید overwrite شوند:
    - strategic/models.py
    - templates/strategic/swot.html

    نیازی به هیچ دستوری نیست — فقط فایل‌ها را جایگزین و سرور را ری‌استارت
    کنید.

۱۱۰. اتصال آخرین حلقه‌ی باقی‌مانده (ماتریس اثر متقابل ↔ PESTEL/Porter) و
    یکدست‌سازی نمایش آن با استاندارد سراسری سامانه:

    الف) از ۱۵ عامل ماتریس اثر متقابل (MICMAC)، **۱۴ تا** به عامل
    PESTEL یا نیروی Porter معادلشون وصل شدن (۱۲ تا تطبیق دقیق، ۲ تا
    نماینده‌ی کلی یک نیرو/دسته چون این عوامل خلاصه‌شده‌ن). فقط «ثبات
    مدیریت» بدون ارتباط موند — هیچ عامل PESTEL/Porter متناظری نداره.
    python manage.py link_cross_impact_sources

    ب) خود صفحه‌ی ماتریس اثر متقابل هم از سبک قدیمی (بج آیکون‌تنها، هاور
    ناقص که برای Porter فقط اسم نیرو رو نشون می‌داد، نه عامل دقیق) به
    همون استاندارد سراسری سامانه (بج با متن + هاور کامل: حرف/نام‌بُعد
    یا نام‌نیرو + متن دقیق عامل + نوع اثر) ارتقا یافت.

    با این تغییر، **همه‌ی ماژول‌های سامانه که رابطه‌ی از قبل تعریف‌شده
    داشتن، الان یا وصل شدن یا آگاهانه (با دلیل مشخص) وصل نشدن** — تنها
    دو ماژول کاملاً مستقل باقی مونده (هوش رقابتی و بازار، اسناد و
    دستورالعمل‌ها) که اصلاً فیلد ارتباطی توی مدلشون تعریف نشده.

    فایل‌هایی که باید overwrite شوند:
    - strategic/management/commands/link_cross_impact_sources.py (فایل جدید)
    - templates/strategic/cross_impact.html

    دستور لازم بعد از اضافه‌کردن فایل (migration لازم نیست):
    python manage.py link_cross_impact_sources

۱۱۱. دو ویژگی جدید در صفحه‌ی «سناریوهای راهبردی»، طبق درخواست کاربر و
    بحث مفصلی که درباره‌ی استاندارد سناریوپردازی داشتیم:

    الف) **اتصال محورها به ماتریس اثر متقابل**: دو محور عدم‌قطعیت
    سناریو (که ۴ سناریو از تقاطعشون ساخته می‌شن) حالا می‌تونن به
    پیشران معادلشون توی ماتریس اثر متقابل وصل بشن — فیلد جدید
    `axis1_source`/`axis2_source` روی مدل ScenarioAxes. یه migration
    جدید لازمه:
    python manage.py makemigrations strategic
    python manage.py migrate

    کنار عنوان هر محور (بالای نمودار ۲×۲)، یه بج کوچیک نشون داده می‌شه
    که با هاور، مسیر کامل تا منبع اصلی (PESTEL/Porter) رو نشون می‌ده.
    دستور زیر ۲ محور موجود («تأمین منابع مالی» و «ارتباطات بین‌المللی»)
    رو به پیشران معادلشون وصل می‌کنه:
    python manage.py link_scenario_axes

    ب) **برجسته‌سازی هوشمند متن روایت سناریو**: وقتی متن یه سناریو
    (مثل «هوای مه‌آلود») رو می‌نویسید، سامانه خودکار دنبال کلماتی
    می‌گرده که با عوامل PESTEL/Porter هم‌خوانی دارن (مثلاً «ناترازی
    انرژی» → عامل PESTEL «ناترازی منابع انرژی») و اون‌ها رو رنگی و
    برجسته می‌کنه، با هاور که منبع دقیق رو نشون می‌ده.

    ⚠️ **نکته‌ی مهم که کاربر هم توافق کرد**: این «بهترین‌تلاش» هوش
    مصنوعیه، نه بخشی از متدولوژی کلاسیک سناریوپردازی (شوارتز/GBN) —
    و چون تطبیق تقریبیه (نه دقیق صددرصد)، امکان جاافتادن بعضی موارد یا
    برجسته‌شدن اشتباهی یه عبارت وجود داره. تست شد و روی متن واقعی
    «هوای مه‌آلود» که کاربر داد، ۳ مورد (ناترازی انرژی، دسترسی به نیروی
    انسانی توانمند، نرخ سرمایه‌گذاری) درست و دقیق تشخیص داده شد.

    فایل‌هایی که باید overwrite شوند:
    - strategic/models.py
    - strategic/forms.py
    - strategic/migrations/0060_scenarioaxes_axis1_source_scenarioaxes_axis2_source.py (فایل جدید)
    - strategic/management/commands/link_scenario_axes.py (فایل جدید)
    - templates/strategic/scenarios.html

    ترتیب دقیق دستورها بعد از overwrite کردن فایل‌ها:
    python manage.py makemigrations strategic
    python manage.py migrate
    python manage.py link_scenario_axes

۱۱۲. رفع یه مورد جامانده در برجسته‌سازی هوشمند متن سناریو (بند ۱۱۱):
    هاور کلمات برجسته‌شده، برخلاف بقیه‌ی سامانه، نمی‌گفت منبع از PESTEL
    اومده یا از Porter — فقط جزئیات رو نشون می‌داد. الان دقیقاً مثل همه‌
    جای دیگه، با پیشوند «PESTEL:» یا «Porter:» شروع می‌شه.

    فایلی که باید overwrite شود:
    - strategic/models.py

    نیازی به هیچ دستوری نیست — فقط فایل را جایگزین و سرور را ری‌استارت
    کنید.

۱۱۳. سه اصلاح روی برجسته‌سازی هوشمند متن سناریو، بعد از این‌که مدیر متن
    واقعی «هوای مه‌آلود» رو بررسی کرد و پرسید چرا بعضی کلمات (که خودش
    توی متن می‌دید) رنگی نمی‌شن:

    الف) **باگ واقعی رفع شد**: کلماتی با املای متفاوت هم‌معنی (مثل
    «تامین‌کنندگان» بدون همزه در برابر «تأمین‌کنندگان» با همزه توی
    دیتابیس) قبلاً یکی تشخیص داده نمی‌شدن. الان یکسان‌سازی می‌شن.

    ب) **باگ واقعی دیگه رفع شد**: علامت ویرگول فارسی (،) به‌اشتباه بخشی
    از کلمه در نظر گرفته می‌شد (مثلاً «قوانین،» به‌جای «قوانین»)، که
    باعث می‌شد تطبیق‌های درست هم رد بشن. الان علائم نگارشی درست پاک
    می‌شن، هم موقع تشخیص هم موقع نمایش (بدون ویرگول اضافه‌ی چسبیده
    به کلمه‌ی برجسته‌شده).

    ج) آستانه‌ی تطبیق برای عبارت‌های بلندتر (۴ کلمه‌ای یا بیشتر) کمی
    شل‌تر شد (قبلاً باید ۷۵٪ کلمات پیدا می‌شد، الان ۵۰٪ کافیه).

    نتیجه: روی همون متن واقعی «هوای مه‌آلود» که مدیر بررسی کرد، تعداد
    کلمات برجسته‌شده از ۴ به ۸ رسید (اضافه شد: «تامین‌کنندگان»، «قوانین
    و استانداردها»، «نرخ ارز»، «قطعات غیراصلی»). یک مورد («ارتباطات
    بین‌المللی») هنوز جا می‌افته چون متن مدیر اصلاً کلمه‌ی «ارتباطات»
    رو نداره (نوشته «انزوای... بین‌المللی») — این محدودیت ذاتی تشخیص
    بر پایه‌ی کلمه است، نه باگ.

    فایلی که باید overwrite شود:
    - strategic/models.py

    نیازی به هیچ دستوری نیست — فقط فایل را جایگزین و سرور را ری‌استارت
    کنید.

۱۱۴. اصلاح چیدمان ردیف اول کارت‌های PESTEL/Porter — طبق عکسی که کاربر
    فرستاد، وقتی یک عامل چندین بج هم‌زمان داشت (نوع اثر + الزامات
    قانونی + ذینفعان)، چون همه با هم توی یک خط جا نمی‌شدن، مرورگر
    نامنظم می‌شکوندشون و یه ردیف بصری اضافه بین متن و ردیف اولویت/اثر
    ایجاد می‌شد. این یک مشکل کد نبود، فقط چیدمان `.txt` هنوز به‌شکل متن
    ساده بود، نه یک ظرف فلکس منظم.

    الان `.txt` به یک فلکس‌باکس با `flex-wrap` و فاصله‌ی یکسان (`gap`)
    تبدیل شد، و خود متن عامل هم داخل یک `span` مستقل (`txt-main`) قرار
    گرفت تا مثل بقیه‌ی بج‌ها به‌عنوان یک آیتم فلکس مستقل بشکنه. نتیجه:
    هر چند بج هم باشه، منظم و با فاصله‌ی یکسان می‌شکونن، بدون این‌که
    ردیف اولویت/اثر/احتمال جابه‌جا بشه.

    فایل‌هایی که باید overwrite شوند:
    - templates/strategic/pestel.html
    - templates/strategic/porter.html
    - templates/strategic/_porter_item.html

    نیازی به هیچ دستوری نیست — فقط فایل‌ها را جایگزین و سرور را ری‌استارت
    کنید.

۱۱۵. طرح جدید ردیف‌بندی PESTEL/Porter، طبق درخواست صریح کاربر (جایگزین
    کامل بند ۱۱۴ که راه‌حلش کافی نبود):

    الف) ردیف اول حالا فقط شامل «متن عامل + نوع اثر + استاندارد مرتبط»
    است — همون شکل ساده‌ی قبلی، بدون بج‌های ارتباطی.

    ب) بج‌های «الزامات قانونی مرتبط» (PESTEL) و «ذینفعان مرتبط»
    (PESTEL و Porter) کاملاً از ردیف اول برداشته و به **انتهای ردیف
    دوم، دقیقاً بعد از «راهنما»** منتقل شدن — همون ردیفی که اولویت/اثر/
    احتمال/عدم‌قطعیت/افق/روند توش هستن. چون این ردیف از قبل هم `flex-
    wrap` و `gap` داشت، هر چقدر بج اضافه بشه، جلوش جا باز می‌کنه بدون
    این‌که ردیف اول رو به‌هم بریزه.

    فایل‌هایی که باید overwrite شوند:
    - templates/strategic/pestel.html
    - templates/strategic/porter.html
    - templates/strategic/_porter_item.html

    نیازی به هیچ دستوری نیست — فقط فایل‌ها را جایگزین و سرور را ری‌استارت
    کنید (یک Ctrl+F5 هم بزنید چون CSS تغییر کرده).

۱۱۶. تکمیل بند ۱۱۵ — کاربر درست گفت فقط بج «الزامات قانونی» جابه‌جا
    شده بود، ولی بج‌های «استفاده در SWOT» و «استفاده در اثرات متقابل»
    هنوز اول ردیف دوم (قبل از اولویت) بودن. الان اون‌ها هم به همون
    ترتیب نهایی منتقل شدن:

    اولویت ← اثر ← احتمال ← عدم‌قطعیت ← افق ← روند ← راهنما ← SWOT ←
    اثرات متقابل ← الزامات قانونی ← ذینفعان

    یعنی «اولویت» الان دقیقاً اولین آیتم ردیف دوم است، و همه‌ی بج‌های
    ارتباطی (بدون استثنا) بعد از «راهنما» می‌آیند.

    فایل‌هایی که باید overwrite شوند:
    - templates/strategic/pestel.html
    - templates/strategic/_porter_item.html

    نیازی به هیچ دستوری نیست — فقط فایل‌ها را جایگزین و سرور را ری‌استارت
    کنید.

۱۱۷. هاور کامل‌تر برای بج «استفاده در SWOT» توی PESTEL و Porter — طبق
    درخواست کاربر. قبلاً فقط «کسب‌وکار (نوع)» رو نشون می‌داد، بدون متن
    یا کد دقیق. الان برای هر مورد: «● کسب‌وکار — کد (نوع): متن کامل
    همون بند SWOT» — دقیقاً هم‌الگو با کاری که قبلاً برای McKinsey 7S
    ساختیم.

    یه تابع کمکی مشترک جدید (`_swot_code_map`) به views.py اضافه شد که
    کد S/W/O/T رو یک‌جا محاسبه می‌کنه (به‌جای تکرار همون منطق در هر
    ویو) — ویو McKinsey 7S هم برای استفاده از همین تابع مشترک به‌روز
    شد، بدون تغییر در رفتارش.

    فایل‌هایی که باید overwrite شوند:
    - strategic/views.py
    - templates/strategic/pestel.html
    - templates/strategic/_porter_item.html

    نیازی به هیچ دستوری نیست — فقط فایل‌ها را جایگزین و سرور را ری‌استارت
    کنید.

۱۱۸. افکت «نور چرخان دور کادر» برای کارت‌های صفحه‌ی خانه (اینفوگرافی) —
    طبق درخواست کاربر، **فقط موقع هاور** فعال می‌شه (نه دائمی، تا شلوغ
    نشه): وقتی ماوس روی هر ایستگاه می‌ره، یه حلقه‌ی نور به‌رنگ خودِ همون
    ایستگاه، آروم دور لبه‌ی کادر می‌چرخه. با CSS خالص (بدون جاوااسکریپت
    اضافه) پیاده شد، پس هیچ اثری روی کارایی صفحه نداره.

    فایلی که باید overwrite شود:
    - templates/strategic/home.html

    نیازی به هیچ دستوری نیست — فقط فایل را جایگزین و سرور را ری‌استارت
    کنید (یک Ctrl+F5 هم بزنید چون CSS تغییر کرده).

۱۱۹. هاور کامل‌تر برای بج «استفاده در SWOT» توی صفحه‌ی «زنجیره ارزش
    پورتر» — طبق درخواست کاربر، بعد از تأیید این‌که طبق متدولوژی استاندارد
    پورتر، زنجیره ارزش فقط باید به SWOT (قوت/ضعف) وصل باشه، نه چیز
    دیگه‌ای — و این ارتباط از قبل کامل بود، فقط نمایشش (بج) قدیمی بود.

    الان دقیقاً هم‌الگو با PESTEL/Porter/McKinsey7S: «● کسب‌وکار — کد
    (نوع): متن کامل». از همون تابع مشترک `_swot_code_map` (بند ۱۱۷)
    استفاده شد.

    فایل‌هایی که باید overwrite شوند:
    - strategic/views.py
    - templates/strategic/value_chain.html

    نیازی به هیچ دستوری نیست — فقط فایل‌ها را جایگزین و سرور را ری‌استارت
    کنید.

۱۲۰. جایگزینی کامل «برجسته‌سازی خودکار» متن سناریو (بندهای ۱۱۱-۱۱۳) با
    «برچسب‌گذاری دستیِ کاربر» — طبق تصمیم صریح کاربر، چون تشخیص خودکار
    گاهی موارد رو جا می‌انداخت. الان کاملاً حذف شد و این جایگزینش شد:

    الف) توی فرم ویرایش هر سناریو، یه بخش جدید «برچسب‌گذاری دستی متن»
    اضافه شد: کاربر تیکه‌ای از متن روایت رو با ماوس انتخاب می‌کنه، از
    یه دراپ‌داون (گروه‌بندی‌شده بر اساس ناحیه‌ی ماتریس) عامل مرتبط رو
    انتخاب می‌کنه، و «افزودن» می‌زنه. لیست برچسب‌های ثبت‌شده (با دکمه‌ی
    حذف) پایین همون‌جا نشون داده می‌شه.

    ب) موقع نمایش سناریو، همون تیکه‌های متن **دقیقاً و فقط جایی که خود
    کاربر مشخص کرده** پررنگ می‌شن — با رنگ **قرمز برای عوامل «پیشران‌ها»**
    (طبق درخواست مدیر) و رنگ خاص هر ناحیه برای بقیه (دوجانبه/رصد/خنثی).
    هاور هر برچسب، هم متن کامل عامل ماتریس، هم منبع اصلی‌اش (PESTEL/
    Porter پشت‌سرش) رو نشون می‌ده.

    ج) یه مدل جدید (`ScenarioHighlight`) این ارتباط‌ها رو نگه می‌داره
    (سناریو + تکه‌متن + عامل ماتریس). یه migration جدید لازمه:
    python manage.py makemigrations strategic
    python manage.py migrate

    د) توابع تشخیص خودکار قدیمی (نرمال‌سازی کلمات، تطبیق تقریبی و...)
    کاملاً از کد حذف شدن، نه فقط غیرفعال — سبک‌تر و ساده‌تر شد.

    فایل‌هایی که باید overwrite شوند:
    - strategic/models.py
    - strategic/views.py
    - strategic/migrations/0061_scenariohighlight.py (فایل جدید)
    - templates/strategic/scenarios.html

    ترتیب دقیق دستورها بعد از overwrite کردن فایل‌ها:
    python manage.py makemigrations strategic
    python manage.py migrate

۱۲۱. هاور کامل‌تر برای دو بج بالای هر ۴ کارت سناریو (SWOT و ریسک) —
    طبق درخواست کاربر، همون مشکل «اطلاعات کم» که توی PESTEL/Porter/
    زنجیره ارزش داشتیم، اینجا هم بود:

    - بج SWOT: قبلاً فقط «کسب‌وکار (نوع)»؛ الان «● کسب‌وکار — کد (نوع):
      متن کامل» — هم‌الگو با بقیه‌ی سامانه.
    - بج ریسک: قبلاً فقط عنوان ریسک؛ الان «● عنوان — دسته — علت —
      راهبرد پاسخ (کاهش/انتقال/پذیرش/اجتناب)».

    فایل‌هایی که باید overwrite شوند:
    - strategic/views.py
    - templates/strategic/scenarios.html

    نیازی به هیچ دستوری نیست — فقط فایل‌ها را جایگزین و سرور را ری‌استارت
    کنید.

۱۲۲. اتصال محتوایی موارد SWOT و ریسک به سناریوی «هوای مه‌آلود» (طبق
    تحلیل متن واقعی این سناریو که کاربر تأیید کرد) — یه دستور جدید و
    بدون migration:
    python manage.py link_foggy_scenario

    ۲۰ مورد SWOT (تهدید، در ۵ کسب‌وکار) که موضوعشون دقیقاً توی متن این
    سناریو اومده (تحریم، تورم، ناترازی انرژی، خروج نیروی انسانی، کمبود
    نقدینگی، کمبود مواد اولیه، قطعات تقلبی، کاهش قدرت خرید، مهاجرت به
    تعمیرگاه آزاد) و ۵ از ۹ ریسک کلان شرکت وصل شدن.

    ⚠️ نکته‌ی فنی مهم: چون مدل SWOTItem فقط اجازه‌ی یک «منبع اصلی» در
    یک زمان می‌ده (اولویت با PESTEL/Porter)، این دستور «منبع سناریو» رو
    **علاوه بر** منبع قبلی (نه به‌جای اون) ثبت می‌کنه — یعنی چیزی که قبلاً
    (بند ۱۰۸) به PESTEL/Porter وصل بود، همچنان درست نمایش داده می‌شه؛
    فقط الان بج «مرتبط با این سناریو» هم روی کارت سناریو اضافه شده. این
    قبل از اجرا با یه تست دقیق (روی کپی تازه‌ای از بکاپ اصلی) تأیید شد.

    فایل جدیدی که باید اضافه بشه:
    - strategic/management/commands/link_foggy_scenario.py

    دستور لازم بعد از اضافه‌کردن فایل (migration لازم نیست):
    python manage.py link_foggy_scenario
