# -*- coding: utf-8 -*-
"""اتصال کارت‌های نقشه استراتژیک «آپشن» به شاخص‌های عملیاتی مرتبط —
بر اساس تحلیل محتوایی کارشناسی روی کل بانک ۲۴۲ شاخص. این کسب‌وکار یه
دپارتمان اختصاصی («مدیریت آپشن») با ۱۶ شاخص خودش داره، به‌علاوه‌ی
شاخص‌های عمومی پلتفرم/شایستگی/بهبود مستمر."""
from django.core.management.base import BaseCommand
from strategic.models import StrategicObjective, OperationalKPI, BusinessUnit


# (کد کارت، [کدهای شاخص عملیاتی مرتبط])
KPI_LINKS = [
    ("F1", ["SP-22-01", "SP-27-01"]),
    # سود عملیاتی آپشن ← سود ناخالص قطعات بازرگانی (آپشن) + حاشیه سود ناخالص قطعات آپشن

    ("F3", ["P-31-00"]),
    # استراتژی رشد ← فروش خالص ریالی قطعات آپشن (روند رشد کل فروش)

    ("F6", ["P-30-00", "P-30-01"]),
    # درآمد قطعات و اپلیکیشن‌های اختصاصی آپشن ← فروش ریالی/تعدادی خالص قطعات آپشن (AM)

    ("F7", ["C-01-33"]),
    # درآمد اتصال تکمیل‌کنندگان به پلتفرم‌ها ← میزان فروش حاصل از واسطه‌گری

    ("C1", ["C-01-60"]),
    # تنوع قطعات جانبی و اپلیکیشن‌ها ← تنوع اقلام قابل ارائه در پلتفرم فروش واسطه‌گری

    ("C3", ["C-01-61"]),
    # دسترسی آسان به پلتفرم ارتباطی و مولتی‌مدیا تا فروش ← تعداد خریداران منفرد در فروش واسطه‌گری

    ("C4", ["C-01-40"]),
    # باورمندی به استفاده وسیع از پلتفرم ← فروشندگان/تأمین‌کنندگان/توزیع‌کنندگان جذب‌شده

    ("C5", ["P-32-00"]),
    # رشد سریع مشتریان ← فروش خالص تعدادی قطعات آپشن (رشد حجم مشتری/تراکنش)

    ("P3", ["C-01-40"]),
    # جذب/حفظ تکمیل‌کنندگان ← فروشندگان/تأمین‌کنندگان/توزیع‌کنندگان جذب‌شده (همان مخاطب هدف)

    ("P4", ["C-01-40"]),
    # ارائه خدمات و مزایا به تکمیل‌کنندگان ← فروشندگان/تأمین‌کنندگان جذب‌شده (همان مخاطب هدف)

    ("P7", ["P-214-00"]),
    # آگاهی‌بخشی به مشتریان از قابلیت‌ها ← رضایت مشتریان از خدمات آپشن (پیامد آگاهی‌بخشی مؤثر)

    ("P9", ["C-01-61"]),
    # افزایش کاربردهای پلتفرم با حفظ قابلیت‌های نسخه‌های پیشین ← تعداد خریداران منفرد (شاخص کاربرد واقعی)

    ("P12", ["HR-94-00"]),
    # استانداردهای ایمنی و بهداشت شغلی — الزامات زیست‌محیطی ← ضریب شدت تکرار حوادث

    ("L1", ["HR-100-27", "HR-100-05", "HR-100-20"]),
    # شایستگی‌محور، مشاغل، جانشین‌پروری ← انطباق شایستگی/احراز شغلی

    ("L4", ["P-29-00", "P-29-01"]),
    # توسعه پلتفرم تأمین OEM آفلاین فروشگاه ← فروش ریالی/تعدادی قطعات آپشن (OEM آفلاین اختیاری) — تطابق مستقیم

    ("L5", ["SN-01-00", "SN-01-01", "SN-01-02"]),
    # توسعه فرهنگ بهبود مستمر ← سرانه پیشنهادات + نرخ مشارکت + ضریب پذیرش پیشنهادات
]


class Command(BaseCommand):
    help = "کارت‌های نقشه استراتژیک آپشن را به شاخص‌های عملیاتی مرتبط وصل می‌کند."

    def handle(self, *args, **options):
        bu = BusinessUnit.objects.get(name__icontains="آپشن")
        kpi_by_code = {k.code: k for k in OperationalKPI.objects.all()}

        linked, not_found = 0, []
        for card_code, kpi_codes in KPI_LINKS:
            try:
                obj = StrategicObjective.objects.get(business_unit=bu, code=card_code)
            except StrategicObjective.DoesNotExist:
                not_found.append(f"کارت {card_code} یافت نشد")
                continue
            obj.linked_operational_kpis.clear()
            for kpi_code in kpi_codes:
                kpi = kpi_by_code.get(kpi_code)
                if not kpi:
                    not_found.append(f"شاخص {kpi_code} یافت نشد")
                    continue
                obj.linked_operational_kpis.add(kpi)
                linked += 1

        self.stdout.write(self.style.SUCCESS(f"ثبت شد: {linked} ارتباط شاخص عملیاتی روی {len(KPI_LINKS)} کارت."))
        if not_found:
            self.stdout.write(self.style.WARNING("موارد یافت‌نشده:"))
            for msg in not_found:
                self.stdout.write("  - " + msg)
